# Source: ComplianceAsCode/content (scap-security-guide) — BSD-3-Clause
# Imported by StigForge; cloud-breaker tasks stripped per policy/cloud-ami.yml
---
- block:
  - name: Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the package facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the package facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure prelink is Removed
    ansible.builtin.package:
      name: prelink
      state: absent
    tags:
    - disable_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - package_prelink_removed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure prelink is Removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure prelink is Removed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure aide is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651010
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_aide_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure aide is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure aide is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure aide is installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - aide
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure aide is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure aide is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure AIDE is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure AIDE is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure AIDE is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Install cron
    ansible.builtin.package:
      name: cron
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Install cron'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Install cron'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure sudo is installed
    ansible.builtin.package:
      name: sudo
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_sudo_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sudo is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sudo is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure libpam-pwquality is installed
    ansible.builtin.package:
      name: libpam-pwquality
      state: present
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-215010
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_pam_pwquality_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure libpam-pwquality is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure libpam-pwquality is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure apparmor is installed
    ansible.builtin.package:
      name: apparmor
      state: present
    when: ansible_virtualization_type not in ["docker", "lxc", "openvz", "podman", "container"]
    tags:
    - DISA-STIG-UBTU-22-431010
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_apparmor_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure apparmor is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure apparmor is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog is installed
    ansible.builtin.package:
      name: rsyslog
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_rsyslog_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure systemd-journal-remote is installed
    ansible.builtin.package:
      name: systemd-journal-remote
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_systemd-journal-remote_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure systemd-journal-remote is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure systemd-journal-remote is installed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure iptables is installed
    ansible.builtin.package:
      name: iptables
      state: present
    when: ( "linux-base" in ansible_facts.packages )
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-1.4.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_iptables_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure iptables is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure iptables is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Remove iptables-persistent Package: Ensure iptables-persistent is removed'
    ansible.builtin.package:
      name: iptables-persistent
      state: absent
    when: '"ufw" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_iptables-persistent_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove iptables-persistent Package: Ensure iptables-persistent is removed''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove iptables-persistent Package: Ensure iptables-persistent is removed'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure nftables is installed
    ansible.builtin.package:
      name: nftables
      state: present
    when: ( "linux-base" in ansible_facts.packages )
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_nftables_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure nftables is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure nftables is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Remove ufw Package: Ensure ufw is removed'
    ansible.builtin.package:
      name: ufw
      state: absent
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_ufw_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove ufw Package: Ensure ufw is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove ufw Package: Ensure ufw is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure NetworkManager is installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - NetworkManager
    when: ( not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] ) )
    tags:
    - DISA-STIG-UBTU-22-291015
    - NIST-800-171-3.1.16
    - NIST-800-53-AC-18(3)
    - NIST-800-53-AC-18(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - PCI-DSS-Req-1.3.3
    - PCI-DSSv4-1.3
    - PCI-DSSv4-1.3.3
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - unknown_strategy
    - wireless_disable_interfaces
  rescue:
  - name: 'StigForge: continue after error in ''Ensure NetworkManager is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure NetworkManager is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall avahi Server Package: Ensure avahi-daemon is removed'
    ansible.builtin.package:
      name: avahi-daemon
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_avahi_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall avahi Server Package: Ensure avahi-daemon is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall avahi Server Package: Ensure avahi-daemon is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall the nis package: Ensure nis is removed'
    ansible.builtin.package:
      name: nis
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_nis_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall the nis package: Ensure nis is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall the nis package: Ensure nis is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall DHCP Server Package: Ensure isc-dhcp-server is removed'
    ansible.builtin.package:
      name: isc-dhcp-server
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_dhcp_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall DHCP Server Package: Ensure isc-dhcp-server is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall DHCP Server Package: Ensure isc-dhcp-server is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall bind Package: Ensure bind9 is removed'
    ansible.builtin.package:
      name: bind9
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_bind_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall bind Package: Ensure bind9 is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall bind Package: Ensure bind9 is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall vsftpd Package: Ensure vsftpd is removed'
    ansible.builtin.package:
      name: vsftpd
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-CM-7.1(ii)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(1).1(v)
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_vsftpd_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall vsftpd Package: Ensure vsftpd is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall vsftpd Package: Ensure vsftpd is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall apache2 Package: Ensure apache2 is removed'
    ansible.builtin.package:
      name: apache2
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_httpd_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall apache2 Package: Ensure apache2 is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall apache2 Package: Ensure apache2 is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall nginx Package: Ensure nginx is removed'
    ansible.builtin.package:
      name: nginx
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_nginx_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall nginx Package: Ensure nginx is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall nginx Package: Ensure nginx is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall cyrus-imapd Package: Ensure cyrus-imapd is removed'
    ansible.builtin.package:
      name: cyrus-imapd
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_cyrus-imapd_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall cyrus-imapd Package: Ensure cyrus-imapd is removed'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall cyrus-imapd Package: Ensure cyrus-imapd is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall dovecot Package: Ensure dovecot-core is removed'
    ansible.builtin.package:
      name: dovecot-core
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_dovecot_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall dovecot Package: Ensure dovecot-core is removed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall dovecot Package: Ensure dovecot-core is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Ensure LDAP client is not installed: Ensure ldap-utils is removed'
    ansible.builtin.package:
      name: ldap-utils
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_openldap-clients_removed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure LDAP client is not installed: Ensure ldap-utils is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure LDAP client is not installed: Ensure ldap-utils is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall openldap-servers Package: Ensure slapd is removed'
    ansible.builtin.package:
      name: slapd
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_openldap-servers_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall openldap-servers Package: Ensure slapd is removed'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall openldap-servers Package: Ensure slapd is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall nfs-kernel-server Package: Ensure nfs-kernel-server is removed'
    ansible.builtin.package:
      name: nfs-kernel-server
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_nfs-kernel-server_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall nfs-kernel-server Package: Ensure nfs-kernel-server is removed''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall nfs-kernel-server Package: Ensure nfs-kernel-server is removed'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Uninstall rpcbind Package: Ensure rpcbind is removed'
    ansible.builtin.package:
      name: rpcbind
      state: absent
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_rpcbind_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall rpcbind Package: Ensure rpcbind is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall rpcbind Package: Ensure rpcbind is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure systemd-timesyncd is installed
    ansible.builtin.package:
      name: systemd-timesyncd
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4
    - enable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_timesyncd_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure systemd-timesyncd is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure systemd-timesyncd is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall rsync Package: Ensure rsync is removed'
    ansible.builtin.package:
      name: rsync
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_rsync_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall rsync Package: Ensure rsync is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall rsync Package: Ensure rsync is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall rsh Package: Ensure rsh-client is removed'
    ansible.builtin.package:
      name: rsh-client
      state: absent
    tags:
    - NIST-800-171-3.1.13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_rsh_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall rsh Package: Ensure rsh-client is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall rsh Package: Ensure rsh-client is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall talk Package: Ensure talk is removed'
    ansible.builtin.package:
      name: talk
      state: absent
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_talk_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall talk Package: Ensure talk is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall talk Package: Ensure talk is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Remove telnet Clients: Ensure telnet is removed'
    ansible.builtin.package:
      name: telnet
      state: absent
    tags:
    - NIST-800-171-3.1.13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_telnet_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove telnet Clients: Ensure telnet is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove telnet Clients: Ensure telnet is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall CUPS Package: Ensure cups is removed'
    ansible.builtin.package:
      name: cups
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_cups_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall CUPS Package: Ensure cups is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall CUPS Package: Ensure cups is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall squid Package: Ensure squid is removed'
    ansible.builtin.package:
      name: squid
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_squid_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall squid Package: Ensure squid is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall squid Package: Ensure squid is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall Samba Package: Ensure samba is removed'
    ansible.builtin.package:
      name: samba
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_samba_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall Samba Package: Ensure samba is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall Samba Package: Ensure samba is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall net-snmp Package: Ensure snmp is removed'
    ansible.builtin.package:
      name: snmp
      state: absent
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_net-snmp_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall net-snmp Package: Ensure snmp is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall net-snmp Package: Ensure snmp is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Remove the X Windows Package Group: Ensure xserver-xorg is removed'
    ansible.builtin.package:
      name: xserver-xorg
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_xorg-x11-server-common_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove the X Windows Package Group: Ensure xserver-xorg is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove the X Windows Package Group: Ensure xserver-xorg is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the package facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the package facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable rsyslog Service - Enable service rsyslog
    block:
    - name: Enable rsyslog Service - Enable Service rsyslog
      ansible.builtin.systemd:
        name: rsyslog
        enabled: true
        state: started
        masked: false
      when:
      - '"rsyslog" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-652010
    - NIST-800-53-AU-4(1)
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_rsyslog_enabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable rsyslog Service - Enable service rsyslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable rsyslog Service - Enable service rsyslog'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable systemd-journald Service - Enable service systemd-journald
    block:
    - name: Enable systemd-journald Service - Enable Service systemd-journald
      ansible.builtin.systemd:
        name: systemd-journald
        enabled: true
        state: started
        masked: false
      when:
      - '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-24
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_systemd-journald_enabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable systemd-journald Service - Enable service systemd-journald'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable systemd-journald Service - Enable service systemd-journald'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Verify nftables Service is Enabled - Enable service nftables
    block:
    - name: Verify nftables Service is Enabled - Enable Service nftables
      ansible.builtin.systemd:
        name: nftables
        enabled: true
        state: started
        masked: false
      when:
      - '"nftables" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_nftables_enabled
    - special_service_block
    when: ( "nftables" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Verify nftables Service is Enabled - Enable service nftables'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify nftables Service is Enabled - Enable service nftables'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Verify ufw Enabled - Enable service ufw
    block:
    - name: Verify ufw Enabled - Enable Service ufw
      ansible.builtin.systemd:
        name: ufw
        enabled: true
        state: started
        masked: false
      when:
      - '"ufw" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-251020
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_ufw_enabled
    - special_service_block
    when:
    - '"linux-base" in ansible_facts.packages'
    - ( "ufw" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Verify ufw Enabled - Enable service ufw'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify ufw Enabled - Enable service ufw'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable the Automounter - Disable service autofs
    block:
    - name: Disable the Automounter - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the Automounter - Ensure autofs.service is Masked
      ansible.builtin.systemd:
        name: autofs.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("autofs.service", multiline=True)
    - name: Unit Socket Exists - autofs.socket
      ansible.builtin.command: systemctl -q list-unit-files autofs.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the Automounter - Disable Socket autofs
      ansible.builtin.systemd:
        name: autofs.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("autofs.socket", multiline=True)
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_autofs_disabled
    - special_service_block
    when: ( "autofs" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Disable the Automounter - Disable service autofs'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable the Automounter - Disable service autofs'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable Apport Service - Disable service apport
    block:
    - name: Disable Apport Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Apport Service - Ensure apport.service is Masked
      ansible.builtin.systemd:
        name: apport.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("apport.service", multiline=True)
    - name: Unit Socket Exists - apport.socket
      ansible.builtin.command: systemctl -q list-unit-files apport.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Apport Service - Disable Socket apport
      ansible.builtin.systemd:
        name: apport.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("apport.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_apport_disabled
    - special_service_block
    - unknown_severity
    when: '"apport" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable Apport Service - Disable service apport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Apport Service - Disable service apport'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable Avahi Server Software - Disable service avahi-daemon
    block:
    - name: Disable Avahi Server Software - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Avahi Server Software - Ensure avahi-daemon.service is Masked
      ansible.builtin.systemd:
        name: avahi-daemon.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("avahi-daemon.service", multiline=True)
    - name: Unit Socket Exists - avahi-daemon.socket
      ansible.builtin.command: systemctl -q list-unit-files avahi-daemon.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Avahi Server Software - Disable Socket avahi-daemon
      ansible.builtin.systemd:
        name: avahi-daemon.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("avahi-daemon.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_avahi-daemon_disabled
    - special_service_block
    when: ( "avahi-daemon" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Disable Avahi Server Software - Disable service avahi-daemon'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Avahi Server Software - Disable service avahi-daemon'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable cron Service - Enable service cron
    block:
    - name: Enable cron Service - Enable Service cron
      ansible.builtin.systemd:
        name: cron
        enabled: true
        state: started
        masked: false
      when:
      - '"cron" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_cron_enabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable cron Service - Enable service cron'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable cron Service - Enable service cron'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: The Chronyd service is enabled - Enable service chrony
    block:
    - name: The Chronyd service is enabled - Enable Service chrony
      ansible.builtin.systemd:
        name: chrony
        enabled: true
        state: started
        masked: false
      when:
      - '"chrony" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_chronyd_enabled
    - special_service_block
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''The Chronyd service is enabled - Enable service chrony'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''The Chronyd service is enabled - Enable service chrony'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable the NTP Daemon - Enable service ntp
    block:
    - name: Enable the NTP Daemon - Enable Service ntp
      ansible.builtin.systemd:
        name: ntp
        enabled: true
        state: started
        masked: false
      when:
      - '"ntp" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-8(1)(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.1
    - enable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_ntp_enabled
    - special_service_block
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"ntp" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable the NTP Daemon - Enable service ntp'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable the NTP Daemon - Enable service ntp'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable systemd_timesyncd Service - Enable service systemd-timesyncd
    block:
    - name: Enable systemd_timesyncd Service - Enable Service systemd-timesyncd
      ansible.builtin.systemd:
        name: systemd-timesyncd
        enabled: true
        state: started
        masked: false
      when:
      - '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-8(1)(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.1
    - enable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_timesyncd_enabled
    - special_service_block
    when:
    - '"linux-base" in ansible_facts.packages'
    - ( not ( "chrony" in ansible_facts.packages ) and not ( "ntp" in ansible_facts.packages ) )
  rescue:
  - name: 'StigForge: continue after error in ''Enable systemd_timesyncd Service - Enable service systemd-timesyncd'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable systemd_timesyncd Service - Enable service systemd-timesyncd'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable the CUPS Service - Disable service cups
    block:
    - name: Disable the CUPS Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the CUPS Service - Ensure cups.service is Masked
      ansible.builtin.systemd:
        name: cups.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("cups.service", multiline=True)
    - name: Unit Socket Exists - cups.socket
      ansible.builtin.command: systemctl -q list-unit-files cups.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the CUPS Service - Disable Socket cups
      ansible.builtin.systemd:
        name: cups.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("cups.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_cups_disabled
    - special_service_block
    - unknown_severity
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable the CUPS Service - Disable service cups'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable the CUPS Service - Disable service cups'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Gather the service facts
    ansible.builtin.service_facts: null
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the service facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the service facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Check If Prelinked Is Installed
    ansible.builtin.stat:
      path: /usr/sbin/prelink
      get_checksum: false
    register: prelink
    tags:
    - disable_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - package_prelink_removed
  rescue:
  - name: 'StigForge: continue after error in ''Check If Prelinked Is Installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check If Prelinked Is Installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Restore Prelinked Binaries
    ansible.builtin.command:
      cmd: prelink -ua
    when: (prelink is not skipped and prelink.stat.exists)
    tags:
    - disable_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - package_prelink_removed
  rescue:
  - name: 'StigForge: continue after error in ''Restore Prelinked Binaries'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restore Prelinked Binaries'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Build and Test AIDE Database - Ensure AIDE Is Installed
    ansible.builtin.apt:
      name: aide
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Ensure AIDE Is Installed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Ensure AIDE Is Installed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Build and Test AIDE Database - Check if DB Path in /etc/aide/aide.conf Is Already Set
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database=)(.*)$
      state: absent
    check_mode: true
    changed_when: false
    register: database_replace
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Check if DB Path in /etc/aide/aide.conf Is
      Already Set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Check if DB Path in /etc/aide/aide.conf Is Already Set'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Build and Test AIDE Database - Check if DB Out Path in /etc/aide/aide.conf Is Already Set
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database_out=)(.*)$
      state: absent
    check_mode: true
    changed_when: false
    register: database_out_replace
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Check if DB Out Path in /etc/aide/aide.conf
      Is Already Set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Check if DB Out Path in /etc/aide/aide.conf Is Already Set'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Build and Test AIDE Database - Fix DB Path in Config File if Necessary
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database)(\s*)=(\s*)(.*)$
      line: \2\3=\4file:/var/lib/aide/aide.db
      backrefs: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_replace.found > 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Fix DB Path in Config File if Necessary'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Fix DB Path in Config File if Necessary'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Build and Test AIDE Database - Fix DB Out Path in Config File if Necessary
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database_out)(\s*)=(\s*)(.*)$
      line: \2\3=\4file:/var/lib/aide/aide.db.new
      backrefs: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_out_replace.found > 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Fix DB Out Path in Config File if Necessary''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Fix DB Out Path in Config File if Necessary'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Build and Test AIDE Database - Ensure the Default DB Path is Added
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      line: database=file:/var/lib/aide/aide.db
      create: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_replace.found == 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Ensure the Default DB Path is Added'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Ensure the Default DB Path is Added'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Build and Test AIDE Database - Ensure the Default Out Path is Added
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      line: database_out=file:/var/lib/aide/aide.db.new
      create: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_out_replace.found == 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Ensure the Default Out Path is Added'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Ensure the Default Out Path is Added'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Build and Test AIDE Database - Build and Test AIDE Database
    ansible.builtin.command: /usr/sbin/aideinit -y -f
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Build and Test AIDE Database'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Build and Test AIDE Database'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set audit_tools fact
    ansible.builtin.set_fact:
      audit_tools:
      - /usr/sbin/auditctl
      - /usr/sbin/auditd
      - /usr/sbin/augenrules
      - /usr/sbin/aureport
      - /usr/sbin/ausearch
      - /usr/sbin/autrace
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set audit_tools fact'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set audit_tools fact'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure existing AIDE configuration for audit tools are correct
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^{{ item }}\s
      line: '{{ item }} p+i+n+u+g+s+b+acl+xattrs+sha512'
      create: true
    with_items: '{{ audit_tools }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"aide" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure existing AIDE configuration for audit tools are correct'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure existing AIDE configuration for audit tools are correct'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Configure AIDE to properly protect audit tools
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      line: '{{ item }} p+i+n+u+g+s+b+acl+xattrs+sha512'
      create: true
    with_items: '{{ audit_tools }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"aide" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure AIDE to properly protect audit tools'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure AIDE to properly protect audit tools'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Configure Periodic Execution of AIDE
    ansible.builtin.cron:
      name: run AIDE check
      minute: 5
      hour: 4
      weekday: 0
      user: root
      job: /usr/bin/aide --check
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure Periodic Execution of AIDE'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Periodic Execution of AIDE'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure use_pty is enabled in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      regexp: ^[\s]*Defaults.*\buse_pty\b.*$
      line: Defaults use_pty
      validate: /usr/sbin/visudo -cf %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_add_use_pty
  rescue:
  - name: 'StigForge: continue after error in ''Ensure use_pty is enabled in /etc/sudoers'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure use_pty is enabled in /etc/sudoers'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure logfile is enabled with the appropriate value in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      regexp: ^[\s]*Defaults\s(.*)\blogfile=[-]?.+\b(.*)$
      line: Defaults \1logfile={{ var_sudo_logfile }}\2
      validate: /usr/sbin/visudo -cf %s
      backrefs: true
    register: edit_sudoers_logfile_option
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_custom_logfile
  rescue:
  - name: 'StigForge: continue after error in ''Ensure logfile is enabled with the appropriate value in /etc/sudoers'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure logfile is enabled with the appropriate value in /etc/sudoers'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable logfile option with appropriate value in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      line: Defaults logfile={{ var_sudo_logfile }}
      validate: /usr/sbin/visudo -cf %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    - edit_sudoers_logfile_option is defined and not edit_sudoers_logfile_option.changed
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_custom_logfile
  rescue:
  - name: 'StigForge: continue after error in ''Enable logfile option with appropriate value in /etc/sudoers'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable logfile option with appropriate value in /etc/sudoers'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/sudoers.d/ files
    ansible.builtin.find:
      paths:
      - /etc/sudoers.d/
    register: sudoers
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-11
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_remove_no_authenticate
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/sudoers.d/ files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/sudoers.d/ files'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Remove lines containing !authenticate from sudoers files
    ansible.builtin.replace:
      regexp: (^(?!#).*[\s]+\!authenticate.*$)
      replace: '# \g<1>'
      path: '{{ item.path }}'
      validate: /usr/sbin/visudo -cf %s
    with_items:
    - path: /etc/sudoers
    - '{{ sudoers.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-11
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_remove_no_authenticate
  rescue:
  - name: 'StigForge: continue after error in ''Remove lines containing !authenticate from sudoers files'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove lines containing !authenticate from sudoers files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Modify the System Login Banner for Remote Connections - ensure correct banner
    ansible.builtin.copy:
      dest: /etc/issue.net
      content: '{{ remote_login_banner_text | regex_replace("^\^(.*)\$$", "\1") | regex_replace("^\((.*\.)\|.*\)$", "\1")
        | regex_replace("\[\\s\\n\]\+"," ") | regex_replace("\(\?:\[\\n\]\+\|\(\?:\\\\n\)\+\)", "\n") | regex_replace("\\",
        "") | wordwrap() }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255020
    - banner_etc_issue_net
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Modify the System Login Banner for Remote Connections - ensure correct banner''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Modify the System Login Banner for Remote Connections - ensure correct banner'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Set the file_groupowner_etc_issue_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_issue_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_issue_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_issue_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      follow: false
      group: '{{ file_groupowner_etc_issue_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_etc_issue_net_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_issue_net_newgroup: '0'
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_issue_net_newgroup variable if represented by
      gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_issue_net_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      follow: false
      group: '{{ file_groupowner_etc_issue_net_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_etc_motd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_motd_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_motd_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_motd_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      follow: false
      group: '{{ file_groupowner_etc_motd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_etc_issue_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_issue_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_issue_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_issue_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      follow: false
      owner: '{{ file_owner_etc_issue_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/issue'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_etc_issue_net_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_issue_net_newown: '0'
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_issue_net_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_issue_net_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      follow: false
      owner: '{{ file_owner_etc_issue_net_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_etc_motd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_motd_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_motd_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_motd_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      follow: false
      owner: '{{ file_owner_etc_motd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/motd'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_permissions_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_permissions_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/motd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Limit Password Reuse - Check if the required PAM module option is present in /etc/pam.d/common-password
    ansible.builtin.lineinfile:
      path: /etc/pam.d/common-password
      regexp: ^\s*password\s+\[success=[A-Za-z0-9].*\]\s+pam_unix.so\s*.*\sremember\b
      state: absent
    check_mode: true
    changed_when: false
    register: result_pam_module_remember_option_present
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_unix_remember
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse - Check if the required PAM module option is present
      in /etc/pam.d/common-password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse - Check if the required PAM module option is present in /etc/pam.d/common-password''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Limit Password Reuse - Ensure the "remember" PAM option for "pam_unix.so" is included in /etc/pam.d/common-password
    ansible.builtin.lineinfile:
      path: /etc/pam.d/common-password
      backrefs: true
      regexp: ^(\s*password\s+\[success=[A-Za-z0-9].*\]\s+pam_unix.so.*)
      line: \1 remember={{ var_password_pam_unix_remember }}
      state: present
    register: result_pam_remember_add
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - result_pam_module_remember_option_present.found == 0
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_unix_remember
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse - Ensure the "remember" PAM option for "pam_unix.so"
      is included in /etc/pam.d/common-password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse - Ensure the "remember" PAM option for "pam_unix.so" is included in /etc/pam.d/common-password''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Limit Password Reuse - Ensure the required value for "remember" PAM option from "pam_unix.so" in /etc/pam.d/common-password
    ansible.builtin.lineinfile:
      path: /etc/pam.d/common-password
      backrefs: true
      regexp: ^(\s*password\s+\[success=[A-Za-z0-9].*\]\s+pam_unix.so\s+.*)(remember)=[0-9a-zA-Z]+\s*(.*)
      line: \1\2={{ var_password_pam_unix_remember }} \3
    register: result_pam_remember_edit
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - result_pam_module_remember_option_present.found > 0
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_unix_remember
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse - Ensure the required value for "remember" PAM option
      from "pam_unix.so" in /etc/pam.d/common-password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse - Ensure the required value for "remember" PAM option from "pam_unix.so" in
        /etc/pam.d/common-password'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611020
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_dcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Check
      if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Check if system relies on
        pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Remediation where pam-auth-update tool is
      present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611020
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_dcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Remediation
      where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure PAM variable dcredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*dcredit
      line: dcredit = {{ var_password_pam_dcredit }}
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611020
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_dcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure
      PAM variable dcredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure PAM variable dcredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611015
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_lcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters -
      Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Check if system relies
        on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611015
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_lcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters -
      Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Ensure PAM variable lcredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*lcredit
      line: lcredit = {{ var_password_pam_lcredit }}
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611015
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_lcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters -
      Ensure PAM variable lcredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Ensure PAM variable lcredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_minclass
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Categories -
      Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Categories - Check if system relies
        on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_minclass
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Categories -
      Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Categories - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure PAM variable minclass is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*minclass
      line: minclass = {{ var_password_pam_minclass }}
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_minclass
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Categories -
      Ensure PAM variable minclass is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure PAM variable minclass
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Length - Check if system relies on pam-auth-update tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_minlen
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Length - Check if system
      relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Length - Check if system relies on pam-auth-update
        tool'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Length - Remediation where pam-auth-update tool is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Length - Ensure pam-auth-update profile changes are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_minlen
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Length - Remediation where
      pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Length - Remediation where pam-auth-update tool
        is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable minlen is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*minlen
      line: minlen = {{ var_password_pam_minlen }}
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_minlen
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable
      minlen is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable minlen is set accordingly''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_ocredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Check
      if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Check if system relies on
        pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_ocredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Remediation
      where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure PAM variable ocredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*ocredit
      line: ocredit = {{ var_password_pam_ocredit }}
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_ocredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure
      PAM variable ocredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure PAM variable ocredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - accounts_password_pam_ucredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters -
      Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Check if system relies
        on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - accounts_password_pam_ucredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters -
      Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Ensure PAM variable ucredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*ucredit
      line: ucredit = {{ var_password_pam_ucredit }}
    when: '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - accounts_password_pam_ucredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters -
      Ensure PAM variable ucredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Ensure PAM variable ucredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure interactive local users are the owners of their respective initialization files
    ansible.builtin.lineinfile:
      dest: /etc/group
      backrefs: true
      regexp: (^shadow:[^:]*:[^:]*:)([^:]+$)
      line: \1
    tags:
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - ensure_shadow_group_empty
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the owners of their respective initialization
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the owners of their respective initialization files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Collect users with not correct minimum time period between password changes
    ansible.builtin.command: 'awk -F'':'' ''(/^[^:]+:[^!*]/ && ($4 < {{ var_accounts_minimum_age_login_defs }} || $4 == ""))
      {print $1}'' /etc/shadow

      '
    register: user_names
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - accounts_password_set_min_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with not correct minimum time period between password changes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with not correct minimum time period between password changes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Change the minimum time period between password changes
    ansible.builtin.command: 'chage -m {{ var_accounts_minimum_age_login_defs }} {{ item }}

      '
    with_items: '{{ user_names.stdout_lines }}'
    when: user_names.stdout_lines | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - accounts_password_set_min_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Change the minimum time period between password changes'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Change the minimum time period between password changes'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Collect users with no password
    ansible.builtin.command: 'awk -F: ''!$2 {print $1}'' /etc/shadow

      '
    register: users_nopasswd
    changed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611065
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Lock users with no password
    ansible.builtin.command: 'passwd -l {{ item }}

      '
    with_items: '{{ users_nopasswd.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - users_nopasswd is not skipped and users_nopasswd.stdout_lines | length > 0
    tags:
    - DISA-STIG-UBTU-22-611065
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Get all /etc/passwd file entries
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all /etc/passwd file entries'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all /etc/passwd file entries'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Lock the password of the user accounts other than root with uid 0
    ansible.builtin.command: passwd -l {{ item.key }}
    loop: '{{ getent_passwd | dict2items | rejectattr(''key'', ''search'', ''root'') | list }}'
    when: item.value.1  == '0'
    tags:
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock the password of the user accounts other than root with uid 0'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock the password of the user accounts other than root with uid 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
      }} Group Exists
    ansible.builtin.group:
      name: '{{ var_pam_wheel_group_for_su }}'
      state: present
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - ensure_pam_wheel_group_empty
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty
      - Ensure {{ var_pam_wheel_group_for_su }} Group Exists'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
        }} Group Exists'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
      }} Group is Empty
    ansible.builtin.lineinfile:
      path: /etc/group
      regexp: ^({{ var_pam_wheel_group_for_su }}:[^:]+:[0-9]+:).*$
      line: \1
      backrefs: true
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - ensure_pam_wheel_group_empty
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty
      - Ensure {{ var_pam_wheel_group_for_su }} Group is Empty'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
        }} Group is Empty'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local Users From /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local
      Users From /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local Users From /etc/passwd''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users Variable From getent_passwd Facts
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd | dict2items }}'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users
      Variable From getent_passwd Facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users Variable From getent_passwd
        Facts'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login Shell for System Accounts
    ansible.builtin.user:
      name: '{{ item.key }}'
      shell: /sbin/nologin
    loop: '{{ local_users }}'
    when:
    - item.key not in ['root']
    - item.value[1]|int < 1000
    - item.value[5] not in ['/sbin/shutdown', '/sbin/halt', '/bin/sync']
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login
      Shell for System Accounts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login Shell for System Accounts''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Enforce Usage of pam_wheel with Group Parameter for su Authentication - Add the group to the /etc/pam.d/su file
    ansible.builtin.lineinfile:
      path: /etc/pam.d/su
      state: present
      regexp: ^[\s]*#[\s]*auth[\s]+required[\s]+pam_wheel\.so[\s]+use_uid group=$
      line: auth             required        pam_wheel.so use_uid group={{ var_pam_wheel_group_for_su }}
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - use_pam_wheel_group_for_su
  rescue:
  - name: 'StigForge: continue after error in ''Enforce Usage of pam_wheel with Group Parameter for su Authentication - Add
      the group to the /etc/pam.d/su file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enforce Usage of pam_wheel with Group Parameter for su Authentication - Add the group to the /etc/pam.d/su
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
- block:
  - name: Correct any occurrence of TMOUT in /etc/profile
    ansible.builtin.replace:
      path: /etc/profile
      regexp: ^[^#].*TMOUT=.*
      replace: typeset -xr TMOUT={{ var_accounts_tmout }}
    register: profile_replaced
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-412030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSSv4-8.6
    - PCI-DSSv4-8.6.1
    - accounts_tmout
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Correct any occurrence of TMOUT in /etc/profile'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Correct any occurrence of TMOUT in /etc/profile'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set Interactive Session Timeout
    ansible.builtin.lineinfile:
      path: /etc/profile.d/tmout.sh
      create: true
      regexp: TMOUT=
      line: typeset -xr TMOUT={{ var_accounts_tmout }}
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-412030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSSv4-8.6
    - PCI-DSSv4-8.6.1
    - accounts_tmout
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Interactive Session Timeout'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Interactive Session Timeout'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure interactive local users are the group-owners of their respective initialization files
    ansible.builtin.shell:
      cmd: '

        awk -F: ''{if ($4 >= 1000 && $4 != 65534) print $4":"$6}'' /etc/passwd | while IFS=: read -r gid home; do find -P
        "$home" -maxdepth 1 -type f -name "\.[^.]*" -exec chgrp -f --no-dereference -- $gid "{}" \;; done'
    tags:
    - accounts_user_dot_group_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the group-owners of their respective initialization
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the group-owners of their respective initialization files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: User Initialization Files Must Not Run World-Writable Programs - Initialize variables
    ansible.builtin.set_fact:
      home_user_dirs: []
      world_writable_files: []
    tags:
    - accounts_user_dot_no_world_writable_programs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Not Run World-Writable Programs - Initialize
      variables'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Not Run World-Writable Programs - Initialize variables'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: User Initialization Files Must Not Run World-Writable Programs - Get user's home dir list
    ansible.builtin.getent:
      database: passwd
    register: passwd_database
    tags:
    - accounts_user_dot_no_world_writable_programs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "User Initialization Files Must Not Run World-Writable Programs - Get user''s
      home dir list" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "User Initialization Files Must Not Run World-Writable Programs - Get user''s home dir list" raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: User Initialization Files Must Not Run World-Writable Programs - Fill home_user_dirs
    ansible.builtin.set_fact:
      home_user_dirs: '{{ home_user_dirs + [item.data[4]] }}'
    when: item.data[4] is defined and item.data[2]|int >= 1000 and item.data[2]|int != 65534
    with_items: '{{ passwd_database.ansible_facts.getent_passwd | dict2items(key_name=''user'', value_name=''data'')}}'
    tags:
    - accounts_user_dot_no_world_writable_programs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Not Run World-Writable Programs - Fill home_user_dirs''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Not Run World-Writable Programs - Fill home_user_dirs'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: User Initialization Files Must Not Run World-Writable Programs - Get world writable files
    ansible.builtin.shell: 'find / -xdev -type f -perm -0002 2> /dev/null

      '
    register: world_writable_files
    tags:
    - accounts_user_dot_no_world_writable_programs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Not Run World-Writable Programs - Get world
      writable files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Not Run World-Writable Programs - Get world writable files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: User Initialization Files Must Not Run World-Writable Programs - Find referenced_files in init files
    ansible.builtin.find:
      paths: '{{ home_user_dirs }}'
      contains: '{{ item }}'
      hidden: true
      read_whole_file: true
      recurse: true
    with_items: '{{ world_writable_files.stdout_lines }}'
    register: referenced_files
    tags:
    - accounts_user_dot_no_world_writable_programs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Not Run World-Writable Programs - Find referenced_files
      in init files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Not Run World-Writable Programs - Find referenced_files in init files''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: User Initialization Files Must Not Run World-Writable Programs - Remove world writable permissions
    ansible.builtin.file:
      path: '{{ item.item }}'
      mode: o-w
    when: item.matched > 0
    with_items: '{{ referenced_files.results }}'
    tags:
    - accounts_user_dot_no_world_writable_programs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Not Run World-Writable Programs - Remove world
      writable permissions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Not Run World-Writable Programs - Remove world writable permissions''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure interactive local users are the owners of their respective initialization files
    ansible.builtin.shell:
      cmd: 'awk -F: ''{if ($3 >= 1000 && $3 != 65534) print $3":"$6}'' /etc/passwd | while IFS=: read -r uid home; do find
        -P "$home" -maxdepth 1 -type f -name "\.[^.]*" -exec chown -f --no-dereference -- $uid "{}" \;; done'
    tags:
    - accounts_user_dot_user_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the owners of their respective initialization
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the owners of their respective initialization files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure interactive users have a home directory exists
    ansible.builtin.user:
      name: '{{ item.key }}'
      create_home: true
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive users have a home directory exists'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive users have a home directory exists'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence of home directories to avoid creating them, but only fixing group ownership
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence of home directories to avoid creating them, but only fixing
      group ownership'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence of home directories to avoid creating them, but only fixing group ownership''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure interactive local users are the group-owners of their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      group: '{{ item.0.value[2] }}'
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the group-owners of their respective home
      directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the group-owners of their respective home directories'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence of home directories to avoid creating them, but only fixing ownership
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence of home directories to avoid creating them, but only fixing
      ownership'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence of home directories to avoid creating them, but only fixing ownership'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure interactive local users are the owners of their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      owner: '{{ item.0.value[1] }}'
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the owners of their respective home directories''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the owners of their respective home directories'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence home directories to avoid creating them.
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    - item.value[4] != "/"
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence home directories to avoid creating them.'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence home directories to avoid creating them.'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure interactive local users have proper permissions on their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      mode: u-s,g-w-s,o=-
      follow: false
      recurse: false
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users have proper permissions on their respective
      home directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users have proper permissions on their respective home directories'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Check if umask in /etc/bash.bashrc is already set
    ansible.builtin.lineinfile:
      path: /etc/bash.bashrc
      regexp: ^[^#]*\bumask\s+\d+$
      state: absent
    check_mode: true
    changed_when: false
    register: umask_replace
    when: '"bash" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if umask in /etc/bash.bashrc is already set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if umask in /etc/bash.bashrc is already set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Replace user umask in /etc/bash.bashrc
    ansible.builtin.replace:
      path: /etc/bash.bashrc
      regexp: ^([^#]*\b)umask\s+\d+$
      replace: \g<1>umask {{ var_accounts_user_umask }}
    when:
    - '"bash" in ansible_facts.packages'
    - umask_replace.found > 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Replace user umask in /etc/bash.bashrc'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Replace user umask in /etc/bash.bashrc'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure the Default umask is Appended Correctly
    ansible.builtin.lineinfile:
      create: true
      path: /etc/bash.bashrc
      line: umask {{ var_accounts_user_umask }}
    when:
    - '"bash" in ansible_facts.packages'
    - umask_replace.found == 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default umask is Appended Correctly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default umask is Appended Correctly'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Check if UMASK is already set
    ansible.builtin.lineinfile:
      path: /etc/login.defs
      regexp: ^(\s*)UMASK\s+.*
      state: absent
    check_mode: true
    changed_when: false
    register: result_umask_is_set
    when: '"login" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-412035
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if UMASK is already set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if UMASK is already set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Replace user UMASK in /etc/login.defs
    ansible.builtin.replace:
      path: /etc/login.defs
      regexp: ^(\s*)UMASK(\s+).*
      replace: \g<1>UMASK\g<2>{{ var_accounts_user_umask }}
    when:
    - '"login" in ansible_facts.packages'
    - result_umask_is_set.found > 0
    tags:
    - DISA-STIG-UBTU-22-412035
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Replace user UMASK in /etc/login.defs'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Replace user UMASK in /etc/login.defs'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure the Default UMASK is Appended Correctly
    ansible.builtin.lineinfile:
      create: true
      path: /etc/login.defs
      line: UMASK {{ var_accounts_user_umask }}
    when:
    - '"login" in ansible_facts.packages'
    - result_umask_is_set.found == 0
    tags:
    - DISA-STIG-UBTU-22-412035
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default UMASK is Appended Correctly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default UMASK is Appended Correctly'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile Configuration Files Where umask Is Defined
    ansible.builtin.find:
      paths:
      - /etc/profile.d
      patterns:
      - sh.local
      - '*.sh'
      contains: ^[\s]*umask\s+\d+
    register: result_profile_d_files
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile
      Configuration Files Where umask Is Defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile Configuration Files Where
        umask Is Defined'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing umask Value in Files From /etc/profile.d
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^(\s*)umask\s+\d+
      replace: \1umask {{ var_accounts_user_umask }}
    loop: '{{ result_profile_d_files.files }}'
    register: result_umask_replaced_profile_d
    when: result_profile_d_files.matched
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing
      umask Value in Files From /etc/profile.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing umask Value in Files
        From /etc/profile.d'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is Set in /etc/profile if Not Already Set
      Elsewhere
    ansible.builtin.lineinfile:
      create: true
      mode: 420
      path: /etc/profile
      line: umask {{ var_accounts_user_umask }}
    when: not result_profile_d_files.matched
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is
      Set in /etc/profile if Not Already Set Elsewhere'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is Set in /etc/profile if
        Not Already Set Elsewhere'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value For All Existing umask Definition
      in /etc/profile
    ansible.builtin.replace:
      path: /etc/profile
      regexp: ^(\s*)umask\s+\d+
      replace: \1umask {{ var_accounts_user_umask }}
    register: result_umask_replaced_profile
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value
      For All Existing umask Definition in /etc/profile'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value For All Existing umask
        Definition in /etc/profile'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_grub2_cfg_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_grub2_cfg_newown: '0'
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_grub2_cfg_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_grub2_cfg_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /boot/grub/grub.cfg
    ansible.builtin.stat:
      path: /boot/grub/grub.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /boot/grub/grub.cfg
    ansible.builtin.file:
      path: /boot/grub/grub.cfg
      follow: false
      owner: '{{ file_owner_grub2_cfg_newown }}'
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /boot/grub/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /boot/grub/grub.cfg'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /boot/grub/grub.cfg
    ansible.builtin.stat:
      path: /boot/grub/grub.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub/grub.cfg
    ansible.builtin.file:
      path: /boot/grub/grub.cfg
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub/grub.cfg'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub/grub.cfg'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog Main Config
      File
    ansible.builtin.find:
      paths: /etc
      pattern: rsyslog.conf
      contains: ^\s*\$FileCreateMode\s*\d+
    register: rsyslog_main_file_with_filecreatemode
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode
      Parameter in rsyslog Main Config File'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog
        Main Config File'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog Include Files
    ansible.builtin.find:
      paths: /etc/rsyslog.d/
      pattern: '*.conf'
      contains: ^\s*\$FileCreateMode\s*\d+
    register: rsyslog_includes_with_filecreatemode
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode
      Parameter in rsyslog Include Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog
        Include Files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Assemble List of rsyslog Configuration Files with $FileCreateMode
      Parameter
    ansible.builtin.set_fact:
      rsyslog_filecreatemode_files: '{{ rsyslog_main_file_with_filecreatemode.files | map(attribute=''path'') | list + rsyslog_includes_with_filecreatemode.files
        | map(attribute=''path'') | list }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Assemble List of rsyslog
      Configuration Files with $FileCreateMode Parameter'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Assemble List of rsyslog Configuration Files
        with $FileCreateMode Parameter'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Remove $FileCreateMode Parameter from Multiple Files to Avoid
      Conflicts
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: \$FileCreateMode.*
      state: absent
    register: result_rsyslog_filecreatemode_removed
    loop: '{{ rsyslog_filecreatemode_files }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - rsyslog_filecreatemode_files | length > 1
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Remove $FileCreateMode
      Parameter from Multiple Files to Avoid Conflicts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Remove $FileCreateMode Parameter from Multiple
        Files to Avoid Conflicts'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Add $FileCreateMode Parameter and Expected Value
    ansible.builtin.lineinfile:
      path: /etc/rsyslog.d/00-rsyslog_filecreatemode.conf
      line: $FileCreateMode 0640
      mode: 416
      create: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - rsyslog_filecreatemode_files | length == 0 or result_rsyslog_filecreatemode_removed is not skipped
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Add $FileCreateMode Parameter
      and Expected Value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Add $FileCreateMode Parameter and Expected Value''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Ensure Correct Value of Existing $FileCreateMode Parameter
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: ^\$FileCreateMode
      line: $FileCreateMode 0640
    loop: '{{ rsyslog_filecreatemode_files }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - rsyslog_filecreatemode_files | length == 1
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Ensure Correct Value
      of Existing $FileCreateMode Parameter'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Ensure Correct Value of Existing $FileCreateMode
        Parameter'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
- block:
  - name: Setting unquoted shell-style assignment of 'Compress' to 'yes' in '/etc/systemd/journald.conf'
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Compress=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Compress=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Compress=
        line: Compress=yes
        state: present
        insertbefore: ^# Compress
        validate: /usr/bin/bash -n %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - journald_compress
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Setting unquoted shell-style assignment of ''Compress'' to ''yes'' in ''/etc/systemd/journald.conf''"
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Setting unquoted shell-style assignment of ''Compress'' to ''yes'' in ''/etc/systemd/journald.conf''"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Setting unquoted shell-style assignment of 'Storage' to 'persistent' in '/etc/systemd/journald.conf'
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Storage=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Storage=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Storage=
        line: Storage=persistent
        state: present
        insertbefore: ^# Storage
        validate: /usr/bin/bash -n %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - journald_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Setting unquoted shell-style assignment of ''Storage'' to ''persistent'' in
      ''/etc/systemd/journald.conf''" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Setting unquoted shell-style assignment of ''Storage'' to ''persistent'' in ''/etc/systemd/journald.conf''"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Disable systemd-journal-remote Socket - Collect systemd Socket Units Present in the System
    ansible.builtin.command:
      cmd: systemctl -q list-unit-files --type socket
    register: result_systemd_unit_files
    changed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - socket_systemd-journal-remote_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd-journal-remote Socket - Collect systemd Socket Units Present
      in the System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd-journal-remote Socket - Collect systemd Socket Units Present in the System'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket is Masked
    ansible.builtin.systemd:
      name: systemd-journal-remote.socket
      state: stopped
      enabled: false
      masked: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - result_systemd_unit_files.stdout_lines is search("systemd-journal-remote.socket")
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - socket_systemd-journal-remote_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket
      is Masked'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket is Masked'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config Lines Regex in
      Legacy Syntax
    ansible.builtin.set_fact:
      rsyslog_listen_legacy_regex: ^\s*\$(((Input(TCP|RELP)|UDP)ServerRun)|ModLoad\s+(imtcp|imudp|imrelp))
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Define Rsyslog Config Lines Regex in Legacy Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config
        Lines Regex in Legacy Syntax'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config Lines in Rsyslog
      Main Config File
    ansible.builtin.find:
      paths: /etc
      pattern: rsyslog.conf
      contains: '{{ rsyslog_listen_legacy_regex }}'
    register: rsyslog_listen_legacy_main_file
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for Legacy Config Lines in Rsyslog Main Config File'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config
        Lines in Rsyslog Main Config File'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config Lines in Rsyslog
      Include Files
    ansible.builtin.find:
      paths: /etc/rsyslog.d/
      pattern: '*.conf'
      contains: '{{ rsyslog_listen_legacy_regex }}'
    register: rsyslog_listen_legacy_include_files
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for Legacy Config Lines in Rsyslog Include Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config
        Lines in Rsyslog Include Files'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config Files With
      Listen Lines in Legacy Syntax
    ansible.builtin.set_fact:
      rsyslog_legacy_remote_listen_files: '{{ rsyslog_listen_legacy_main_file.files | map(attribute=''path'') | list + rsyslog_listen_legacy_include_files.files
        | map(attribute=''path'') | list }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Assemble List of Config Files With Listen Lines in Legacy Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config
        Files With Listen Lines in Legacy Syntax'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config Lines Wherever
      Defined Using Legacy Syntax
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: '{{ rsyslog_listen_legacy_regex }}'
      replace: '# \1'
    loop: '{{ rsyslog_legacy_remote_listen_files }}'
    register: rsyslog_listen_legacy_comment
    when:
    - '"linux-base" in ansible_facts.packages'
    - rsyslog_legacy_remote_listen_files | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Comment Listen Config Lines Wherever Defined Using Legacy Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config
        Lines Wherever Defined Using Legacy Syntax'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config Lines Regex in
      RainerScript Syntax
    ansible.builtin.set_fact:
      rsyslog_listen_rainer_regex: ^\s*(module|input)\((load|type)="(imtcp|imudp)".*$
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Define Rsyslog Config Lines Regex in RainerScript Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config
        Lines Regex in RainerScript Syntax'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript Config Lines
      in Rsyslog Main Config File
    ansible.builtin.find:
      paths: /etc
      pattern: rsyslog.conf
      contains: '{{ rsyslog_listen_rainer_regex }}'
    register: rsyslog_rainer_remote_main_file
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for RainerScript Config Lines in Rsyslog Main Config File'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript
        Config Lines in Rsyslog Main Config File'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript Config Lines
      in Rsyslog Include Files
    ansible.builtin.find:
      paths: /etc/rsyslog.d/
      pattern: '*.conf'
      contains: '{{ rsyslog_listen_rainer_regex }}'
    register: rsyslog_rainer_remote_include_files
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for RainerScript Config Lines in Rsyslog Include Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript
        Config Lines in Rsyslog Include Files'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config Files With
      Listen Lines in RainerScript
    ansible.builtin.set_fact:
      rsyslog_rainer_remote_listen_files: '{{ rsyslog_rainer_remote_main_file.files | map(attribute=''path'') | list + rsyslog_rainer_remote_include_files.files
        | map(attribute=''path'') | list }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Assemble List of Config Files With Listen Lines in RainerScript'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config
        Files With Listen Lines in RainerScript'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config Lines Wherever
      Defined Using RainerScript
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: '{{ rsyslog_listen_rainer_regex }}'
      replace: '# \1'
    loop: '{{ rsyslog_rainer_remote_listen_files }}'
    register: rsyslog_listen_rainer_comment
    when:
    - '"linux-base" in ansible_facts.packages'
    - rsyslog_rainer_remote_listen_files | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Comment Listen Config Lines Wherever Defined Using RainerScript'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config
        Lines Wherever Defined Using RainerScript'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Restart Rsyslog if Any Line Were Commented
      Out
    ansible.builtin.service:
      name: rsyslog
      state: restarted
    when:
    - '"linux-base" in ansible_facts.packages'
    - rsyslog_listen_legacy_comment is changed or rsyslog_listen_rainer_comment is changed
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Restart Rsyslog if Any Line Were Commented Out'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Restart Rsyslog if Any
        Line Were Commented Out'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Check if IPv6 is enabled
    ansible.builtin.command: sysctl -n net.ipv6.conf.all.disable_ipv6
    register: ipv6_status
    failed_when: ipv6_status.stdout != "0"
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check if IPv6 is enabled'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if IPv6 is enabled'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Allow incoming traffic on the loopback interface
    ansible.builtin.iptables:
      ipv6: true
      chain: INPUT
      in_interface: lo
      jump: ACCEPT
    when:
    - ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    - ipv6_status.stdout == '0'
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow incoming traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow incoming traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Allow outgoing traffic on the loopback interface
    ansible.builtin.iptables:
      ipv6: true
      chain: OUTPUT
      out_interface: lo
      jump: ACCEPT
    when:
    - ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    - ipv6_status.stdout == '0'
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow outgoing traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow outgoing traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Drop incoming traffic from the localhost
    ansible.builtin.iptables:
      ipv6: true
      chain: INPUT
      source: ::1
      jump: DROP
    when:
    - ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    - ipv6_status.stdout == '0'
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Drop incoming traffic from the localhost'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Drop incoming traffic from the localhost'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Allow incoming traffic on the loopback interface
    ansible.builtin.iptables:
      chain: INPUT
      in_interface: lo
      jump: ACCEPT
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow incoming traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow incoming traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Allow outgoing traffic on the loopback interface
    ansible.builtin.iptables:
      chain: OUTPUT
      out_interface: lo
      jump: ACCEPT
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow outgoing traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow outgoing traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Drop incoming traffic from the localhost
    ansible.builtin.iptables:
      chain: INPUT
      source: 127.0.0.0/8
      jump: DROP
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Drop incoming traffic from the localhost'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Drop incoming traffic from the localhost'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.accept_ra.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_ra from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_ra
      replace: '#net.ipv6.conf.all.accept_ra'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_ra from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_ra from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_ra from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.accept_ra.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_ra from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_ra from /etc/ufw/sysctl.conf'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv6.conf.all.accept_ra is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_ra
      value: '{{ sysctl_net_ipv6_conf_all_accept_ra_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.accept_ra is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.accept_ra is set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_redirects
      replace: '#net.ipv6.conf.all.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_redirects from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_redirects from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv6.conf.all.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_redirects
      value: '{{ sysctl_net_ipv6_conf_all_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.accept_redirects is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_source_route
      replace: '#net.ipv6.conf.all.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_source_route from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_source_route from
      /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_source_route from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv6.conf.all.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_source_route
      value: '{{ sysctl_net_ipv6_conf_all_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.accept_source_route is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.forwarding.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.forwarding from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.forwarding
      replace: '#net.ipv6.conf.all.forwarding'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.forwarding from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.forwarding from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.forwarding from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.forwarding.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.forwarding from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.forwarding from /etc/ufw/sysctl.conf'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv6.conf.all.forwarding is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.forwarding
      value: '{{ sysctl_net_ipv6_conf_all_forwarding_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.forwarding is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.forwarding is set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.default.accept_ra.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_ra from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_ra
      replace: '#net.ipv6.conf.default.accept_ra'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_ra from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_ra from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_ra from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.default.accept_ra.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_ra from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_ra from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv6.conf.default.accept_ra is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_ra
      value: '{{ sysctl_net_ipv6_conf_default_accept_ra_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.default.accept_ra is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.default.accept_ra is set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.default.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_redirects
      replace: '#net.ipv6.conf.default.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_redirects from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_redirects from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.default.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_redirects from
      /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_redirects from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv6.conf.default.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_redirects
      value: '{{ sysctl_net_ipv6_conf_default_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.default.accept_redirects is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.default.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.default.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_source_route
      replace: '#net.ipv6.conf.default.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_source_route from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.default.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_source_route from
      /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_source_route from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv6.conf.default.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_source_route
      value: '{{ sysctl_net_ipv6_conf_default_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.default.accept_source_route is set'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.default.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.accept_redirects
      replace: '#net.ipv4.conf.all.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.accept_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.accept_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.accept_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.accept_redirects from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.accept_redirects from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.all.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.accept_redirects
      value: '{{ sysctl_net_ipv4_conf_all_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.accept_redirects is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.accept_source_route
      replace: '#net.ipv4.conf.all.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.accept_source_route from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.accept_source_route from
      /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.accept_source_route from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.all.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.accept_source_route
      value: '{{ sysctl_net_ipv4_conf_all_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.accept_source_route is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.log_martians.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.log_martians from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.log_martians
      replace: '#net.ipv4.conf.all.log_martians'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.log_martians from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.log_martians from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.log_martians from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.log_martians.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.log_martians from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.log_martians from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.all.log_martians is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.log_martians
      value: '{{ sysctl_net_ipv4_conf_all_log_martians_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.log_martians is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.log_martians is set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.rp_filter.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.rp_filter from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.rp_filter
      replace: '#net.ipv4.conf.all.rp_filter'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.rp_filter from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.rp_filter from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.rp_filter from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.rp_filter.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.rp_filter from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.rp_filter from /etc/ufw/sysctl.conf'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.all.rp_filter is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.rp_filter
      value: '{{ sysctl_net_ipv4_conf_all_rp_filter_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.rp_filter is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.rp_filter is set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.secure_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.secure_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.secure_redirects
      replace: '#net.ipv4.conf.all.secure_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.secure_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.secure_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.secure_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.secure_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.secure_redirects from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.secure_redirects from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.all.secure_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.secure_redirects
      value: '{{ sysctl_net_ipv4_conf_all_secure_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.secure_redirects is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.secure_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.accept_redirects
      replace: '#net.ipv4.conf.default.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.accept_redirects from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.accept_redirects from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.accept_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.accept_redirects from
      /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.accept_redirects from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.default.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.accept_redirects
      value: '{{ sysctl_net_ipv4_conf_default_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.accept_redirects is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.accept_source_route
      replace: '#net.ipv4.conf.default.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.accept_source_route from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.accept_source_route from
      /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.accept_source_route from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.default.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.accept_source_route
      value: '{{ sysctl_net_ipv4_conf_default_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.accept_source_route is set'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.log_martians.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.log_martians from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.log_martians
      replace: '#net.ipv4.conf.default.log_martians'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.log_martians from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.log_martians from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.log_martians from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.log_martians.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.log_martians from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.log_martians from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.default.log_martians is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.log_martians
      value: '{{ sysctl_net_ipv4_conf_default_log_martians_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.log_martians is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.log_martians is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.rp_filter.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.rp_filter from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.rp_filter
      replace: '#net.ipv4.conf.default.rp_filter'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.rp_filter from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.rp_filter from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.rp_filter from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.rp_filter.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.rp_filter from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.rp_filter from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.default.rp_filter is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.rp_filter
      value: '{{ sysctl_net_ipv4_conf_default_rp_filter_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.rp_filter is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.rp_filter is set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.secure_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.secure_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.secure_redirects
      replace: '#net.ipv4.conf.default.secure_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.secure_redirects from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.secure_redirects from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.secure_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.secure_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.secure_redirects from
      /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.secure_redirects from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.default.secure_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.secure_redirects
      value: '{{ sysctl_net_ipv4_conf_default_secure_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.secure_redirects is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.secure_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.icmp_echo_ignore_broadcasts.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.icmp_echo_ignore_broadcasts
      replace: '#net.ipv4.icmp_echo_ignore_broadcasts'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.icmp_echo_ignore_broadcasts.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts is set
    ansible.posix.sysctl:
      name: net.ipv4.icmp_echo_ignore_broadcasts
      value: '{{ sysctl_net_ipv4_icmp_echo_ignore_broadcasts_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.icmp_ignore_bogus_error_responses.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.icmp_ignore_bogus_error_responses
      replace: '#net.ipv4.icmp_ignore_bogus_error_responses'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses
      from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.icmp_ignore_bogus_error_responses.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses
      from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses is set
    ansible.posix.sysctl:
      name: net.ipv4.icmp_ignore_bogus_error_responses
      value: '{{ sysctl_net_ipv4_icmp_ignore_bogus_error_responses_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses is set'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.tcp_syncookies.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.tcp_syncookies from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.tcp_syncookies
      replace: '#net.ipv4.tcp_syncookies'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.tcp_syncookies from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.tcp_syncookies from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.tcp_syncookies from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.tcp_syncookies.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.tcp_syncookies from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.tcp_syncookies from /etc/ufw/sysctl.conf'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Ensure sysctl net.ipv4.tcp_syncookies is set
    ansible.posix.sysctl:
      name: net.ipv4.tcp_syncookies
      value: '{{ sysctl_net_ipv4_tcp_syncookies_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.tcp_syncookies is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.tcp_syncookies is set'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.send_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.send_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.send_redirects
      replace: '#net.ipv4.conf.all.send_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.send_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.send_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.send_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.send_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.send_redirects from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.send_redirects from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.all.send_redirects is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.send_redirects
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.send_redirects is set to 0'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.send_redirects is set to 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.send_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.send_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.send_redirects
      replace: '#net.ipv4.conf.default.send_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.send_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.send_redirects from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.send_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.send_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.send_redirects from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.send_redirects from /etc/ufw/sysctl.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure sysctl net.ipv4.conf.default.send_redirects is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.send_redirects
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.send_redirects is set to 0'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.send_redirects is set to 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.ip_forward.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.ip_forward from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.ip_forward
      replace: '#net.ipv4.ip_forward'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.ip_forward from config files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.ip_forward from config files'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of net.ipv4.ip_forward from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.ip_forward.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.ip_forward from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.ip_forward from /etc/ufw/sysctl.conf'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure sysctl net.ipv4.ip_forward is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.ip_forward
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.ip_forward is set to 0'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.ip_forward is set to 0'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure Base Chains Exist for Nftables - Check Existence of Nftables Table
    ansible.builtin.shell: nft list tables | grep '{{ var_nftables_family }} {{ var_nftables_table }}'
    register: existing_nftables
    changed_when: false
    failed_when: false
    when: '"nftables" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_base_chain
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Base Chains Exist for Nftables - Check Existence of Nftables Table''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Base Chains Exist for Nftables - Check Existence of Nftables Table'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure Base Chains Exist for Nftables - Set NFTables Table
    ansible.builtin.command: nft create table {{ var_nftables_family }} {{ var_nftables_table }}
    when:
    - '"nftables" in ansible_facts.packages'
    - existing_nftables is not skipped and existing_nftables.rc > 0
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_base_chain
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Base Chains Exist for Nftables - Set NFTables Table'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Base Chains Exist for Nftables - Set NFTables Table'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure Base Chains Exist for Nftables - Add Base Chains
    ansible.builtin.command: nft 'add chain {{ var_nftables_family }} {{ var_nftables_table }} {{ item.0 }} { type {{ item.1
      }} hook {{ item.2 }} priority {{ item.3 }} ; policy {{ item.4 }} ; }'
    with_together:
    - '{{ var_nftables_base_chain_names.split(",") }}'
    - '{{ var_nftables_base_chain_types.split(",") }}'
    - '{{ var_nftables_base_chain_hooks.split(",") }}'
    - '{{ var_nftables_base_chain_priorities.split(",") }}'
    - '{{ var_nftables_base_chain_policies.split(",") }}'
    when: '"nftables" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_base_chain
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Base Chains Exist for Nftables - Add Base Chains'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Base Chains Exist for Nftables - Add Base Chains'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Implement Loopback Rules
    ansible.builtin.command: nft add rule inet filter input iif lo accept
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Implement Loopback Rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Implement Loopback Rules'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Create Rule to Drop Input IP Address from Loopback
    ansible.builtin.command: nft add rule inet filter input ip saddr 127.0.0.0/8 counter drop
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Create Rule to Drop Input IP Address from Loopback'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create Rule to Drop Input IP Address from Loopback'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Check if IPv6 is Disabled in grub Configuration
    ansible.builtin.shell: '[ -z "$(grep "^\s*linux" /boot/grub2/grub.cfg | grep -v ipv6.disabled=1)" ]

      '
    register: ipv6_status
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check if IPv6 is Disabled in grub Configuration'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if IPv6 is Disabled in grub Configuration'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Check sysctl value of net.ipv6.conf.all.disable_ipv6
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.disable_ipv6
      state: present
      value: '1'
    check_mode: true
    register: sysctl_ipv6_all
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check sysctl value of net.ipv6.conf.all.disable_ipv6'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check sysctl value of net.ipv6.conf.all.disable_ipv6'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Check sysctl value of net.ipv6.conf.default.disable_ipv6
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.disable_ipv6
      state: present
      value: '1'
    check_mode: true
    register: sysctl_ipv6_default
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check sysctl value of net.ipv6.conf.default.disable_ipv6'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check sysctl value of net.ipv6.conf.default.disable_ipv6'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Implement IPv6 loopback rules
    ansible.builtin.command: nft add rule inet filter input ip6 saddr ::1 counter drop
    when:
    - ( "nftables" in ansible_facts.packages )
    - ipv6_status is not skipped
    - sysctl_ipv6_default is not skipped
    - sysctl_ipv6_all is not skipped
    - ipv6_status.rc == 0 or sysctl_ipv6_all.found > 0 or sysctl_ipv6_default.found > 0
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Implement IPv6 loopback rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Implement IPv6 loopback rules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Collect Existing Nftables
    ansible.builtin.command: nft list table {{ var_nftables_family }} {{ var_nftables_table }}
    register: result_nftables_table_family
    changed_when: false
    failed_when: result_nftables_table_family.rc not in [0, 1]
    when: '"nftables" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_table
  rescue:
  - name: 'StigForge: continue after error in ''Collect Existing Nftables'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect Existing Nftables'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set Nftable Table
    ansible.builtin.command: nft create table {{ var_nftables_family }} {{ var_nftables_table }}
    when:
    - '"nftables" in ansible_facts.packages'
    - result_nftables_table_family is not skipped
    - result_nftables_table_family.rc != 0
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_table
  rescue:
  - name: 'StigForge: continue after error in ''Set Nftable Table'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Nftable Table'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: NetworkManager Deactivate Wireless Network Interfaces
    ansible.builtin.command: nmcli radio wifi off
    when:
    - ( not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] ) )
    - '''NetworkManager'' in ansible_facts.packages'
    - ansible_facts.services['NetworkManager.service'].state == 'running'
    tags:
    - DISA-STIG-UBTU-22-291015
    - NIST-800-171-3.1.16
    - NIST-800-53-AC-18(3)
    - NIST-800-53-AC-18(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - PCI-DSS-Req-1.3.3
    - PCI-DSSv4-1.3
    - PCI-DSSv4-1.3.3
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - unknown_strategy
    - wireless_disable_interfaces
  rescue:
  - name: 'StigForge: continue after error in ''NetworkManager Deactivate Wireless Network Interfaces'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''NetworkManager Deactivate Wireless Network Interfaces'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /var/log/ file(s) recursively
    ansible.builtin.command: find -P /var/log/  -perm /u+xs,g+xws,o+xwrt ! -name "history.log*" ! -name "eipp.log.xz*" ! -name
      "[bw]tmp" ! -name "[bw]tmp.*" ! -name "[bw]tmp-*" ! -name "lastlog" ! -name "lastlog.*" -type f -regextype posix-extended
      -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - DISA-STIG-UBTU-22-232026
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - NIST-800-53-SI-11.1(iii)
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - permissions_local_var_log
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) recursively'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) recursively'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /var/log/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - DISA-STIG-UBTU-22-232026
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - NIST-800-53-SI-11.1(iii)
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - permissions_local_var_log
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_backup_etc_group_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_group_newgroup: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_group_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_group_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      follow: false
      group: '{{ file_groupowner_backup_etc_group_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_gshadow_newgroup: '42'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      follow: false
      group: '{{ file_groupowner_backup_etc_gshadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_backup_etc_passwd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_passwd_newgroup: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_passwd_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_passwd_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      follow: false
      group: '{{ file_groupowner_backup_etc_passwd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_backup_etc_shadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_shadow_newgroup: '42'
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_shadow_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_shadow_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      follow: false
      group: '{{ file_groupowner_backup_etc_shadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_etc_group_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_group_newgroup: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_group_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_group_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/group
    ansible.builtin.file:
      path: /etc/group
      follow: false
      group: '{{ file_groupowner_etc_group_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_gshadow_newgroup: '42'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      follow: false
      group: '{{ file_groupowner_etc_gshadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_etc_passwd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_passwd_newgroup: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_passwd_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_passwd_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      follow: false
      group: '{{ file_groupowner_etc_passwd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_etc_shadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_shadow_newgroup: '42'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_shadow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_shadow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      follow: false
      group: '{{ file_groupowner_etc_shadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_backup_etc_group_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_group_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_group_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_group_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      follow: false
      owner: '{{ file_owner_backup_etc_group_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_backup_etc_gshadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_gshadow_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_gshadow_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_gshadow_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      follow: false
      owner: '{{ file_owner_backup_etc_gshadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_backup_etc_passwd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_passwd_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_passwd_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_passwd_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      follow: false
      owner: '{{ file_owner_backup_etc_passwd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_backup_etc_shadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_shadow_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_shadow_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_shadow_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      follow: false
      owner: '{{ file_owner_backup_etc_shadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_etc_group_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_group_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_group_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_group_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/group
    ansible.builtin.file:
      path: /etc/group
      follow: false
      owner: '{{ file_owner_etc_group_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/group'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_etc_gshadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_gshadow_newown: '0'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_gshadow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_gshadow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      follow: false
      owner: '{{ file_owner_etc_gshadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_etc_passwd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_passwd_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_passwd_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_passwd_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      follow: false
      owner: '{{ file_owner_etc_passwd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_etc_shadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_shadow_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_shadow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_shadow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      follow: false
      owner: '{{ file_owner_etc_shadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/group-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/group-'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - configure_strategy
    - file_permissions_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - configure_strategy
    - file_permissions_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow-'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow-'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/group
    ansible.builtin.file:
      path: /etc/group
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/group'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/group'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure kernel module 'cramfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/cramfs.conf
      regexp: install\s+cramfs
      line: install cramfs /bin/false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_cramfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''cramfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''cramfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure kernel module 'cramfs' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/cramfs.conf
      regexp: ^blacklist cramfs$
      line: blacklist cramfs
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_cramfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''cramfs'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''cramfs'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure kernel module 'usb-storage' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/usb-storage.conf
      regexp: install\s+usb-storage
      line: install usb-storage /bin/false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-291010
    - NIST-800-171-3.1.21
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - PCI-DSSv4-3.4
    - PCI-DSSv4-3.4.2
    - disable_strategy
    - kernel_module_usb-storage_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''usb-storage'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''usb-storage'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure kernel module 'usb-storage' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/usb-storage.conf
      regexp: ^blacklist usb-storage$
      line: blacklist usb-storage
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-291010
    - NIST-800-171-3.1.21
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - PCI-DSSv4-3.4
    - PCI-DSSv4-3.4.2
    - disable_strategy
    - kernel_module_usb-storage_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''usb-storage'' is blacklisted" (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''usb-storage'' is blacklisted" raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /dev/shm: Check information associated to mountpoint'
    command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when: not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option'
    mount:
      path: /dev/shm
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /dev/shm: Check information associated to mountpoint'
    command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when: not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option'
    mount:
      path: /dev/shm
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /dev/shm: Check information associated to mountpoint'
    command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when: not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option'
    mount:
      path: /dev/shm
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /home: Check information associated to mountpoint'
    command: findmnt --fstab '/home'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /home: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /home: If /home not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /home
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: If /home not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: If /home not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /home: Make sure nodev option is part of the to /home options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Make sure nodev option is part of the to /home
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Make sure nodev option is part of the to /home options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nodev Option to /home: Ensure /home is mounted with nodev option'
    mount:
      path: /home
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Ensure /home is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Ensure /home is mounted with nodev option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /home: Check information associated to mountpoint'
    command: findmnt --fstab '/home'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /home: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /home: If /home not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /home
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: If /home not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: If /home not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /home: Make sure nosuid option is part of the to /home options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Make sure nosuid option is part of the to /home
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Make sure nosuid option is part of the to /home options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nosuid Option to /home: Ensure /home is mounted with nosuid option'
    mount:
      path: /home
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Ensure /home is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Ensure /home is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'
    mount:
      path: /tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add noexec Option to /tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add noexec Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'
    mount:
      path: /tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'
    mount:
      path: /tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /var/log/audit: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nodev Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info
      manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nodev Option to /var/log/audit: Make sure nodev option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Make sure nodev option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Make sure nodev option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: 'Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with nodev option'
    mount:
      path: /var/log/audit
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with
      nodev option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with nodev option'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add noexec Option to /var/log/audit: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add noexec Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft
      mount_info manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add noexec Option to /var/log/audit: Make sure noexec option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Make sure noexec option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Make sure noexec option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: 'Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with noexec option'
    mount:
      path: /var/log/audit
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with
      noexec option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with noexec option'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nosuid Option to /var/log/audit: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nosuid Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft
      mount_info manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with nosuid option'
    mount:
      path: /var/log/audit
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with
      nosuid option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with nosuid option'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nodev Option to /var/log: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option'
    mount:
      path: /var/log
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /var/log: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option'
    mount:
      path: /var/log
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /var/log: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option'
    mount:
      path: /var/log
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /var: Check information associated to mountpoint'
    command: findmnt --fstab '/var'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /var: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /var: If /var not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: If /var not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: If /var not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /var: Make sure nodev option is part of the to /var options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Make sure nodev option is part of the to /var options''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Make sure nodev option is part of the to /var options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nodev Option to /var: Ensure /var is mounted with nodev option'
    mount:
      path: /var
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Ensure /var is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Ensure /var is mounted with nodev option'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /var: Check information associated to mountpoint'
    command: findmnt --fstab '/var'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /var: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /var: If /var not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: If /var not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: If /var not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /var: Make sure nosuid option is part of the to /var options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Make sure nosuid option is part of the to /var
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Make sure nosuid option is part of the to /var options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nosuid Option to /var: Ensure /var is mounted with nosuid option'
    mount:
      path: /var
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Ensure /var is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Ensure /var is mounted with nosuid option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /var/tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nodev Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option'
    mount:
      path: /var/tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /var/tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add noexec Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option'
    mount:
      path: /var/tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /var/tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Add nosuid Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: 'Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: 'Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option'
    mount:
      path: /var/tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Disable Core Dumps for All Users - Set dirs, files and regex variables
    ansible.builtin.set_fact:
      limits_dropin_dir: /etc/security/limits.d
      limits_dropin_file: /etc/security/limits.d/10-ssg-hardening.conf
      limits_main_file: /etc/security/limits.conf
      limits_correct_regex: ^\s*\*\s+hard\s+core\s+0\s*$
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Set dirs, files and regex variables'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Set dirs, files and regex variables'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Disable Core Dumps for All Users - Find valid drop-ins for core limit
    ansible.builtin.find:
      paths: '{{ limits_dropin_dir }}'
      patterns: '*.conf'
      contains: '{{ limits_correct_regex }}'
      file_type: file
    register: valid_dropins
    failed_when: false
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Find valid drop-ins for core limit'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Find valid drop-ins for core limit'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable Core Dumps for All Users - Find all drop-ins with any core limit
    ansible.builtin.find:
      paths: '{{ limits_dropin_dir }}'
      patterns: '*.conf'
      contains: ^\s*\*\s+hard\s+core\s+
      file_type: file
    register: all_dropins
    failed_when: false
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Find all drop-ins with any core limit''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Find all drop-ins with any core limit'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Disable Core Dumps for All Users - Get invalid drop-ins
    ansible.builtin.set_fact:
      invalid_dropins: '{{ all_dropins.files | rejectattr(''path'', ''in'', valid_dropins.files | map(attribute=''path'')
        | list) | map(attribute=''path'') | list }}'
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Get invalid drop-ins'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Get invalid drop-ins'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable Core Dumps for All Users - Comment invalid * hard core lines in drop-ins
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: (^\s*\*\s+hard\s+core\s+.*$)
      replace: '#\1'
    loop: '{{ invalid_dropins }}'
    when:
    - '"libpam-runtime" in ansible_facts.packages'
    - invalid_dropins | length > 0
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Comment invalid * hard core lines in drop-ins''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Comment invalid * hard core lines in drop-ins'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Disable Core Dumps for All Users - Check if main limits.conf contains correct core limit
    ansible.builtin.find:
      paths: /etc/security
      patterns: limits.conf
      contains: '{{ limits_correct_regex }}'
      file_type: file
    register: main_valid
    failed_when: false
    when:
    - '"libpam-runtime" in ansible_facts.packages'
    - not (valid_dropins.matched | default(0) > 0)
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Check if main limits.conf contains correct
      core limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Check if main limits.conf contains correct core limit'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Disable Core Dumps for All Users - Set fact if configuration is valid
    ansible.builtin.set_fact:
      core_limit_valid: '{{ (valid_dropins.matched | default(0)) > 0 or (main_valid.matched | default(0)) > 0 }}'
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Set fact if configuration is valid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Set fact if configuration is valid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable Core Dumps for All Users - Ensure drop-in directory exists
    ansible.builtin.file:
      path: '{{ limits_dropin_dir }}'
      state: directory
    when:
    - '"libpam-runtime" in ansible_facts.packages'
    - not core_limit_valid
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Ensure drop-in directory exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Ensure drop-in directory exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable Core Dumps for All Users - Deploy 10-ssg-hardening.conf drop-in with correct core limit
    ansible.builtin.copy:
      dest: '{{ limits_dropin_file }}'
      content: '*     hard   core    0

        '
    when:
    - '"libpam-runtime" in ansible_facts.packages'
    - not core_limit_valid
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Deploy 10-ssg-hardening.conf drop-in with
      correct core limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Deploy 10-ssg-hardening.conf drop-in with correct core limit''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*fs.suid_dumpable.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of fs.suid_dumpable from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*fs.suid_dumpable
      replace: '#fs.suid_dumpable'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of fs.suid_dumpable from config files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of fs.suid_dumpable from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of fs.suid_dumpable from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*fs.suid_dumpable.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of fs.suid_dumpable from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of fs.suid_dumpable from /etc/ufw/sysctl.conf'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure sysctl fs.suid_dumpable is set to 0
    ansible.posix.sysctl:
      name: fs.suid_dumpable
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl fs.suid_dumpable is set to 0'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl fs.suid_dumpable is set to 0'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      contains: ^[\s]*kernel.randomize_va_space.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Comment out any occurrences of kernel.randomize_va_space from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*kernel.randomize_va_space
      replace: '#kernel.randomize_va_space'
    loop: '{{ find_sysctl_d.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of kernel.randomize_va_space from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of kernel.randomize_va_space from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Comment out any occurrences of kernel.randomize_va_space from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*kernel.randomize_va_space.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of kernel.randomize_va_space from /etc/ufw/sysctl.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of kernel.randomize_va_space from /etc/ufw/sysctl.conf'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Ensure sysctl kernel.randomize_va_space is set to 2
    ansible.posix.sysctl:
      name: kernel.randomize_va_space
      value: '2'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl kernel.randomize_va_space is set to 2'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl kernel.randomize_va_space is set to 2'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_cron_d_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_d_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_d_newgroup variable if represented by gid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_d_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure group owner on /etc/cron.d/
    ansible.builtin.file:
      path: /etc/cron.d/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_d_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.d/'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_cron_daily_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_daily_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_daily_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_daily_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure group owner on /etc/cron.daily/
    ansible.builtin.file:
      path: /etc/cron.daily/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_daily_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.daily/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.daily/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_cron_hourly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_hourly_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_hourly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_hourly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure group owner on /etc/cron.hourly/
    ansible.builtin.file:
      path: /etc/cron.hourly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_hourly_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.hourly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.hourly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_cron_monthly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_monthly_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_monthly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_monthly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure group owner on /etc/cron.monthly/
    ansible.builtin.file:
      path: /etc/cron.monthly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_monthly_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.monthly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.monthly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_cron_weekly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_weekly_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_weekly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_weekly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure group owner on /etc/cron.weekly/
    ansible.builtin.file:
      path: /etc/cron.weekly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_weekly_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.weekly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.weekly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_crontab_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_crontab_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_crontab_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_crontab_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      follow: false
      group: '{{ file_groupowner_crontab_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_cron_d_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_d_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_d_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_d_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on directory /etc/cron.d/
    ansible.builtin.file:
      path: /etc/cron.d/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_d_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.d/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_cron_daily_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_daily_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_daily_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_daily_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on directory /etc/cron.daily/
    ansible.builtin.file:
      path: /etc/cron.daily/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_daily_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.daily/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.daily/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_cron_hourly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_hourly_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_hourly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_hourly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on directory /etc/cron.hourly/
    ansible.builtin.file:
      path: /etc/cron.hourly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_hourly_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.hourly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.hourly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_cron_monthly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_monthly_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_monthly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_monthly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on directory /etc/cron.monthly/
    ansible.builtin.file:
      path: /etc/cron.monthly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_monthly_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.monthly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.monthly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_cron_weekly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_weekly_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_weekly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_weekly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on directory /etc/cron.weekly/
    ansible.builtin.file:
      path: /etc/cron.weekly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_weekly_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.weekly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.weekly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_crontab_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_crontab_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_crontab_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_crontab_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      follow: false
      owner: '{{ file_owner_crontab_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/cron.d/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.d/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.d/ file(s)'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /etc/cron.d/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.d/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/cron.daily/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.daily/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.daily/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.daily/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /etc/cron.daily/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.daily/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.daily/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/cron.hourly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.hourly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.hourly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.hourly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /etc/cron.hourly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.hourly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.hourly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/cron.monthly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.monthly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.monthly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.monthly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /etc/cron.monthly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.monthly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.monthly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/cron.weekly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.weekly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.weekly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.weekly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /etc/cron.weekly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.weekly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.weekly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Remove /etc/at.deny
    ansible.builtin.file:
      path: /etc/at.deny
      state: absent
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - disable_strategy
    - file_at_deny_not_exist
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Remove /etc/at.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove /etc/at.deny'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Remove /etc/cron.deny
    ansible.builtin.file:
      path: /etc/cron.deny
      state: absent
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - disable_strategy
    - file_cron_deny_not_exist
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Remove /etc/cron.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove /etc/cron.deny'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_at_allow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_at_allow_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_at_allow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_at_allow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      follow: false
      group: '{{ file_groupowner_at_allow_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Check that the crontab group is defined
    ansible.builtin.getent:
      database: group
      key: crontab
    ignore_errors: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_groupowner_cron_allow_newgroup is undefined
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the crontab group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the crontab group is defined'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_cron_allow_newgroup variable if crontab found
    ansible.builtin.set_fact:
      file_groupowner_cron_allow_newgroup: crontab
    when:
    - '"linux-base" in ansible_facts.packages'
    - ansible_facts.getent_group["crontab"] is defined
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_allow_newgroup variable if crontab found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_allow_newgroup variable if crontab found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      follow: false
      group: '{{ file_groupowner_cron_allow_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_at_allow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_at_allow_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_at_allow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_at_allow_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      follow: false
      owner: '{{ file_owner_at_allow_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_cron_allow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_allow_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_allow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_allow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      follow: false
      owner: '{{ file_owner_cron_allow_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupowner_sshd_config_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_sshd_config_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_sshd_config_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_sshd_config_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      follow: false
      group: '{{ file_groupowner_sshd_config_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_owner_sshd_config_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_sshd_config_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_sshd_config_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_sshd_config_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      follow: false
      owner: '{{ file_owner_sshd_config_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find root:root-owned keys
    ansible.builtin.command: find -H /etc/ssh/ -maxdepth 1 -user root -regex ".*_key$" -type f -group root -perm /u+xs,g+xwrs,o+xwrt
    register: root_owned_keys
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find root:root-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find root:root-owned keys'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for root:root-owned keys
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xwrs,o-xwrt
      state: file
    with_items:
    - '{{ root_owned_keys.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for root:root-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for root:root-owned keys'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/ssh/ file(s)
    ansible.builtin.command: find -P /etc/ssh/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended -regex
      "^.*\.pub$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /etc/ssh/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/ssh/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/ssh/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set SSH Client Alive Count Max
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter ClientAliveCountMax is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        line: ClientAliveCountMax {{ var_sshd_set_keepalive }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Set SSH Client Alive Interval
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter ClientAliveInterval is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        line: ClientAliveInterval {{ sshd_idle_timeout_value }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Disable Host-Based Authentication
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter HostbasedAuthentication is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        line: HostbasedAuthentication no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Disable SSH Access via Empty Passwords
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitEmptyPasswords is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        line: PermitEmptyPasswords no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Disable SSH Support for .rhosts Files
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter IgnoreRhosts is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        line: IgnoreRhosts yes
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Disable SSH Root Login
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitRootLogin is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        line: PermitRootLogin no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Do Not Allow SSH Environment Options
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitUserEnvironment is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        line: PermitUserEnvironment no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Enable PAM
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter UsePAM is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "UsePAM"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        line: UsePAM yes
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Enable SSH Warning Banner
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter Banner is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "Banner"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        line: Banner /etc/issue.net
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure SSH LoginGraceTime is configured
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter LoginGraceTime is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        line: LoginGraceTime {{ var_sshd_set_login_grace_time }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Set LogLevel to INFO
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter LogLevel is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "LogLevel"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        line: LogLevel INFO
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set LogLevel to INFO - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Set SSH authentication attempt limit
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxAuthTries is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        line: MaxAuthTries {{ sshd_max_auth_tries_value }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Set SSH MaxSessions limit
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxSessions is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        line: MaxSessions {{ var_sshd_max_sessions }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure SSH MaxStartups is configured
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxStartups is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        line: MaxStartups {{ var_sshd_set_maxstartups }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Use Only Strong Key Exchange algorithms
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter KexAlgorithms is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
        line: KexAlgorithms {{ sshd_strong_kex }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Use Only Strong Key Exchange algorithms - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Use Only Strong MACs
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MACs"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MACs is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MACs"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MACs"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MACs"| regex_escape }}\s+
        line: MACs {{ sshd_strong_macs }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Use Only Strong MACs - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Get Audit Log Files
    ansible.builtin.command: grep -iw ^log_file /etc/audit/auditd.conf
    failed_when: false
    register: log_file_exists
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Get Audit Log Files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Get Audit Log Files'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Set Log File Facts
    ansible.builtin.set_fact:
      log_file_line: '{{ log_file_exists.stdout | split('' '') | last }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Set Log File Facts'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Set Log File Facts'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Set Default log_file if Not Set
    ansible.builtin.set_fact:
      log_file: /var/log/audit/audit.log
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - (log_file_exists is undefined) or (log_file_exists.stdout | length == 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Set Default log_file if Not
      Set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Set Default log_file if Not Set'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Set log_file From log_file_line if Not Set Already
    ansible.builtin.set_fact:
      log_file: '{{ log_file_line }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - (log_file_line is defined) and (log_file_line | length > 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Set log_file From log_file_line
      if Not Set Already'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Set log_file From log_file_line if Not Set Already''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: System Audit Logs Must Be Group Owned By Root - List All Log File Backups
    ansible.builtin.find:
      path: '{{ log_file | dirname }}'
      patterns: '{{ log_file | basename }}.*'
    register: backup_files
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - List All Log File Backups''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - List All Log File Backups'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Apply Mode to All Backup Log Files
    ansible.builtin.file:
      path: '{{ item }}'
      group: root
    failed_when: false
    loop: '{{ backup_files.files| map(attribute=''path'') | list }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Apply Mode to All Backup Log
      Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Apply Mode to All Backup Log Files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Apply Mode to Log File
    ansible.builtin.file:
      path: '{{ log_file }}'
      group: root
    failed_when: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Apply Mode to Log File'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Apply Mode to Log File'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Set the file_groupownership_audit_configuration_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupownership_audit_configuration_newgroup: '0'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_audit_configuration_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_audit_configuration_newgroup variable if represented by gid'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Find /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.command: find -P /etc/audit/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex "^.*audit(\.rules|d\.conf)$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_audit_configuration_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Find /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex
      "^.*\.rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_audit_configuration_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Set the file_ownership_audit_configuration_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_audit_configuration_newown: '0'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_audit_configuration_newown variable if represented
      by uid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_audit_configuration_newown variable if represented by uid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Find /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.command: find -P /etc/audit/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*audit(\.rules|d\.conf)$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_audit_configuration_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Find /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*\.rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_audit_configuration_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_groupownership_audit_binaries_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupownership_audit_binaries_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_audit_binaries_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_audit_binaries_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure group owner on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/augenrules'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set the file_ownership_audit_binaries_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_audit_binaries_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_audit_binaries_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_audit_binaries_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/audispd
    ansible.builtin.stat:
      path: /sbin/audispd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /sbin/audispd
    ansible.builtin.file:
      path: /sbin/audispd
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure owner on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/auditctl'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/auditctl'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/aureport'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/aureport'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/ausearch'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/ausearch'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/autrace'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/autrace'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/auditd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/audispd
    ansible.builtin.stat:
      path: /sbin/audispd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/audispd
    ansible.builtin.file:
      path: /sbin/audispd
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/audispd'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/audispd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/augenrules'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/augenrules'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Test for existence /etc/audit/auditd.conf
    ansible.builtin.stat:
      path: /etc/audit/auditd.conf
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_auditd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/audit/auditd.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/audit/auditd.conf'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/auditd.conf
    ansible.builtin.file:
      path: /etc/audit/auditd.conf
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_auditd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/auditd.conf'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/auditd.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /etc/audit/rules.d/ file(s)
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -perm /u+xs,g+xwrs,o+xwrt  -type f -regextype posix-extended
      -regex "^.*rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_rulesd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /etc/audit/rules.d/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xwrs,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_rulesd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/audit/rules.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/audit/rules.d/ file(s)'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
