# Source: ComplianceAsCode/content (scap-security-guide) — BSD-3-Clause
# Imported by StigForge; cloud-breaker tasks stripped per policy/cloud-ami.yml
---
- block:
  - name: Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the package facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the package facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_always | default(true) | bool)
- block:
  - name: Ensure prelink is Removed
    ansible.builtin.package:
      name: prelink
      state: absent
    tags:
    - disable_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - package_prelink_removed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure prelink is Removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure prelink is Removed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_prelink_removed | default(true) | bool)
- block:
  - name: Ensure aide is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651010
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_aide_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure aide is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure aide is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_aide_installed | default(true) | bool)
- block:
  - name: Configure AIDE to Verify the Audit Tools - Ensure AIDE is Installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - aide
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure AIDE to Verify the Audit Tools - Ensure AIDE is Installed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure AIDE to Verify the Audit Tools - Ensure AIDE is Installed'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Configure Periodic Execution of AIDE - Ensure AIDE is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure Periodic Execution of AIDE - Ensure AIDE is installed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Periodic Execution of AIDE - Ensure AIDE is installed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_periodic_cron_checking | default(true) | bool)
- block:
  - name: Configure Periodic Execution of AIDE - Install cron
    ansible.builtin.package:
      name: cron
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure Periodic Execution of AIDE - Install cron'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Periodic Execution of AIDE - Install cron'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_periodic_cron_checking | default(true) | bool)
- block:
  - name: Ensure sudo is installed
    ansible.builtin.package:
      name: sudo
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_sudo_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sudo is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sudo is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_sudo_installed | default(true) | bool)
- block:
  - name: Ensure libpam-modules is installed
    ansible.builtin.package:
      name: libpam-modules
      state: present
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_pam_modules_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure libpam-modules is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure libpam-modules is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_pam_modules_installed | default(true) |
    bool)
- block:
  - name: Ensure libpam-pwquality is installed
    ansible.builtin.package:
      name: libpam-pwquality
      state: present
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-215010
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_pam_pwquality_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure libpam-pwquality is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure libpam-pwquality is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_pam_pwquality_installed | default(true)
    | bool)
- block:
  - name: Ensure libpam-runtime is installed
    ansible.builtin.package:
      name: libpam-runtime
      state: present
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_pam_runtime_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure libpam-runtime is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure libpam-runtime is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_pam_runtime_installed | default(true) |
    bool)
- block:
  - name: Ensure apparmor-utils is installed
    ansible.builtin.package:
      name: apparmor-utils
      state: present
    when: ansible_virtualization_type not in ["docker", "lxc", "openvz", "podman", "container"]
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_apparmor-utils_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure apparmor-utils is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure apparmor-utils is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure apparmor is installed
    ansible.builtin.package:
      name: apparmor
      state: present
    when: ansible_virtualization_type not in ["docker", "lxc", "openvz", "podman", "container"]
    tags:
    - DISA-STIG-UBTU-22-431010
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_apparmor_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure apparmor is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure apparmor is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_apparmor_installed | default(true) | bool)
- block:
  - name: Ensure systemd-journal-remote is installed
    ansible.builtin.package:
      name: systemd-journal-remote
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_systemd-journal-remote_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure systemd-journal-remote is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure systemd-journal-remote is installed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure iptables-persistent is installed
    ansible.builtin.package:
      name: iptables-persistent
      state: present
    when:
    - '"iptables" in ansible_facts.packages'
    - var_network_filtering_service == "iptables"
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_iptables-persistent_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure iptables-persistent is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure iptables-persistent is installed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure iptables is installed
    ansible.builtin.package:
      name: iptables
      state: present
    when:
    - ( "linux-base" in ansible_facts.packages )
    - var_network_filtering_service == "iptables"
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-1.4.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_iptables_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure iptables is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure iptables is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_iptables_installed | default(true) | bool)
- block:
  - name: 'Remove iptables-persistent Package: Ensure iptables-persistent is removed'
    ansible.builtin.package:
      name: iptables-persistent
      state: absent
    when: '"ufw" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_iptables-persistent_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove iptables-persistent Package: Ensure iptables-persistent is removed''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove iptables-persistent Package: Ensure iptables-persistent is removed'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure nftables is installed
    ansible.builtin.package:
      name: nftables
      state: present
    when:
    - ( "linux-base" in ansible_facts.packages )
    - var_network_filtering_service == "nftables"
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_nftables_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure nftables is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure nftables is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_nftables_installed | default(true) | bool)
- block:
  - name: Ensure ufw is installed
    ansible.builtin.package:
      name: ufw
      state: present
    when:
    - '"linux-base" in ansible_facts.packages'
    - var_network_filtering_service is regex("ufw")
    tags:
    - DISA-STIG-UBTU-22-251010
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_ufw_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure ufw is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure ufw is installed'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_ufw_installed | default(true) | bool)
- block:
  - name: Ensure ufw is removed
    ansible.builtin.package:
      name: ufw
      state: absent
    when:
    - '"linux-base" in ansible_facts.packages'
    - var_network_filtering_service != "ufw"
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_ufw_removed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure ufw is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure ufw is removed'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_ufw_removed | default(true) | bool)
- block:
  - name: 'Remove autofs Package: Ensure autofs is removed'
    ansible.builtin.package:
      name: autofs
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_autofs_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove autofs Package: Ensure autofs is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove autofs Package: Ensure autofs is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_autofs_removed | default(true) | bool)
- block:
  - name: 'Uninstall avahi Server Package: Ensure avahi-daemon is removed'
    ansible.builtin.package:
      name: avahi-daemon
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_avahi_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall avahi Server Package: Ensure avahi-daemon is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall avahi Server Package: Ensure avahi-daemon is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_avahi_removed | default(true) | bool)
- block:
  - name: Ensure cron is installed
    ansible.builtin.package:
      name: cron
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_cron_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure cron is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure cron is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_cron_installed | default(true) | bool)
- block:
  - name: 'Uninstall the nis package: Ensure nis is removed'
    ansible.builtin.package:
      name: nis
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_nis_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall the nis package: Ensure nis is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall the nis package: Ensure nis is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_nis_removed | default(true) | bool)
- block:
  - name: 'Uninstall DHCP Server Package: Ensure isc-dhcp-server is removed'
    ansible.builtin.package:
      name: isc-dhcp-server
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_dhcp_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall DHCP Server Package: Ensure isc-dhcp-server is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall DHCP Server Package: Ensure isc-dhcp-server is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_dhcp_removed | default(true) | bool)
- block:
  - name: 'Uninstall dnsmasq Package: Ensure dnsmasq is removed'
    ansible.builtin.package:
      name: dnsmasq
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_dnsmasq_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall dnsmasq Package: Ensure dnsmasq is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall dnsmasq Package: Ensure dnsmasq is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_dnsmasq_removed | default(true) | bool)
- block:
  - name: 'Uninstall bind Package: Ensure bind9 is removed'
    ansible.builtin.package:
      name: bind9
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_bind_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall bind Package: Ensure bind9 is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall bind Package: Ensure bind9 is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_bind_removed | default(true) | bool)
- block:
  - name: 'Remove ftp Package: Ensure ftp is removed'
    ansible.builtin.package:
      name: ftp
      state: absent
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_ftp_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove ftp Package: Ensure ftp is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove ftp Package: Ensure ftp is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_ftp_removed | default(true) | bool)
- block:
  - name: 'Uninstall vsftpd Package: Ensure vsftpd is removed'
    ansible.builtin.package:
      name: vsftpd
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-CM-7.1(ii)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(1).1(v)
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_vsftpd_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall vsftpd Package: Ensure vsftpd is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall vsftpd Package: Ensure vsftpd is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_vsftpd_removed | default(true) | bool)
- block:
  - name: 'Uninstall apache2 Package: Ensure apache2 is removed'
    ansible.builtin.package:
      name: apache2
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_httpd_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall apache2 Package: Ensure apache2 is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall apache2 Package: Ensure apache2 is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_httpd_removed | default(true) | bool)
- block:
  - name: 'Uninstall nginx Package: Ensure nginx is removed'
    ansible.builtin.package:
      name: nginx
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_nginx_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall nginx Package: Ensure nginx is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall nginx Package: Ensure nginx is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_nginx_removed | default(true) | bool)
- block:
  - name: 'Uninstall dovecot Package: Ensure dovecot-core is removed'
    ansible.builtin.package:
      name: dovecot-core
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_dovecot_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall dovecot Package: Ensure dovecot-core is removed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall dovecot Package: Ensure dovecot-core is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_dovecot_removed | default(true) | bool)
- block:
  - name: 'Ensure LDAP client is not installed: Ensure ldap-utils is removed'
    ansible.builtin.package:
      name: ldap-utils
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_openldap-clients_removed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure LDAP client is not installed: Ensure ldap-utils is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure LDAP client is not installed: Ensure ldap-utils is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall slapd Package: Ensure slapd is removed'
    ansible.builtin.package:
      name: slapd
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_openldap-servers_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall slapd Package: Ensure slapd is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall slapd Package: Ensure slapd is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall nfs-kernel-server Package: Ensure nfs-kernel-server is removed'
    ansible.builtin.package:
      name: nfs-kernel-server
      state: absent
    tags:
    - DISA-STIG-UBTU-22-215040
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_nfs-kernel-server_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall nfs-kernel-server Package: Ensure nfs-kernel-server is removed''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall nfs-kernel-server Package: Ensure nfs-kernel-server is removed'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Uninstall rpcbind Package: Ensure rpcbind is removed'
    ansible.builtin.package:
      name: rpcbind
      state: absent
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_rpcbind_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall rpcbind Package: Ensure rpcbind is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall rpcbind Package: Ensure rpcbind is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_rpcbind_removed | default(true) | bool)
- block:
  - name: Ensure chrony is installed
    ansible.builtin.package:
      name: chrony
      state: present
    when:
    - '"linux-base" in ansible_facts.packages'
    - var_timesync_service == "chronyd"
    tags:
    - DISA-STIG-UBTU-22-215015
    - PCI-DSS-Req-10.4
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_chrony_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure chrony is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure chrony is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_chrony_installed | default(true) | bool)
- block:
  - name: Ensure systemd-timesyncd is installed
    ansible.builtin.package:
      name: systemd-timesyncd
      state: present
    when:
    - '"linux-base" in ansible_facts.packages'
    - var_timesync_service == "systemd-timesyncd"
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4
    - enable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_timesyncd_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure systemd-timesyncd is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure systemd-timesyncd is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_timesyncd_installed | default(true) | bool)
- block:
  - name: 'Uninstall rsync Package: Ensure rsync is removed'
    ansible.builtin.package:
      name: rsync
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_rsync_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall rsync Package: Ensure rsync is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall rsync Package: Ensure rsync is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_rsync_removed | default(true) | bool)
- block:
  - name: 'Uninstall xinetd package if not used by network services: Ensure xinetd is removed'
    ansible.builtin.package:
      name: xinetd
      state: absent
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_xinetd_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall xinetd package if not used by network services: Ensure xinetd is
      removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall xinetd package if not used by network services: Ensure xinetd is removed'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_xinetd_removed | default(true) | bool)
- block:
  - name: 'Uninstall ypserv Package: Ensure ypserv is removed'
    ansible.builtin.package:
      name: ypserv
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-5(1)(c)
    - PCI-DSS-Req-2.2.2
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_ypserv_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall ypserv Package: Ensure ypserv is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall ypserv Package: Ensure ypserv is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_ypserv_removed | default(true) | bool)
- block:
  - name: 'Uninstall rsh Package: Ensure rsh-client is removed'
    ansible.builtin.package:
      name: rsh-client
      state: absent
    tags:
    - NIST-800-171-3.1.13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_rsh_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall rsh Package: Ensure rsh-client is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall rsh Package: Ensure rsh-client is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_rsh_removed | default(true) | bool)
- block:
  - name: 'Uninstall talk Package: Ensure talk is removed'
    ansible.builtin.package:
      name: talk
      state: absent
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_talk_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall talk Package: Ensure talk is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall talk Package: Ensure talk is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_talk_removed | default(true) | bool)
- block:
  - name: 'Remove telnet Clients: Ensure telnet is removed'
    ansible.builtin.package:
      name: telnet
      state: absent
    tags:
    - NIST-800-171-3.1.13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_telnet_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove telnet Clients: Ensure telnet is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove telnet Clients: Ensure telnet is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_telnet_removed | default(true) | bool)
- block:
  - name: 'Uninstall tftpd-hpa Package: Ensure tftpd-hpa is removed'
    ansible.builtin.package:
      name: tftpd-hpa
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_tftp-server_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall tftpd-hpa Package: Ensure tftpd-hpa is removed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall tftpd-hpa Package: Ensure tftpd-hpa is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall CUPS Package: Ensure cups is removed'
    ansible.builtin.package:
      name: cups
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_cups_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall CUPS Package: Ensure cups is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall CUPS Package: Ensure cups is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_cups_removed | default(true) | bool)
- block:
  - name: 'Uninstall squid Package: Ensure squid is removed'
    ansible.builtin.package:
      name: squid
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_squid_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall squid Package: Ensure squid is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall squid Package: Ensure squid is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_squid_removed | default(true) | bool)
- block:
  - name: 'Uninstall Samba Package: Ensure samba is removed'
    ansible.builtin.package:
      name: samba
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_samba_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall Samba Package: Ensure samba is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall Samba Package: Ensure samba is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_samba_removed | default(true) | bool)
- block:
  - name: 'Uninstall net-snmp Package: Ensure snmp is removed'
    ansible.builtin.package:
      name: snmp
      state: absent
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_net-snmp_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall net-snmp Package: Ensure snmp is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall net-snmp Package: Ensure snmp is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure audispd-plugins is installed
    ansible.builtin.package:
      name: audispd-plugins
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653020
    - PCI-DSS-Req-10.5.3
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.3
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_audit-audispd-plugins_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure audispd-plugins is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure audispd-plugins is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure auditd is installed
    ansible.builtin.package:
      name: auditd
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653010
    - NIST-800-53-AC-7(a)
    - NIST-800-53-AU-12(2)
    - NIST-800-53-AU-14
    - NIST-800-53-AU-2(a)
    - NIST-800-53-AU-7(1)
    - NIST-800-53-AU-7(2)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.1
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_audit_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_audit_installed | default(true) | bool)
- block:
  - name: Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the package facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the package facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_always | default(true) | bool)
- block:
  - name: Enable systemd-journal-upload Service - Enable service systemd-journal-upload
    block:
    - name: Enable systemd-journal-upload Service - Enable Service systemd-journal-upload
      ansible.builtin.systemd:
        name: systemd-journal-upload
        enabled: true
        state: started
        masked: false
      when:
      - '"systemd-journal-remote" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_systemd-journal-upload_enabled
    - special_service_block
    when:
    - '"linux-base" in ansible_facts.packages'
    - ( ansible_virtualization_type not in ["docker", "lxc", "openvz", "podman", "container"] and "systemd-journal-remote"
      in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Enable systemd-journal-upload Service - Enable service systemd-journal-upload''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable systemd-journal-upload Service - Enable service systemd-journal-upload'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_special_service_block | default(true) | bool)
- block:
  - name: Enable systemd-journald Service - Enable service systemd-journald
    block:
    - name: Enable systemd-journald Service - Enable Service systemd-journald
      ansible.builtin.systemd:
        name: systemd-journald
        enabled: true
        state: started
        masked: false
      when:
      - '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-24
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_systemd-journald_enabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable systemd-journald Service - Enable service systemd-journald'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable systemd-journald Service - Enable service systemd-journald'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_special_service_block | default(true) | bool)
- block:
  - name: Verify nftables Service is Enabled - Enable service nftables
    block:
    - name: Verify nftables Service is Enabled - Enable Service nftables
      ansible.builtin.systemd:
        name: nftables
        enabled: true
        state: started
        masked: false
      when:
      - '"nftables" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_nftables_enabled
    - special_service_block
    when: ( "nftables" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Verify nftables Service is Enabled - Enable service nftables'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify nftables Service is Enabled - Enable service nftables'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_nftables_enabled | default(true) | bool)
- block:
  - name: Verify nftables Service is Disabled - Disable service nftables
    block:
    - name: Verify nftables Service is Disabled - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Verify nftables Service is Disabled - Ensure nftables.service is Masked
      ansible.builtin.systemd:
        name: nftables.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("nftables.service", multiline=True)
    - name: Unit Socket Exists - nftables.socket
      ansible.builtin.command: systemctl -q list-unit-files nftables.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Verify nftables Service is Disabled - Disable Socket nftables
      ansible.builtin.systemd:
        name: nftables.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("nftables.socket", multiline=True)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.1
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_nftables_disabled
    - special_service_block
    when: ( "nftables" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Verify nftables Service is Disabled - Disable service nftables'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify nftables Service is Disabled - Disable service nftables'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_nftables_disabled | default(true) | bool)
- block:
  - name: Disable Bluetooth Service - Disable service bluetooth
    block:
    - name: Disable Bluetooth Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Bluetooth Service - Ensure bluetooth.service is Masked
      ansible.builtin.systemd:
        name: bluetooth.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("bluetooth.service", multiline=True)
    - name: Unit Socket Exists - bluetooth.socket
      ansible.builtin.command: systemctl -q list-unit-files bluetooth.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Bluetooth Service - Disable Socket bluetooth
      ansible.builtin.systemd:
        name: bluetooth.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("bluetooth.socket", multiline=True)
    tags:
    - NIST-800-171-3.1.16
    - NIST-800-53-AC-18(3)
    - NIST-800-53-AC-18(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_bluetooth_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable Bluetooth Service - Disable service bluetooth'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Bluetooth Service - Disable service bluetooth'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_bluetooth_disabled | default(true) | bool)
- block:
  - name: Disable the Automounter - Disable service autofs
    block:
    - name: Disable the Automounter - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the Automounter - Ensure autofs.service is Masked
      ansible.builtin.systemd:
        name: autofs.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("autofs.service", multiline=True)
    - name: Unit Socket Exists - autofs.socket
      ansible.builtin.command: systemctl -q list-unit-files autofs.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the Automounter - Disable Socket autofs
      ansible.builtin.systemd:
        name: autofs.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("autofs.socket", multiline=True)
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_autofs_disabled
    - special_service_block
    when: ( "autofs" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Disable the Automounter - Disable service autofs'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable the Automounter - Disable service autofs'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_autofs_disabled | default(true) | bool)
- block:
  - name: Disable Apport Service - Disable service apport
    block:
    - name: Disable Apport Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Apport Service - Ensure apport.service is Masked
      ansible.builtin.systemd:
        name: apport.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("apport.service", multiline=True)
    - name: Unit Socket Exists - apport.socket
      ansible.builtin.command: systemctl -q list-unit-files apport.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Apport Service - Disable Socket apport
      ansible.builtin.systemd:
        name: apport.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("apport.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_apport_disabled
    - special_service_block
    - unknown_severity
    when: '"apport" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable Apport Service - Disable service apport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Apport Service - Disable service apport'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_apport_disabled | default(true) | bool)
- block:
  - name: Disable Avahi Server Software - Disable service avahi-daemon
    block:
    - name: Disable Avahi Server Software - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Avahi Server Software - Ensure avahi-daemon.service is Masked
      ansible.builtin.systemd:
        name: avahi-daemon.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("avahi-daemon.service", multiline=True)
    - name: Unit Socket Exists - avahi-daemon.socket
      ansible.builtin.command: systemctl -q list-unit-files avahi-daemon.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Avahi Server Software - Disable Socket avahi-daemon
      ansible.builtin.systemd:
        name: avahi-daemon.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("avahi-daemon.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_avahi-daemon_disabled
    - special_service_block
    when: ( "avahi-daemon" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Disable Avahi Server Software - Disable service avahi-daemon'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Avahi Server Software - Disable service avahi-daemon'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_special_service_block | default(true) | bool)
- block:
  - name: Enable cron Service - Enable service cron
    block:
    - name: Enable cron Service - Enable Service cron
      ansible.builtin.systemd:
        name: cron
        enabled: true
        state: started
        masked: false
      when:
      - '"cron" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_cron_enabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable cron Service - Enable service cron'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable cron Service - Enable service cron'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_special_service_block | default(true) | bool)
- block:
  - name: Disable DHCPD6 Service - Disable service dhcpd6
    block:
    - name: Disable DHCPD6 Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable DHCPD6 Service - Ensure dhcpd6.service is Masked
      ansible.builtin.systemd:
        name: dhcpd6.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("dhcpd6.service", multiline=True)
    - name: Unit Socket Exists - dhcpd6.socket
      ansible.builtin.command: systemctl -q list-unit-files dhcpd6.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable DHCPD6 Service - Disable Socket dhcpd6
      ansible.builtin.systemd:
        name: dhcpd6.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("dhcpd6.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_dhcpd6_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable DHCPD6 Service - Disable service dhcpd6'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable DHCPD6 Service - Disable service dhcpd6'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_dhcpd6_disabled | default(true) | bool)
- block:
  - name: Disable DHCP Service - Disable service dhcpd
    block:
    - name: Disable DHCP Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable DHCP Service - Ensure dhcpd.service is Masked
      ansible.builtin.systemd:
        name: dhcpd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("dhcpd.service", multiline=True)
    - name: Unit Socket Exists - dhcpd.socket
      ansible.builtin.command: systemctl -q list-unit-files dhcpd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable DHCP Service - Disable Socket dhcpd
      ansible.builtin.systemd:
        name: dhcpd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("dhcpd.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_dhcpd_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable DHCP Service - Disable service dhcpd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable DHCP Service - Disable service dhcpd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_dhcpd_disabled | default(true) | bool)
- block:
  - name: Disable dnsmasq Service - Disable service dnsmasq
    block:
    - name: Disable dnsmasq Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable dnsmasq Service - Ensure dnsmasq.service is Masked
      ansible.builtin.systemd:
        name: dnsmasq.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("dnsmasq.service", multiline=True)
    - name: Unit Socket Exists - dnsmasq.socket
      ansible.builtin.command: systemctl -q list-unit-files dnsmasq.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable dnsmasq Service - Disable Socket dnsmasq
      ansible.builtin.systemd:
        name: dnsmasq.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("dnsmasq.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_dnsmasq_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable dnsmasq Service - Disable service dnsmasq'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable dnsmasq Service - Disable service dnsmasq'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_dnsmasq_disabled | default(true) | bool)
- block:
  - name: Disable vsftpd Service - Disable service vsftpd
    block:
    - name: Disable vsftpd Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable vsftpd Service - Ensure vsftpd.service is Masked
      ansible.builtin.systemd:
        name: vsftpd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("vsftpd.service", multiline=True)
    - name: Unit Socket Exists - vsftpd.socket
      ansible.builtin.command: systemctl -q list-unit-files vsftpd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable vsftpd Service - Disable Socket vsftpd
      ansible.builtin.systemd:
        name: vsftpd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("vsftpd.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_vsftpd_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable vsftpd Service - Disable service vsftpd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable vsftpd Service - Disable service vsftpd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_vsftpd_disabled | default(true) | bool)
- block:
  - name: Disable apache2 Service - Disable service apache2
    block:
    - name: Disable apache2 Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable apache2 Service - Ensure apache2.service is Masked
      ansible.builtin.systemd:
        name: apache2.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("apache2.service", multiline=True)
    - name: Unit Socket Exists - apache2.socket
      ansible.builtin.command: systemctl -q list-unit-files apache2.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable apache2 Service - Disable Socket apache2
      ansible.builtin.systemd:
        name: apache2.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("apache2.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_httpd_disabled
    - special_service_block
    - unknown_severity
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable apache2 Service - Disable service apache2'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable apache2 Service - Disable service apache2'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_httpd_disabled | default(true) | bool)
- block:
  - name: Disable nginx Service - Disable service nginx
    block:
    - name: Disable nginx Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable nginx Service - Ensure nginx.service is Masked
      ansible.builtin.systemd:
        name: nginx.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("nginx.service", multiline=True)
    - name: Unit Socket Exists - nginx.socket
      ansible.builtin.command: systemctl -q list-unit-files nginx.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable nginx Service - Disable Socket nginx
      ansible.builtin.systemd:
        name: nginx.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("nginx.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_nginx_disabled
    - special_service_block
    - unknown_severity
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable nginx Service - Disable service nginx'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable nginx Service - Disable service nginx'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_nginx_disabled | default(true) | bool)
- block:
  - name: Disable Dovecot Service - Disable service dovecot
    block:
    - name: Disable Dovecot Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Dovecot Service - Ensure dovecot.service is Masked
      ansible.builtin.systemd:
        name: dovecot.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("dovecot.service", multiline=True)
    - name: Unit Socket Exists - dovecot.socket
      ansible.builtin.command: systemctl -q list-unit-files dovecot.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Dovecot Service - Disable Socket dovecot
      ansible.builtin.systemd:
        name: dovecot.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("dovecot.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_dovecot_disabled
    - special_service_block
    - unknown_severity
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable Dovecot Service - Disable service dovecot'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Dovecot Service - Disable service dovecot'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_dovecot_disabled | default(true) | bool)
- block:
  - name: Disable LDAP Server (slapd) - Disable service slapd
    block:
    - name: Disable LDAP Server (slapd) - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable LDAP Server (slapd) - Ensure slapd.service is Masked
      ansible.builtin.systemd:
        name: slapd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("slapd.service", multiline=True)
    - name: Unit Socket Exists - slapd.socket
      ansible.builtin.command: systemctl -q list-unit-files slapd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable LDAP Server (slapd) - Disable Socket slapd
      ansible.builtin.systemd:
        name: slapd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("slapd.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_slapd_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable LDAP Server (slapd) - Disable service slapd'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable LDAP Server (slapd) - Disable service slapd'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_slapd_disabled | default(true) | bool)
- block:
  - name: Disable rpcbind Service - Disable service rpcbind
    block:
    - name: Disable rpcbind Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable rpcbind Service - Ensure rpcbind.service is Masked
      ansible.builtin.systemd:
        name: rpcbind.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("rpcbind.service", multiline=True)
    - name: Unit Socket Exists - rpcbind.socket
      ansible.builtin.command: systemctl -q list-unit-files rpcbind.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable rpcbind Service - Disable Socket rpcbind
      ansible.builtin.systemd:
        name: rpcbind.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("rpcbind.socket", multiline=True)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - service_rpcbind_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable rpcbind Service - Disable service rpcbind'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable rpcbind Service - Disable service rpcbind'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_rpcbind_disabled | default(true) | bool)
- block:
  - name: Disable Network File System (nfs) - Disable service nfs-server
    block:
    - name: Disable Network File System (nfs) - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Network File System (nfs) - Ensure nfs-server.service is Masked
      ansible.builtin.systemd:
        name: nfs-server.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("nfs-server.service", multiline=True)
    - name: Unit Socket Exists - nfs-server.socket
      ansible.builtin.command: systemctl -q list-unit-files nfs-server.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Network File System (nfs) - Disable Socket nfs-server
      ansible.builtin.systemd:
        name: nfs-server.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("nfs-server.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_nfs_disabled
    - special_service_block
    - unknown_severity
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable Network File System (nfs) - Disable service nfs-server'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Network File System (nfs) - Disable service nfs-server'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_special_service_block | default(true) | bool)
- block:
  - name: Ensure rsyncd service is disabled - Disable service rsyncd
    block:
    - name: Ensure rsyncd service is disabled - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Ensure rsyncd service is disabled - Ensure rsyncd.service is Masked
      ansible.builtin.systemd:
        name: rsyncd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("rsyncd.service", multiline=True)
    - name: Unit Socket Exists - rsyncd.socket
      ansible.builtin.command: systemctl -q list-unit-files rsyncd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Ensure rsyncd service is disabled - Disable Socket rsyncd
      ansible.builtin.systemd:
        name: rsyncd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("rsyncd.socket", multiline=True)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_rsyncd_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyncd service is disabled - Disable service rsyncd'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyncd service is disabled - Disable service rsyncd'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_rsyncd_disabled | default(true) | bool)
- block:
  - name: Disable xinetd Service - Disable service xinetd
    block:
    - name: Disable xinetd Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable xinetd Service - Ensure xinetd.service is Masked
      ansible.builtin.systemd:
        name: xinetd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("xinetd.service", multiline=True)
    - name: Unit Socket Exists - xinetd.socket
      ansible.builtin.command: systemctl -q list-unit-files xinetd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable xinetd Service - Disable Socket xinetd
      ansible.builtin.systemd:
        name: xinetd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("xinetd.socket", multiline=True)
    tags:
    - NIST-800-171-3.4.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_xinetd_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable xinetd Service - Disable service xinetd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable xinetd Service - Disable service xinetd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_xinetd_disabled | default(true) | bool)
- block:
  - name: Disable ypserv Service - Disable service ypserv
    block:
    - name: Disable ypserv Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable ypserv Service - Ensure ypserv.service is Masked
      ansible.builtin.systemd:
        name: ypserv.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("ypserv.service", multiline=True)
    - name: Unit Socket Exists - ypserv.socket
      ansible.builtin.command: systemctl -q list-unit-files ypserv.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable ypserv Service - Disable Socket ypserv
      ansible.builtin.systemd:
        name: ypserv.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("ypserv.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_ypserv_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable ypserv Service - Disable service ypserv'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable ypserv Service - Disable service ypserv'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_ypserv_disabled | default(true) | bool)
- block:
  - name: Disable tftpd-hpa Service - Disable service tftpd-hpa
    block:
    - name: Disable tftpd-hpa Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable tftpd-hpa Service - Ensure tftpd-hpa.service is Masked
      ansible.builtin.systemd:
        name: tftpd-hpa.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("tftpd-hpa.service", multiline=True)
    - name: Unit Socket Exists - tftpd-hpa.socket
      ansible.builtin.command: systemctl -q list-unit-files tftpd-hpa.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable tftpd-hpa Service - Disable Socket tftpd-hpa
      ansible.builtin.systemd:
        name: tftpd-hpa.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("tftpd-hpa.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_tftp_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable tftpd-hpa Service - Disable service tftpd-hpa'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable tftpd-hpa Service - Disable service tftpd-hpa'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_tftp_disabled | default(true) | bool)
- block:
  - name: Disable the CUPS Service - Disable service cups
    block:
    - name: Disable the CUPS Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the CUPS Service - Ensure cups.service is Masked
      ansible.builtin.systemd:
        name: cups.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("cups.service", multiline=True)
    - name: Unit Socket Exists - cups.socket
      ansible.builtin.command: systemctl -q list-unit-files cups.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the CUPS Service - Disable Socket cups
      ansible.builtin.systemd:
        name: cups.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("cups.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_cups_disabled
    - special_service_block
    - unknown_severity
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable the CUPS Service - Disable service cups'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable the CUPS Service - Disable service cups'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_cups_disabled | default(true) | bool)
- block:
  - name: Disable Squid - Disable service squid
    block:
    - name: Disable Squid - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Squid - Ensure squid.service is Masked
      ansible.builtin.systemd:
        name: squid.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("squid.service", multiline=True)
    - name: Unit Socket Exists - squid.socket
      ansible.builtin.command: systemctl -q list-unit-files squid.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Squid - Disable Socket squid
      ansible.builtin.systemd:
        name: squid.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("squid.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_squid_disabled
    - special_service_block
    - unknown_severity
    when: ( "squid" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Disable Squid - Disable service squid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Squid - Disable service squid'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_squid_disabled | default(true) | bool)
- block:
  - name: Disable Samba - Disable service smbd
    block:
    - name: Disable Samba - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Samba - Ensure smbd.service is Masked
      ansible.builtin.systemd:
        name: smbd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("smbd.service", multiline=True)
    - name: Unit Socket Exists - smbd.socket
      ansible.builtin.command: systemctl -q list-unit-files smbd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Samba - Disable Socket smbd
      ansible.builtin.systemd:
        name: smbd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("smbd.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - service_smb_disabled
    - special_service_block
    when: '"linux-base" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable Samba - Disable service smbd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Samba - Disable service smbd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_special_service_block | default(true) | bool)
- block:
  - name: Disable snmpd Service - Disable service snmpd
    block:
    - name: Disable snmpd Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable snmpd Service - Ensure snmpd.service is Masked
      ansible.builtin.systemd:
        name: snmpd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("snmpd.service", multiline=True)
    - name: Unit Socket Exists - snmpd.socket
      ansible.builtin.command: systemctl -q list-unit-files snmpd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable snmpd Service - Disable Socket snmpd
      ansible.builtin.systemd:
        name: snmpd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("snmpd.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - service_snmpd_disabled
    - special_service_block
    when: ( "snmp" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Disable snmpd Service - Disable service snmpd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable snmpd Service - Disable service snmpd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_snmpd_disabled | default(true) | bool)
- block:
  - name: Enable auditd Service - Enable service auditd
    block:
    - name: Enable auditd Service - Enable Service auditd
      ansible.builtin.systemd:
        name: auditd
        enabled: true
        state: started
        masked: false
      when:
      - '"auditd" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-653015
    - NIST-800-171-3.3.1
    - NIST-800-171-3.3.2
    - NIST-800-171-3.3.6
    - NIST-800-53-AC-2(g)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-10
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-14(1)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-AU-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-4(23)
    - PCI-DSS-Req-10.1
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_auditd_enabled
    - special_service_block
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"auditd" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable auditd Service - Enable service auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable auditd Service - Enable service auditd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_auditd_enabled | default(true) | bool)
- block:
  - name: Gather the service facts
    ansible.builtin.service_facts: null
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the service facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the service facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_always | default(true) | bool)
- block:
  - name: Check If Prelinked Is Installed
    ansible.builtin.stat:
      path: /usr/sbin/prelink
      get_checksum: false
    register: prelink
    tags:
    - disable_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - package_prelink_removed
  rescue:
  - name: 'StigForge: continue after error in ''Check If Prelinked Is Installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check If Prelinked Is Installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_prelink_removed | default(true) | bool)
- block:
  - name: Restore Prelinked Binaries
    ansible.builtin.command:
      cmd: prelink -ua
    when: (prelink is not skipped and prelink.stat.exists)
    tags:
    - disable_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - package_prelink_removed
  rescue:
  - name: 'StigForge: continue after error in ''Restore Prelinked Binaries'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restore Prelinked Binaries'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_package_prelink_removed | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Ensure AIDE Is Installed
    ansible.builtin.apt:
      name: aide
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Ensure AIDE Is Installed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Ensure AIDE Is Installed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Check if DB Path in /etc/aide/aide.conf Is Already Set
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database=)(.*)$
      state: absent
    check_mode: true
    changed_when: false
    register: database_replace
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Check if DB Path in /etc/aide/aide.conf Is
      Already Set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Check if DB Path in /etc/aide/aide.conf Is Already Set'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Check if DB Out Path in /etc/aide/aide.conf Is Already Set
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database_out=)(.*)$
      state: absent
    check_mode: true
    changed_when: false
    register: database_out_replace
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Check if DB Out Path in /etc/aide/aide.conf
      Is Already Set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Check if DB Out Path in /etc/aide/aide.conf Is Already Set'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Fix DB Path in Config File if Necessary
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database)(\s*)=(\s*)(.*)$
      line: \2\3=\4file:/var/lib/aide/aide.db
      backrefs: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_replace.found > 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Fix DB Path in Config File if Necessary'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Fix DB Path in Config File if Necessary'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Fix DB Out Path in Config File if Necessary
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^#?(\s*)(database_out)(\s*)=(\s*)(.*)$
      line: \2\3=\4file:/var/lib/aide/aide.db.new
      backrefs: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_out_replace.found > 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Fix DB Out Path in Config File if Necessary''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Fix DB Out Path in Config File if Necessary'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Ensure the Default DB Path is Added
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      line: database=file:/var/lib/aide/aide.db
      create: true
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_replace.found == 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Ensure the Default DB Path is Added'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Ensure the Default DB Path is Added'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Ensure the Default Out Path is Added
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      line: database_out=file:/var/lib/aide/aide.db.new
      create: true
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - database_out_replace.found == 0
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Ensure the Default Out Path is Added'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Ensure the Default Out Path is Added'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Build and Test AIDE Database
    ansible.builtin.command: /usr/sbin/aideinit -y -f
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651015
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Build and Test AIDE Database'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Build and Test AIDE Database'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_build_database | default(true) | bool)
- block:
  - name: Set "Configure AIDE to Verify the Audit Tools - audit_tools fact"
    ansible.builtin.set_fact:
      audit_tools:
      - /usr/sbin/auditctl
      - /usr/sbin/auditd
      - /usr/sbin/ausearch
      - /usr/sbin/aureport
      - /usr/sbin/autrace
      - /usr/sbin/augenrules
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set "Configure AIDE to Verify the Audit Tools - audit_tools fact"'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set "Configure AIDE to Verify the Audit Tools - audit_tools fact"'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Configure AIDE to Verify the Audit Tools - Ensure Existing AIDE Configuration for Audit Tools are Correct
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      regexp: ^{{ item }}\s
      line: '{{ item }} p+i+n+u+g+s+b+acl+xattrs+sha512'
      create: true
      mode: 420
    with_items: '{{ audit_tools }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"aide" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure AIDE to Verify the Audit Tools - Ensure Existing AIDE Configuration
      for Audit Tools are Correct'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure AIDE to Verify the Audit Tools - Ensure Existing AIDE Configuration for Audit Tools are
        Correct'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Configure AIDE to Verify the Audit Tools - Configure AIDE to Properly Protect Audit Tools
    ansible.builtin.lineinfile:
      path: /etc/aide/aide.conf
      line: '{{ item }} p+i+n+u+g+s+b+acl+xattrs+sha512'
      create: true
      mode: 420
    with_items: '{{ audit_tools }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"aide" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-651030
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure AIDE to Verify the Audit Tools - Configure AIDE to Properly Protect
      Audit Tools'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure AIDE to Verify the Audit Tools - Configure AIDE to Properly Protect Audit Tools'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Configure Periodic Execution of AIDE - Setup Cron Tab
    ansible.builtin.cron:
      name: run AIDE check
      minute: 5
      hour: 4
      user: root
      job: /usr/bin/aide --check
    register: crontab_check
    when:
    - '"linux-base" in ansible_facts.packages'
    - '''cron'' in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - DISA-STIG-UBTU-22-651025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure Periodic Execution of AIDE - Setup Cron Tab'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Periodic Execution of AIDE - Setup Cron Tab'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_aide_periodic_cron_checking | default(true) | bool)
- block:
  - name: Ensure use_pty is enabled in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      regexp: ^[\s]*Defaults.*\buse_pty\b.*$
      line: Defaults use_pty
      validate: /usr/sbin/visudo -cf %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_add_use_pty
  rescue:
  - name: 'StigForge: continue after error in ''Ensure use_pty is enabled in /etc/sudoers'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure use_pty is enabled in /etc/sudoers'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sudo_add_use_pty | default(true) | bool)
- block:
  - name: Ensure logfile is enabled with the appropriate value in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      regexp: ^[\s]*Defaults\s(.*)\blogfile=[-]?.+\b(.*)$
      line: Defaults \1logfile={{ var_sudo_logfile }}\2
      validate: /usr/sbin/visudo -cf %s
      backrefs: true
    register: edit_sudoers_logfile_option
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_custom_logfile
  rescue:
  - name: 'StigForge: continue after error in ''Ensure logfile is enabled with the appropriate value in /etc/sudoers'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure logfile is enabled with the appropriate value in /etc/sudoers'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sudo_custom_logfile | default(true) | bool)
- block:
  - name: Enable logfile option with appropriate value in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      line: Defaults logfile={{ var_sudo_logfile }}
      validate: /usr/sbin/visudo -cf %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    - edit_sudoers_logfile_option is defined and not edit_sudoers_logfile_option.changed
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_custom_logfile
  rescue:
  - name: 'StigForge: continue after error in ''Enable logfile option with appropriate value in /etc/sudoers'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable logfile option with appropriate value in /etc/sudoers'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sudo_custom_logfile | default(true) | bool)
- block:
  - name: Find /etc/sudoers.d/ files
    ansible.builtin.find:
      paths:
      - /etc/sudoers.d/
    register: sudoers
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-432010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-11
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_remove_no_authenticate
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/sudoers.d/ files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/sudoers.d/ files'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sudo_remove_no_authenticate | default(true) | bool)
- block:
  - name: Remove lines containing !authenticate from sudoers files
    ansible.builtin.replace:
      regexp: (^(?!#).*[\s]+\!authenticate.*$)
      replace: '# \g<1>'
      path: '{{ item.path }}'
      validate: /usr/sbin/visudo -cf %s
    with_items:
    - path: /etc/sudoers
    - '{{ sudoers.files }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-432010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-11
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_remove_no_authenticate
  rescue:
  - name: 'StigForge: continue after error in ''Remove lines containing !authenticate from sudoers files'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove lines containing !authenticate from sudoers files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sudo_remove_no_authenticate | default(true) | bool)
- block:
  - name: Ensure Local Login Warning Banner Is Configured Properly - Copy using inline content
    ansible.builtin.copy:
      content: '{{ cis_banner_text }}'
      dest: /etc/issue
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - banner_etc_issue_cis
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Local Login Warning Banner Is Configured Properly - Copy using inline
      content'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Local Login Warning Banner Is Configured Properly - Copy using inline content'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_banner_etc_issue_cis | default(true) | bool)
- block:
  - name: Ensure Remote Login Warning Banner Is Configured Properly - Copy using inline content
    ansible.builtin.copy:
      content: '{{ cis_banner_text }}'
      dest: /etc/issue.net
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - banner_etc_issue_net_cis
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Remote Login Warning Banner Is Configured Properly - Copy using inline
      content'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Remote Login Warning Banner Is Configured Properly - Copy using inline content'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_banner_etc_issue_net_cis | default(true) | bool)
- block:
  - name: Ensure Message Of The Day Is Configured Properly - Copy using inline content
    ansible.builtin.copy:
      content: '{{ cis_banner_text }}'
      dest: /etc/motd
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - banner_etc_motd_cis
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Message Of The Day Is Configured Properly - Copy using inline content''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Message Of The Day Is Configured Properly - Copy using inline content'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_banner_etc_motd_cis | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_issue_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_issue_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_issue_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_issue_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_issue | default(true) | bool)
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_issue | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      follow: false
      group: '{{ file_groupowner_etc_issue_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_issue | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_issue_net_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_issue_net_newgroup: '0'
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_issue_net_newgroup variable if represented by
      gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_issue_net_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_issue_net | default(true) |
    bool)
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_issue_net | default(true) |
    bool)
- block:
  - name: Ensure group owner on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      follow: false
      group: '{{ file_groupowner_etc_issue_net_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_issue_net | default(true) |
    bool)
- block:
  - name: Set the file_groupowner_etc_motd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_motd_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_motd_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_motd_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_motd | default(true) | bool)
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_motd | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      follow: false
      group: '{{ file_groupowner_etc_motd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_motd | default(true) | bool)
- block:
  - name: Set the file_owner_etc_issue_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_issue_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_issue_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_issue_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_issue | default(true) | bool)
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_issue | default(true) | bool)
- block:
  - name: Ensure owner on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      follow: false
      owner: '{{ file_owner_etc_issue_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/issue'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_issue | default(true) | bool)
- block:
  - name: Set the file_owner_etc_issue_net_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_issue_net_newown: '0'
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_issue_net_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_issue_net_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_issue_net | default(true) | bool)
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_issue_net | default(true) | bool)
- block:
  - name: Ensure owner on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      follow: false
      owner: '{{ file_owner_etc_issue_net_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_issue_net | default(true) | bool)
- block:
  - name: Set the file_owner_etc_motd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_motd_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_motd_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_motd_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_motd | default(true) | bool)
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_motd | default(true) | bool)
- block:
  - name: Ensure owner on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      follow: false
      owner: '{{ file_owner_etc_motd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/motd'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_motd | default(true) | bool)
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_issue | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_issue | default(true) | bool)
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_permissions_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_issue_net | default(true) |
    bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_permissions_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_issue_net | default(true) |
    bool)
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_motd | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/motd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_motd | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611020
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_dcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Check
      if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Check if system relies on
        pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_dcredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Remediation where pam-auth-update tool is
      present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611020
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_dcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Remediation
      where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_dcredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure PAM variable dcredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*dcredit
      line: dcredit = {{ var_password_pam_dcredit }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611020
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_dcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure
      PAM variable dcredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Digit Characters - Ensure PAM variable dcredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_dcredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary Words - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611030
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_dictcheck
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary
      Words - Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary Words - Check if system
        relies on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_dictcheck | default(true)
    | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary Words - Remediation where pam-auth-update
      tool is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary Words - Ensure pam-auth-update profile
        changes are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611030
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_dictcheck
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary
      Words - Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary Words - Remediation where
        pam-auth-update tool is present'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_dictcheck | default(true)
    | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary Words - Ensure PAM variable dictcheck
      is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*dictcheck
      line: dictcheck = {{ var_password_pam_dictcheck }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611030
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_dictcheck
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary
      Words - Ensure PAM variable dictcheck is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Prevent the Use of Dictionary Words - Ensure PAM variable
        dictcheck is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_dictcheck | default(true)
    | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611040
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(b)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_difok
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Characters -
      Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Characters - Check if system relies
        on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_difok | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Characters - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Different Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611040
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(b)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_difok
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Characters -
      Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_difok | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Characters - Ensure PAM variable difok is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*difok
      line: difok = {{ var_password_pam_difok }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611040
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(b)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_difok
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Characters -
      Ensure PAM variable difok is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Characters - Ensure PAM variable difok
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_difok | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Enforcing
    ansible.builtin.lineinfile:
      path: /etc/security/pwquality.conf
      create: true
      regexp: ''
      line: enforcing = 1
      state: present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611045
    - accounts_password_pam_enforcing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Enforcing'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Enforcing'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_enforcing | default(true)
    | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611015
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_lcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters -
      Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Check if system relies
        on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_lcredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611015
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_lcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters -
      Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_lcredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Ensure PAM variable lcredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*lcredit
      line: lcredit = {{ var_password_pam_lcredit }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611015
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_lcredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters -
      Ensure PAM variable lcredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters - Ensure PAM variable lcredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_lcredit | default(true) |
    bool)
- block:
  - name: Set Password Maximum Consecutive Repeating Characters - Check if system relies on pam-auth-update tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_maxrepeat
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Password Maximum Consecutive Repeating Characters - Check if system relies
      on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Password Maximum Consecutive Repeating Characters - Check if system relies on pam-auth-update
        tool'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_maxrepeat | default(true)
    | bool)
- block:
  - name: Set Password Maximum Consecutive Repeating Characters - Remediation where pam-auth-update tool is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Set Password Maximum Consecutive Repeating Characters - Ensure pam-auth-update profile changes are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_maxrepeat
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Password Maximum Consecutive Repeating Characters - Remediation where
      pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Password Maximum Consecutive Repeating Characters - Remediation where pam-auth-update tool is
        present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_maxrepeat | default(true)
    | bool)
- block:
  - name: Set Password Maximum Consecutive Repeating Characters - Ensure PAM variable maxrepeat is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*maxrepeat
      line: maxrepeat = {{ var_password_pam_maxrepeat }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_maxrepeat
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Password Maximum Consecutive Repeating Characters - Ensure PAM variable
      maxrepeat is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Password Maximum Consecutive Repeating Characters - Ensure PAM variable maxrepeat is set accordingly''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_maxrepeat | default(true)
    | bool)
- block:
  - name: Limit the maximum number of sequential characters in passwords - Check if system relies on pam-auth-update tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - accounts_password_pam_maxsequence
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Limit the maximum number of sequential characters in passwords - Check if
      system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit the maximum number of sequential characters in passwords - Check if system relies on pam-auth-update
        tool'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_maxsequence | default(true)
    | bool)
- block:
  - name: Limit the maximum number of sequential characters in passwords - Remediation where pam-auth-update tool is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Limit the maximum number of sequential characters in passwords - Ensure pam-auth-update profile changes are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - accounts_password_pam_maxsequence
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Limit the maximum number of sequential characters in passwords - Remediation
      where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit the maximum number of sequential characters in passwords - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_maxsequence | default(true)
    | bool)
- block:
  - name: Limit the maximum number of sequential characters in passwords - Ensure PAM variable maxsequence is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*maxsequence
      line: maxsequence = {{ var_password_pam_maxsequence }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - accounts_password_pam_maxsequence
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Limit the maximum number of sequential characters in passwords - Ensure PAM
      variable maxsequence is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit the maximum number of sequential characters in passwords - Ensure PAM variable maxsequence
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_maxsequence | default(true)
    | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_minclass
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Categories -
      Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Categories - Check if system relies
        on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_minclass | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_minclass
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Categories -
      Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Categories - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_minclass | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure PAM variable minclass is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*minclass
      line: minclass = {{ var_password_pam_minclass }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_minclass
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Categories -
      Ensure PAM variable minclass is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure PAM variable minclass
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_minclass | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Length - Check if system relies on pam-auth-update tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_minlen
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Length - Check if system
      relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Length - Check if system relies on pam-auth-update
        tool'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_minlen | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Length - Remediation where pam-auth-update tool is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Length - Ensure pam-auth-update profile changes are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_minlen
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Length - Remediation where
      pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Length - Remediation where pam-auth-update tool
        is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_minlen | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable minlen is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*minlen
      line: minlen = {{ var_password_pam_minlen }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - DISA-STIG-UBTU-22-611035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_minlen
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable
      minlen is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable minlen is set accordingly''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_minlen | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_ocredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Check
      if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Check if system relies on
        pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_ocredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_ocredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Remediation
      where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_ocredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure PAM variable ocredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*ocredit
      line: ocredit = {{ var_password_pam_ocredit }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611025
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_ocredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure
      PAM variable ocredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Special Characters - Ensure PAM variable ocredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_ocredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Check if system relies on pam-auth-update
      tool
    ansible.builtin.stat:
      path: /usr/sbin/pam-auth-update
    register: result_pam_auth_update_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - accounts_password_pam_ucredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters -
      Check if system relies on pam-auth-update tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Check if system relies
        on pam-auth-update tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_ucredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Remediation where pam-auth-update tool
      is present
    block:
    - name: Check if /usr/share/pam-configs/cac_pwquality exists
      ansible.builtin.stat:
        path: /usr/share/pam-configs/cac_pwquality
      register: pwquality_file_stat
    - name: Put the content into /usr/share/pam-configs/cac_pwquality if it does not exist
      ansible.builtin.copy:
        dest: /usr/share/pam-configs/cac_pwquality
        content: "Name: Pwquality password strength checking\nDefault: yes\nPriority: 1024\nConflicts: cracklib\nPassword-Type:\
          \ Primary\nPassword:\n  requisite           pam_pwquality.so retry=3\n"
        force: true
        mode: 420
      when: not (pwquality_file_stat is not skipped and pwquality_file_stat.stat.exists)
    - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Ensure pam-auth-update profile changes
        are applied
      ansible.builtin.command:
        cmd: pam-auth-update --enable cac_pwquality
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    - (result_pam_auth_update_present is not skipped and result_pam_auth_update_present.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-611010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - accounts_password_pam_ucredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters -
      Remediation where pam-auth-update tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Remediation where pam-auth-update
        tool is present'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_ucredit | default(true) |
    bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Ensure PAM variable ucredit is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*ucredit
      line: ucredit = {{ var_password_pam_ucredit }}
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpwquality1" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611010
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - accounts_password_pam_ucredit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters -
      Ensure PAM variable ucredit is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Uppercase Characters - Ensure PAM variable ucredit
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_ucredit | default(true) |
    bool)
- block:
  - name: Set PAM Password Hashing Algorithm - system-auth - Check if /etc/pam.d/system-auth file is present
    ansible.builtin.stat:
      path: /etc/pam.d/system-auth
    register: result_pam_auth_file_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in ''Set PAM Password Hashing Algorithm - system-auth - Check if /etc/pam.d/system-auth
      file is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set PAM Password Hashing Algorithm - system-auth - Check if /etc/pam.d/system-auth file is present''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Set PAM Password Hashing Algorithm - system-auth - Check the proper remediation for the system
    block:
    - name: Set PAM Password Hashing Algorithm - system-auth - Define the PAM file to be edited as a local fact
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/system-auth
    - name: Set PAM Password Hashing Algorithm - system-auth - Check if system relies on authselect tool
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect custom profile is used if authselect is present
      block:
      - name: Set PAM Password Hashing Algorithm - system-auth - Check integrity of authselect current profile
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: Set PAM Password Hashing Algorithm - system-auth - Informative message based on the authselect integrity check
          result
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: Set PAM Password Hashing Algorithm - system-auth - Get authselect current profile
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: Set PAM Password Hashing Algorithm - system-auth - Define the current authselect profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Define the new authselect custom profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Get authselect current features to also enable them in the
          custom profile
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Check if any custom profile with the same name was already
          created
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Create an authselect custom profile based on the current
          profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM Password Hashing Algorithm - system-auth - Create an authselect custom profile based on sssd profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure the authselect custom profile is selected
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM Password Hashing Algorithm - system-auth - Restore the authselect features in the custom profile
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: Set PAM Password Hashing Algorithm - system-auth - Change the PAM file to be edited according to the custom
          authselect profile
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: Set PAM Password Hashing Algorithm - system-auth - Define a fact for control already filtered in case filters
        are used
      ansible.builtin.set_fact:
        pam_module_control: sufficient
    - name: Set PAM Password Hashing Algorithm - system-auth - Check if expected PAM module line is present in {{ pam_file_path
        }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so\s*.*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_present
    - name: Set PAM Password Hashing Algorithm - system-auth - Include or update the PAM module line in {{ pam_file_path }}
      block:
      - name: Set PAM Password Hashing Algorithm - system-auth - Check if required PAM module line is present in {{ pam_file_path
          }} with different control
        ansible.builtin.lineinfile:
          path: '{{ pam_file_path }}'
          regexp: ^\s*password\s+.*\s+pam_unix.so\s*
          state: absent
        check_mode: true
        changed_when: false
        register: result_pam_line_other_control_present
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure the correct control for the required PAM module line
          in {{ pam_file_path }}
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: ^(\s*password\s+).*(\bpam_unix.so.*)
          replace: \1{{ pam_module_control }} \2
        register: result_pam_module_edit
        when:
        - result_pam_line_other_control_present.found == 1
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure the required PAM module line is included in {{ pam_file_path
          }}
        ansible.builtin.lineinfile:
          dest: '{{ pam_file_path }}'
          line: password    {{ pam_module_control }}    pam_unix.so
        register: result_pam_module_add
        when:
        - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - result_authselect_present is defined
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
          \ result_pam_module_edit.changed)"
      when:
      - result_pam_line_present.found is defined
      - result_pam_line_present.found == 0
    - name: Set PAM Password Hashing Algorithm - system-auth - Define a fact for control already filtered in case filters
        are used
      ansible.builtin.set_fact:
        pam_module_control: sufficient
    - name: Set PAM Password Hashing Algorithm - system-auth - Check if the required PAM module option is present in {{ pam_file_path
        }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so\s*.*\s{{ var_password_hashing_algorithm_pam.split("|")[0]
          }}\b
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_module_set_password_hashing_algorithm_systemauth_option_present
    - name: Set PAM Password Hashing Algorithm - system-auth - Ensure the "{{ var_password_hashing_algorithm_pam.split("|")[0]
        }}" PAM option for "pam_unix.so" is included in {{ pam_file_path }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        backrefs: true
        regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so.*)
        line: \1 {{ var_password_hashing_algorithm_pam.split("|")[0] }}
        state: present
      register: result_pam_set_password_hashing_algorithm_systemauth_add
      when:
      - result_pam_module_set_password_hashing_algorithm_systemauth_option_present.found is defined
      - result_pam_module_set_password_hashing_algorithm_systemauth_option_present.found == 0
    - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - "(result_pam_set_password_hashing_algorithm_systemauth_add is defined and result_pam_set_password_hashing_algorithm_systemauth_add.changed)\n\
        \ or (result_pam_set_password_hashing_algorithm_systemauth_edit is defined and result_pam_set_password_hashing_algorithm_systemauth_edit.changed)"
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    - (result_pam_auth_file_present is not skipped and result_pam_auth_file_present.stat.exists)
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in ''Set PAM Password Hashing Algorithm - system-auth - Check the proper remediation
      for the system'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set PAM Password Hashing Algorithm - system-auth - Check the proper remediation for the system''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Set PAM Password Hashing Algorithm - system-auth - Check if /etc/pam.d/system-auth File is Present
    ansible.builtin.stat:
      path: /etc/pam.d/system-auth
    register: result_pam_auth_file_present
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in ''Set PAM Password Hashing Algorithm - system-auth - Check if /etc/pam.d/system-auth
      File is Present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set PAM Password Hashing Algorithm - system-auth - Check if /etc/pam.d/system-auth File is Present''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Set PAM Password Hashing Algorithm - system-auth - Check The Proper Remediation For The System
    block:
    - name: Set PAM Password Hashing Algorithm - system-auth - Define the PAM file to be edited as a local fact
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/system-auth
    - name: Set PAM Password Hashing Algorithm - system-auth - Check if system relies on authselect tool
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect custom profile is used if authselect is present
      block:
      - name: Set PAM Password Hashing Algorithm - system-auth - Check integrity of authselect current profile
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: Set PAM Password Hashing Algorithm - system-auth - Informative message based on the authselect integrity check
          result
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: Set PAM Password Hashing Algorithm - system-auth - Get authselect current profile
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: Set PAM Password Hashing Algorithm - system-auth - Define the current authselect profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Define the new authselect custom profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Get authselect current features to also enable them in the
          custom profile
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Check if any custom profile with the same name was already
          created
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM Password Hashing Algorithm - system-auth - Create an authselect custom profile based on the current
          profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM Password Hashing Algorithm - system-auth - Create an authselect custom profile based on sssd profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure the authselect custom profile is selected
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM Password Hashing Algorithm - system-auth - Restore the authselect features in the custom profile
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: Set PAM Password Hashing Algorithm - system-auth - Change the PAM file to be edited according to the custom
          authselect profile
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: Set PAM Password Hashing Algorithm - system-auth - Check if "{{ pam_file_path }}" File is Present
      ansible.builtin.stat:
        path: '{{ pam_file_path }}'
      register: pam_file_path_present
    - name: Set PAM Password Hashing Algorithm - system-auth - Ensure That Only the Correct Hashing Algorithm Option For pam_unix.so
        Is Used in {{ pam_file_path }}
      ansible.builtin.replace:
        dest: '{{ pam_file_path }}'
        regexp: (^\s*password.*pam_unix\.so.*)\b{{ item }}\b\s*(.*)
        replace: \1\2
      when:
      - item != var_password_hashing_algorithm_pam.split('|')[0]
      - (pam_file_path_present is not skipped and pam_file_path_present.stat.exists)
      loop:
      - sha512
      - yescrypt
      - gost_yescrypt
      - blowfish
      - sha256
      - md5
      - bigcrypt
      register: result_pam_hashing_options_removal
    - name: Set PAM Password Hashing Algorithm - system-auth - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - result_pam_hashing_options_removal is changed
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    - (result_pam_auth_file_present is not skipped and result_pam_auth_file_present.stat.exists)
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in ''Set PAM Password Hashing Algorithm - system-auth - Check The Proper Remediation
      For The System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set PAM Password Hashing Algorithm - system-auth - Check The Proper Remediation For The System''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Ensure interactive local users are the owners of their respective initialization files
    ansible.builtin.lineinfile:
      dest: /etc/group
      backrefs: true
      regexp: (^shadow:[^:]*:[^:]*:)([^:]+$)
      line: \1
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - ensure_shadow_group_empty
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the owners of their respective initialization
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the owners of their respective initialization files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_ensure_shadow_group_empty | default(true) | bool)
- block:
  - name: Collect users with not correct minimum time period between password changes
    ansible.builtin.command: 'awk -F'':'' ''(/^[^:]+:[^!*]/ && ($4 < {{ var_accounts_minimum_age_login_defs }} || $4 == ""))
      {print $1}'' /etc/shadow

      '
    register: user_names
    changed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - accounts_password_set_min_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with not correct minimum time period between password changes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with not correct minimum time period between password changes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_set_min_life_existing | default(true)
    | bool)
- block:
  - name: Change the minimum time period between password changes
    ansible.builtin.command: 'chage -m {{ var_accounts_minimum_age_login_defs }} {{ item }}

      '
    with_items: '{{ user_names.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - user_names.stdout_lines | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - accounts_password_set_min_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Change the minimum time period between password changes'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Change the minimum time period between password changes'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_set_min_life_existing | default(true)
    | bool)
- block:
  - name: Collect users with not correct INACTIVE parameter set
    ansible.builtin.command:
      cmd: awk -F':' '(($7 > {{ var_account_disable_post_pw_expiration }} || $7 == "") && $2 ~ /^\$/) {print $1}' /etc/shadow
    register: user_names
    changed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.5.6
    - NIST-800-53-AC-2(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-4(e)
    - PCI-DSS-Req-8.1.4
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.6
    - accounts_set_post_pw_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with not correct INACTIVE parameter set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with not correct INACTIVE parameter set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_set_post_pw_existing | default(true) |
    bool)
- block:
  - name: Change the period of inactivity
    ansible.builtin.command:
      cmd: chage --inactive {{ var_account_disable_post_pw_expiration }} {{ item }}
    with_items: '{{ user_names.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - user_names is not skipped and user_names.stdout_lines | length > 0
    tags:
    - NIST-800-171-3.5.6
    - NIST-800-53-AC-2(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-4(e)
    - PCI-DSS-Req-8.1.4
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.6
    - accounts_set_post_pw_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Change the period of inactivity'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Change the period of inactivity'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_set_post_pw_existing | default(true) |
    bool)
- block:
  - name: Avoid using remember in pam_unix module - Remove remember option from pam_unix.so
    ansible.builtin.replace:
      dest: '{{ item }}'
      regexp: (\s+pam_unix\.so.*)\s+remember=\d+\b(.*)
      replace: \1\2
    loop:
    - /etc/pam.d/common-password
    - /etc/pam.d/common-auth
    - /etc/pam.d/common-account
    - /etc/pam.d/common-session
    - /etc/pam.d/common-session-noninteractive
    when: '"libpam-runtime" in ansible_facts.packages'
    tags:
    - accounts_password_pam_unix_no_remember
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Avoid using remember in pam_unix module - Remove remember option from pam_unix.so''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Avoid using remember in pam_unix module - Remove remember option from pam_unix.so'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_password_pam_unix_no_remember | default(true)
    | bool)
- block:
  - name: Collect users with no password
    ansible.builtin.command: 'awk -F: ''!$2 {print $1}'' /etc/shadow

      '
    register: users_nopasswd
    changed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-611065
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_no_empty_passwords_etc_shadow | default(true) |
    bool)
- block:
  - name: Lock users with no password
    ansible.builtin.command: 'passwd -l {{ item }}

      '
    with_items: '{{ users_nopasswd.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - users_nopasswd is not skipped and users_nopasswd.stdout_lines | length > 0
    tags:
    - DISA-STIG-UBTU-22-611065
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_no_empty_passwords_etc_shadow | default(true) |
    bool)
- block:
  - name: Get all /etc/passwd file entries
    ansible.builtin.getent:
      database: passwd
      split: ':'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all /etc/passwd file entries'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all /etc/passwd file entries'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_no_uid_except_zero | default(true) | bool)
- block:
  - name: Lock the password of the user accounts other than root with uid 0
    ansible.builtin.command: passwd -l {{ item.key }}
    loop: '{{ getent_passwd | dict2items | rejectattr(''key'', ''equalto'', ''root'') | list }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.value.1  == '0'
    tags:
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock the password of the user accounts other than root with uid 0'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock the password of the user accounts other than root with uid 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_no_uid_except_zero | default(true) | bool)
- block:
  - name: Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
      }} Group Exists
    ansible.builtin.group:
      name: '{{ var_pam_wheel_group_for_su }}'
      state: present
    when: ( "libpam-runtime" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - ensure_pam_wheel_group_empty
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty
      - Ensure {{ var_pam_wheel_group_for_su }} Group Exists'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
        }} Group Exists'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_ensure_pam_wheel_group_empty | default(true) | bool)
- block:
  - name: Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
      }} Group is Empty
    ansible.builtin.lineinfile:
      path: /etc/group
      regexp: ^({{ var_pam_wheel_group_for_su }}:[^:]+:[0-9]+:).*$
      line: \1
      backrefs: true
    when: ( "libpam-runtime" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - ensure_pam_wheel_group_empty
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty
      - Ensure {{ var_pam_wheel_group_for_su }} Group is Empty'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty - Ensure {{ var_pam_wheel_group_for_su
        }} Group is Empty'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_ensure_pam_wheel_group_empty | default(true) | bool)
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local Users From /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local
      Users From /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local Users From /etc/passwd''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users Variable From getent_passwd Facts
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd | dict2items }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users
      Variable From getent_passwd Facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users Variable From getent_passwd
        Facts'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login Shell for System Accounts
    ansible.builtin.user:
      name: '{{ item.key }}'
      shell: /sbin/nologin
    loop: '{{ local_users }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.key not in ['root']
    - item.value[1]|int < 1000
    - item.value[5] not in ['/sbin/shutdown', '/sbin/halt', '/bin/sync']
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login
      Shell for System Accounts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login Shell for System Accounts''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Correct any occurrence of TMOUT in /etc/profile
    ansible.builtin.replace:
      path: /etc/profile
      regexp: ^[^#].*TMOUT=.*
      replace: typeset -xr TMOUT={{ var_accounts_tmout }}
    register: profile_replaced
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-412030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSSv4-8.6
    - PCI-DSSv4-8.6.1
    - accounts_tmout
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Correct any occurrence of TMOUT in /etc/profile'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Correct any occurrence of TMOUT in /etc/profile'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_tmout | default(true) | bool)
- block:
  - name: Set Interactive Session Timeout
    ansible.builtin.lineinfile:
      path: /etc/profile.d/tmout.sh
      create: true
      regexp: TMOUT=
      mode: '0644'
      line: typeset -xr TMOUT={{ var_accounts_tmout }}
      state: present
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-412030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSSv4-8.6
    - PCI-DSSv4-8.6.1
    - accounts_tmout
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Interactive Session Timeout'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Interactive Session Timeout'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_tmout | default(true) | bool)
- block:
  - name: User Initialization Files Must Be Group-Owned By The Primary Group - Get interactive users from passwd file
    ansible.builtin.getent:
      database: passwd
    register: passwd_entries
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - accounts_user_dot_group_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Group-Owned By The Primary Group - Get interactive
      users from passwd file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Group-Owned By The Primary Group - Get interactive users from passwd
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_group_ownership | default(true)
    | bool)
- block:
  - name: User Initialization Files Must Be Group-Owned By The Primary Group - Create list of interactive users with GID and
      home directory
    ansible.builtin.set_fact:
      interactive_users: '{{ interactive_users | default([]) + [{''home'': item.value[4], ''gid'': item.value[2]}] }}'
    loop: '{{ passwd_entries.ansible_facts.getent_passwd | dict2items }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.value[2] | int >= 1000 | int
    - item.value[2] | int != 65534 | int
    - item.value[4] != ""
    tags:
    - accounts_user_dot_group_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Group-Owned By The Primary Group - Create
      list of interactive users with GID and home directory'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Group-Owned By The Primary Group - Create list of interactive users
        with GID and home directory'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_group_ownership | default(true)
    | bool)
- block:
  - name: User Initialization Files Must Be Group-Owned By The Primary Group - Find dot files in interactive user home directories
    ansible.builtin.find:
      paths: '{{ item.home }}'
      patterns: .*
      file_type: file
      hidden: true
      depth: 1
      follow: false
    register: user_dotfiles
    loop: '{{ interactive_users | default([]) }}'
    failed_when: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.home != ""
    tags:
    - accounts_user_dot_group_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Group-Owned By The Primary Group - Find
      dot files in interactive user home directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Group-Owned By The Primary Group - Find dot files in interactive
        user home directories'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_group_ownership | default(true)
    | bool)
- block:
  - name: User Initialization Files Must Be Group-Owned By The Primary Group - Set correct group ownership for user initialization
      files
    ansible.builtin.file:
      path: '{{ item.1.path }}'
      group: '{{ item.0.item.gid }}'
      follow: false
    loop: '{{ user_dotfiles.results | subelements(''files'', skip_missing=True) }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.0 is not skipped
    - item.1.path is defined
    tags:
    - accounts_user_dot_group_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Group-Owned By The Primary Group - Set correct
      group ownership for user initialization files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Group-Owned By The Primary Group - Set correct group ownership
        for user initialization files'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_group_ownership | default(true)
    | bool)
- block:
  - name: User Initialization Files Must Be Owned By the Primary User - Get interactive users from passwd file
    ansible.builtin.getent:
      database: passwd
    register: passwd_entries
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - accounts_user_dot_user_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Owned By the Primary User - Get interactive
      users from passwd file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Owned By the Primary User - Get interactive users from passwd file''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_user_ownership | default(true)
    | bool)
- block:
  - name: User Initialization Files Must Be Owned By the Primary User - Create list of interactive users with UID and home
      directory
    ansible.builtin.set_fact:
      interactive_users: '{{ interactive_users | default([]) + [{''uid'': item.value[1], ''home'': item.value[4], ''username'':
        item.key}] }}'
    loop: '{{ passwd_entries.ansible_facts.getent_passwd | dict2items }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.value[1] | int >= 1000 | int
    - item.value[1] | int != 65534 | int
    - item.value[4] != ""
    tags:
    - accounts_user_dot_user_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Owned By the Primary User - Create list
      of interactive users with UID and home directory'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Owned By the Primary User - Create list of interactive users with
        UID and home directory'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_user_ownership | default(true)
    | bool)
- block:
  - name: User Initialization Files Must Be Owned By the Primary User - Find dot files in interactive user home directories
    ansible.builtin.find:
      paths: '{{ item.home }}'
      patterns: .*
      file_type: file
      hidden: true
      depth: 1
      follow: false
    register: user_dotfiles
    loop: '{{ interactive_users | default([]) }}'
    failed_when: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.home != ""
    tags:
    - accounts_user_dot_user_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Owned By the Primary User - Find dot files
      in interactive user home directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Owned By the Primary User - Find dot files in interactive user
        home directories'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_user_ownership | default(true)
    | bool)
- block:
  - name: User Initialization Files Must Be Owned By the Primary User - Set correct ownership for user initialization files
    ansible.builtin.file:
      path: '{{ item.1.path }}'
      owner: '{{ item.0.item.username }}'
      follow: false
    loop: '{{ user_dotfiles.results | subelements(''files'', skip_missing=True) }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.0 is not skipped
    - item.0 is not failed
    - item.0.item is defined
    - item.0.item.username is defined
    - item.1.path is defined
    tags:
    - accounts_user_dot_user_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''User Initialization Files Must Be Owned By the Primary User - Set correct
      ownership for user initialization files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''User Initialization Files Must Be Owned By the Primary User - Set correct ownership for user initialization
        files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_dot_user_ownership | default(true)
    | bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_interactive_home_directory_exists
    | default(true) | bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_interactive_home_directory_exists
    | default(true) | bool)
- block:
  - name: Ensure interactive users have a home directory exists
    ansible.builtin.user:
      name: '{{ item.key }}'
      create_home: true
    loop: '{{ local_users }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive users have a home directory exists'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive users have a home directory exists'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_user_interactive_home_directory_exists
    | default(true) | bool)
- block:
  - name: Get all users from getent to support remote users (LDAP, SSSD, etc.)
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all users from getent to support remote users (LDAP, SSSD, etc.)'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all users from getent to support remote users (LDAP, SSSD, etc.)'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Create interactive_users variable filtering out non-interactive shells
    ansible.builtin.set_fact:
      interactive_users: '{{ ansible_facts.getent_passwd|dict2items|rejectattr("value.5", "match", ".*/sbin/nologin$")|rejectattr("value.5",
        "match", ".*/usr/sbin/nologin$")|rejectattr("value.5", "match", ".*/bin/false$")|rejectattr("value.5", "match", ".*/usr/bin/false$")|list
        }}'
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create interactive_users variable filtering out non-interactive shells'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create interactive_users variable filtering out non-interactive shells'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Test for existence of home directories to avoid creating them, but only fixing group ownership
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ interactive_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    - item.value[4] != "/"
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence of home directories to avoid creating them, but only fixing
      group ownership'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence of home directories to avoid creating them, but only fixing group ownership''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Ensure interactive users are the group-owners of their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      group: '{{ item.0.value[2] }}'
    loop: '{{ interactive_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive users are the group-owners of their respective home directories''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive users are the group-owners of their respective home directories'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_home_directories | default(true)
    | bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_home_directories | default(true)
    | bool)
- block:
  - name: Test for existence of home directories to avoid creating them, but only fixing ownership
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    - item.value[4] != "/"
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence of home directories to avoid creating them, but only fixing
      ownership'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence of home directories to avoid creating them, but only fixing ownership'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_home_directories | default(true)
    | bool)
- block:
  - name: Ensure interactive local users are the owners of their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      owner: '{{ item.0.value[1] }}'
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the owners of their respective home directories''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the owners of their respective home directories'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_home_directories | default(true)
    | bool)
- block:
  - name: Ensure User Bash History File Has Correct Permissions - Gather User Info
    ansible.builtin.getent:
      database: passwd
    when: '"bash" in ansible_facts.packages'
    tags:
    - file_permission_user_bash_history
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure User Bash History File Has Correct Permissions - Gather User Info''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure User Bash History File Has Correct Permissions - Gather User Info'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permission_user_bash_history | default(true)
    | bool)
- block:
  - name: Ensure User Bash History File Has Correct Permissions - Check Bash History Files Existence
    ansible.builtin.stat:
      path: '{{ item.value[4] }}/.bash_history'
    register: bash_history_files
    with_dict: '{{ ansible_facts.getent_passwd }}'
    when:
    - '"bash" in ansible_facts.packages'
    - item.value[4] != "/sbin/nologin"
    - item.key not in ["nobody", "nfsnobody"]
    - item.value[1] | int >= 1000
    tags:
    - file_permission_user_bash_history
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure User Bash History File Has Correct Permissions - Check Bash History
      Files Existence'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure User Bash History File Has Correct Permissions - Check Bash History Files Existence'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permission_user_bash_history | default(true)
    | bool)
- block:
  - name: Ensure User Bash History File Has Correct Permissions - Fix Bash History Files Permissions
    ansible.builtin.file:
      path: '{{ item.stat.path }}'
      mode: u-sx,go=
    with_items: '{{ bash_history_files.results }}'
    when:
    - '"bash" in ansible_facts.packages'
    - item.stat is defined
    - (item is not skipped and item.stat.exists)
    - not (item is not skipped and item.stat.islnk)
    tags:
    - file_permission_user_bash_history
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure User Bash History File Has Correct Permissions - Fix Bash History Files
      Permissions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure User Bash History File Has Correct Permissions - Fix Bash History Files Permissions'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permission_user_bash_history | default(true)
    | bool)
- block:
  - name: Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Gather User Info
    ansible.builtin.getent:
      database: passwd
    tags:
    - file_permission_user_init_files
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Gather
      User Info'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Gather User Info'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permission_user_init_files | default(true)
    | bool)
- block:
  - name: Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Find Init Files
    ansible.builtin.find:
      paths: '{{ item.value[4] }}'
      pattern: '{{ var_user_initialization_files_regex }}'
      hidden: true
      use_regex: true
    with_dict: '{{ ansible_facts.getent_passwd }}'
    when:
    - item.value[4] != "/sbin/nologin"
    - item.key not in ["nobody", "nfsnobody"]
    - item.value[1] | int >= 1000
    register: found_init_files
    tags:
    - file_permission_user_init_files
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Find
      Init Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Find Init Files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permission_user_init_files | default(true)
    | bool)
- block:
  - name: Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Fix Init Files Permissions
    ansible.builtin.file:
      path: '{{ item.1.path }}'
      mode: u-s,g-wxs,o=
    loop: '{{ q(''ansible.builtin.subelements'', found_init_files.results, ''files'', {''skip_missing'': True}) }}'
    tags:
    - file_permission_user_init_files
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Fix
      Init Files Permissions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Fix Init Files Permissions''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permission_user_init_files | default(true)
    | bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Test for existence home directories to avoid creating them.
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    - item.value[4] != "/"
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence home directories to avoid creating them.'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence home directories to avoid creating them.'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Ensure interactive local users have proper permissions on their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      mode: u-s,g-w-s,o=-
      follow: false
      recurse: false
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users have proper permissions on their respective
      home directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users have proper permissions on their respective home directories'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Check if umask in /etc/bash.bashrc is already set
    ansible.builtin.lineinfile:
      path: /etc/bash.bashrc
      regexp: ^[^#]*\bumask\s+\d+$
      state: absent
    check_mode: true
    changed_when: false
    register: umask_replace
    when: '"bash" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if umask in /etc/bash.bashrc is already set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if umask in /etc/bash.bashrc is already set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_bashrc | default(true) | bool)
- block:
  - name: Replace user umask in /etc/bash.bashrc
    ansible.builtin.replace:
      path: /etc/bash.bashrc
      regexp: ^([^#]*\b)umask\s+\d+$
      replace: \g<1>umask {{ var_accounts_user_umask }}
    when:
    - '"bash" in ansible_facts.packages'
    - umask_replace.found > 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Replace user umask in /etc/bash.bashrc'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Replace user umask in /etc/bash.bashrc'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_bashrc | default(true) | bool)
- block:
  - name: Ensure the Default umask is Appended Correctly
    ansible.builtin.lineinfile:
      create: true
      path: /etc/bash.bashrc
      line: umask {{ var_accounts_user_umask }}
      mode: 420
    when:
    - '"bash" in ansible_facts.packages'
    - umask_replace.found == 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default umask is Appended Correctly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default umask is Appended Correctly'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_bashrc | default(true) | bool)
- block:
  - name: Set default umask
    ansible.builtin.lineinfile:
      path: /etc/login.defs
      regexp: ^(\s*)UMASK\s+.*
      state: absent
    check_mode: true
    changed_when: false
    register: result_umask_is_set
    when: ( "login" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    tags:
    - DISA-STIG-UBTU-22-412035
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set default umask'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set default umask'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_login_defs | default(true) |
    bool)
- block:
  - name: Replace user UMASK in /etc/login.defs
    ansible.builtin.replace:
      path: /etc/login.defs
      regexp: ^(\s*)UMASK(\s+).*
      replace: \g<1>UMASK\g<2>{{ var_accounts_user_umask }}
    when:
    - ( "login" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - result_umask_is_set.found > 0
    tags:
    - DISA-STIG-UBTU-22-412035
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Replace user UMASK in /etc/login.defs'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Replace user UMASK in /etc/login.defs'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_login_defs | default(true) |
    bool)
- block:
  - name: Ensure the Default UMASK is Appended Correctly
    ansible.builtin.lineinfile:
      create: true
      path: /etc/login.defs
      line: UMASK {{ var_accounts_user_umask }}
      mode: 420
    when:
    - ( "login" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - result_umask_is_set.found == 0
    tags:
    - DISA-STIG-UBTU-22-412035
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default UMASK is Appended Correctly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default UMASK is Appended Correctly'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_login_defs | default(true) |
    bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile Configuration Files Where umask Is Defined
    ansible.builtin.find:
      paths:
      - /etc/profile.d
      patterns:
      - sh.local
      - '*.sh'
      contains: ^[\s]*umask\s+\d+
    register: result_profile_d_files
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile
      Configuration Files Where umask Is Defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile Configuration Files Where
        umask Is Defined'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing umask Value in Files From /etc/profile.d
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^(\s*)umask\s+\d+
      replace: \1umask {{ var_accounts_user_umask }}
    loop: '{{ result_profile_d_files.files }}'
    register: result_umask_replaced_profile_d
    when: result_profile_d_files.matched
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing
      umask Value in Files From /etc/profile.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing umask Value in Files
        From /etc/profile.d'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is Set in /etc/profile if Not Already Set
      Elsewhere
    ansible.builtin.lineinfile:
      create: true
      mode: 420
      path: /etc/profile
      line: umask {{ var_accounts_user_umask }}
    when: not result_profile_d_files.matched
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is
      Set in /etc/profile if Not Already Set Elsewhere'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is Set in /etc/profile if
        Not Already Set Elsewhere'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value For All Existing umask Definition
      in /etc/profile
    ansible.builtin.replace:
      path: /etc/profile
      regexp: ^(\s*)umask\s+\d+
      replace: \1umask {{ var_accounts_user_umask }}
    register: result_umask_replaced_profile
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value
      For All Existing umask Definition in /etc/profile'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value For All Existing umask
        Definition in /etc/profile'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Set the file_owner_grub2_cfg_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_grub2_cfg_newown: '0'
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_grub2_cfg_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_grub2_cfg_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_grub2_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub/grub.cfg
    ansible.builtin.stat:
      path: /boot/grub/grub.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_grub2_cfg | default(true) | bool)
- block:
  - name: Ensure owner on /boot/grub/grub.cfg
    ansible.builtin.file:
      path: /boot/grub/grub.cfg
      follow: false
      owner: '{{ file_owner_grub2_cfg_newown }}'
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /boot/grub/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /boot/grub/grub.cfg'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_grub2_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub/grub.cfg
    ansible.builtin.stat:
      path: /boot/grub/grub.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_grub2_cfg | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub/grub.cfg
    ansible.builtin.file:
      path: /boot/grub/grub.cfg
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - ( "grub2-common" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub/grub.cfg'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub/grub.cfg'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_grub2_cfg | default(true) | bool)
- block:
  - name: Ensure journald is configured to compress large log files - Search for a section in files
    ansible.builtin.find:
      paths: '{{item.path}}'
      patterns: '{{item.pattern}}'
      contains: ^\s*\[Journal\]
      read_whole_file: true
      use_regex: true
    register: systemd_dropin_files_with_section
    loop:
    - path: '{{ ''/etc/systemd/journald.conf'' | dirname }}'
      pattern: '{{ ''/etc/systemd/journald.conf'' | basename | regex_escape }}'
    - path: /etc/systemd/journald.conf.d
      pattern: .*\.conf
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - journald_compress
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to compress large log files - Search for a section
      in files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to compress large log files - Search for a section in files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_compress | default(true) | bool)
- block:
  - name: Ensure journald is configured to compress large log files - Count number of files which contain the correct section
    ansible.builtin.set_fact:
      count_of_systemd_dropin_files_with_section: '{{systemd_dropin_files_with_section.results | map(attribute=''matched'')
        | list | map(''int'') | sum}}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - journald_compress
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to compress large log files - Count number of
      files which contain the correct section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to compress large log files - Count number of files which contain the
        correct section'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_compress | default(true) | bool)
- block:
  - name: Ensure journald is configured to compress large log files - Add missing configuration to correct section
    community.general.ini_file:
      path: '{{item}}'
      section: Journal
      option: Compress
      value: 'yes'
      state: present
      no_extra_spaces: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int > 0
    loop: '{{systemd_dropin_files_with_section.results | sum(attribute=''files'', start=[]) | map(attribute=''path'') | list
      }}'
    tags:
    - journald_compress
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to compress large log files - Add missing configuration
      to correct section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to compress large log files - Add missing configuration to correct
        section'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_compress | default(true) | bool)
- block:
  - name: Ensure journald is configured to compress large log files - Add configuration to new remediation file
    community.general.ini_file:
      path: /etc/systemd/journald.conf.d/complianceascode_hardening.conf
      section: Journal
      option: Compress
      value: 'yes'
      state: present
      no_extra_spaces: true
      create: true
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int == 0
    tags:
    - journald_compress
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to compress large log files - Add configuration
      to new remediation file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to compress large log files - Add configuration to new remediation
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_compress | default(true) | bool)
- block:
  - name: Ensure journald ForwardToSyslog is disabled - Search for a section in files
    ansible.builtin.find:
      paths: '{{item.path}}'
      patterns: '{{item.pattern}}'
      contains: ^\s*\[Journal\]
      read_whole_file: true
      use_regex: true
    register: systemd_dropin_files_with_section
    loop:
    - path: '{{ ''/etc/systemd/journald.conf'' | dirname }}'
      pattern: '{{ ''/etc/systemd/journald.conf'' | basename | regex_escape }}'
    - path: /etc/systemd/journald.conf.d
      pattern: .*\.conf
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd" in ansible_facts.packages'
    tags:
    - journald_disable_forward_to_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald ForwardToSyslog is disabled - Search for a section in files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald ForwardToSyslog is disabled - Search for a section in files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_disable_forward_to_syslog | default(true)
    | bool)
- block:
  - name: Ensure journald ForwardToSyslog is disabled - Count number of files which contain the correct section
    ansible.builtin.set_fact:
      count_of_systemd_dropin_files_with_section: '{{systemd_dropin_files_with_section.results | map(attribute=''matched'')
        | list | map(''int'') | sum}}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd" in ansible_facts.packages'
    tags:
    - journald_disable_forward_to_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald ForwardToSyslog is disabled - Count number of files which
      contain the correct section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald ForwardToSyslog is disabled - Count number of files which contain the correct section''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_disable_forward_to_syslog | default(true)
    | bool)
- block:
  - name: Ensure journald ForwardToSyslog is disabled - Add missing configuration to correct section
    community.general.ini_file:
      path: '{{item}}'
      section: Journal
      option: ForwardToSyslog
      value: 'no'
      state: present
      no_extra_spaces: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int > 0
    loop: '{{systemd_dropin_files_with_section.results | sum(attribute=''files'', start=[]) | map(attribute=''path'') | list
      }}'
    tags:
    - journald_disable_forward_to_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald ForwardToSyslog is disabled - Add missing configuration to
      correct section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald ForwardToSyslog is disabled - Add missing configuration to correct section'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_disable_forward_to_syslog | default(true)
    | bool)
- block:
  - name: Ensure journald ForwardToSyslog is disabled - Add configuration to new remediation file
    community.general.ini_file:
      path: /etc/systemd/journald.conf.d/complianceascode_hardening.conf
      section: Journal
      option: ForwardToSyslog
      value: 'no'
      state: present
      no_extra_spaces: true
      create: true
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int == 0
    tags:
    - journald_disable_forward_to_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald ForwardToSyslog is disabled - Add configuration to new remediation
      file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald ForwardToSyslog is disabled - Add configuration to new remediation file'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_disable_forward_to_syslog | default(true)
    | bool)
- block:
  - name: Ensure journald is configured to write log files to persistent disk - Search for a section in files
    ansible.builtin.find:
      paths: '{{item.path}}'
      patterns: '{{item.pattern}}'
      contains: ^\s*\[Journal\]
      read_whole_file: true
      use_regex: true
    register: systemd_dropin_files_with_section
    loop:
    - path: '{{ ''/etc/systemd/journald.conf'' | dirname }}'
      pattern: '{{ ''/etc/systemd/journald.conf'' | basename | regex_escape }}'
    - path: /etc/systemd/journald.conf.d
      pattern: .*\.conf
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - journald_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to write log files to persistent disk - Search
      for a section in files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to write log files to persistent disk - Search for a section in files''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_storage | default(true) | bool)
- block:
  - name: Ensure journald is configured to write log files to persistent disk - Count number of files which contain the correct
      section
    ansible.builtin.set_fact:
      count_of_systemd_dropin_files_with_section: '{{systemd_dropin_files_with_section.results | map(attribute=''matched'')
        | list | map(''int'') | sum}}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - journald_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to write log files to persistent disk - Count
      number of files which contain the correct section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to write log files to persistent disk - Count number of files which
        contain the correct section'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_storage | default(true) | bool)
- block:
  - name: Ensure journald is configured to write log files to persistent disk - Add missing configuration to correct section
    community.general.ini_file:
      path: '{{item}}'
      section: Journal
      option: Storage
      value: persistent
      state: present
      no_extra_spaces: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int > 0
    loop: '{{systemd_dropin_files_with_section.results | sum(attribute=''files'', start=[]) | map(attribute=''path'') | list
      }}'
    tags:
    - journald_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to write log files to persistent disk - Add
      missing configuration to correct section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to write log files to persistent disk - Add missing configuration to
        correct section'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_storage | default(true) | bool)
- block:
  - name: Ensure journald is configured to write log files to persistent disk - Add configuration to new remediation file
    community.general.ini_file:
      path: /etc/systemd/journald.conf.d/complianceascode_hardening.conf
      section: Journal
      option: Storage
      value: persistent
      state: present
      no_extra_spaces: true
      create: true
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int == 0
    tags:
    - journald_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure journald is configured to write log files to persistent disk - Add
      configuration to new remediation file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure journald is configured to write log files to persistent disk - Add configuration to new remediation
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_journald_storage | default(true) | bool)
- block:
  - name: Disable systemd-journal-remote Socket - Collect systemd Socket Units Present in the System
    ansible.builtin.command:
      cmd: systemctl -q list-unit-files --type socket
    register: result_systemd_unit_files
    changed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - socket_systemd-journal-remote_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd-journal-remote Socket - Collect systemd Socket Units Present
      in the System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd-journal-remote Socket - Collect systemd Socket Units Present in the System'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket is Masked
    ansible.builtin.systemd:
      name: systemd-journal-remote.socket
      state: stopped
      enabled: false
      masked: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - result_systemd_unit_files.stdout_lines is search("systemd-journal-remote.socket")
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - socket_systemd-journal-remote_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket
      is Masked'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket is Masked'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Check if IPv6 is enabled
    ansible.builtin.command: sysctl -n net.ipv6.conf.all.disable_ipv6
    register: ipv6_status
    failed_when: ipv6_status.stdout != "0"
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check if IPv6 is enabled'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if IPv6 is enabled'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_ipv6_loopback_traffic | default(true) | bool)
- block:
  - name: Allow incoming traffic on the loopback interface
    ansible.builtin.iptables:
      ipv6: true
      chain: INPUT
      in_interface: lo
      jump: ACCEPT
    when:
    - ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    - ipv6_status.stdout == '0'
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow incoming traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow incoming traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_ipv6_loopback_traffic | default(true) | bool)
- block:
  - name: Allow outgoing traffic on the loopback interface
    ansible.builtin.iptables:
      ipv6: true
      chain: OUTPUT
      out_interface: lo
      jump: ACCEPT
    when:
    - ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    - ipv6_status.stdout == '0'
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow outgoing traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow outgoing traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_ipv6_loopback_traffic | default(true) | bool)
- block:
  - name: Drop incoming traffic from the localhost
    ansible.builtin.iptables:
      ipv6: true
      chain: INPUT
      source: ::1
      jump: DROP
    when:
    - ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    - ipv6_status.stdout == '0'
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_ipv6_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Drop incoming traffic from the localhost'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Drop incoming traffic from the localhost'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_ipv6_loopback_traffic | default(true) | bool)
- block:
  - name: Allow incoming traffic on the loopback interface
    ansible.builtin.iptables:
      chain: INPUT
      in_interface: lo
      jump: ACCEPT
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow incoming traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow incoming traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_loopback_traffic | default(true) | bool)
- block:
  - name: Allow outgoing traffic on the loopback interface
    ansible.builtin.iptables:
      chain: OUTPUT
      out_interface: lo
      jump: ACCEPT
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Allow outgoing traffic on the loopback interface'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Allow outgoing traffic on the loopback interface'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_loopback_traffic | default(true) | bool)
- block:
  - name: Drop incoming traffic from the localhost
    ansible.builtin.iptables:
      chain: INPUT
      source: 127.0.0.0/8
      jump: DROP
    when: ( not ( "nftables" in ansible_facts.packages ) and not ( "ufw" in ansible_facts.packages ) and "iptables" in ansible_facts.packages
      )
    tags:
    - PCI-DSS-Req-1.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - medium_severity
    - set_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Drop incoming traffic from the localhost'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Drop incoming traffic from the localhost'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_loopback_traffic | default(true) | bool)
- block:
  - name: Configure Accepting Router Advertisements on All IPv6 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Set fact
      for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Set fact for sysctl paths'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Configure Accepting Router Advertisements on All IPv6 Interfaces - Find all files that contain net.ipv6.conf.all.accept_ra
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_ra\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Find all
      files that contain net.ipv6.conf.all.accept_ra'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Find all files that contain net.ipv6.conf.all.accept_ra''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Configure Accepting Router Advertisements on All IPv6 Interfaces - Find all files that set net.ipv6.conf.all.accept_ra
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_ra\s*=\s*{{
        sysctl_net_ipv6_conf_all_accept_ra_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Find all
      files that set net.ipv6.conf.all.accept_ra to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Find all files that set net.ipv6.conf.all.accept_ra
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Configure Accepting Router Advertisements on All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_ra
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_ra
      replace: '#net.ipv6.conf.all.accept_ra'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Comment
      out any occurrences of net.ipv6.conf.all.accept_ra from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Comment out any occurrences of
        net.ipv6.conf.all.accept_ra from config files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Configure Accepting Router Advertisements on All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_ra
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.accept_ra.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Comment
      out any occurrences of net.ipv6.conf.all.accept_ra from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Comment out any occurrences of
        net.ipv6.conf.all.accept_ra from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Configure Accepting Router Advertisements on All IPv6 Interfaces - Ensure sysctl net.ipv6.conf.all.accept_ra is
      set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_ra
      value: '{{ sysctl_net_ipv6_conf_all_accept_ra_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Ensure
      sysctl net.ipv6.conf.all.accept_ra is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Accepting Router Advertisements on All IPv6 Interfaces - Ensure sysctl net.ipv6.conf.all.accept_ra
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv6 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Set fact for sysctl
      paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Set fact for sysctl paths'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv6 Interfaces - Find all files that contain net.ipv6.conf.all.accept_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Find all files
      that contain net.ipv6.conf.all.accept_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Find all files that contain net.ipv6.conf.all.accept_redirects''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv6 Interfaces - Find all files that set net.ipv6.conf.all.accept_redirects
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_redirects\s*=\s*{{
        sysctl_net_ipv6_conf_all_accept_redirects_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Find all files
      that set net.ipv6.conf.all.accept_redirects to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Find all files that set net.ipv6.conf.all.accept_redirects
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_redirects
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_redirects
      replace: '#net.ipv6.conf.all.accept_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Comment out any
      occurrences of net.ipv6.conf.all.accept_redirects from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_redirects
        from config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_redirects
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Comment out any
      occurrences of net.ipv6.conf.all.accept_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Comment out any occurrences of net.ipv6.conf.all.accept_redirects
        from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv6 Interfaces - Ensure sysctl net.ipv6.conf.all.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_redirects
      value: '{{ sysctl_net_ipv6_conf_all_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Ensure sysctl net.ipv6.conf.all.accept_redirects
      is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv6 Interfaces - Ensure sysctl net.ipv6.conf.all.accept_redirects
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Set fact for
        sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Find all files that contain
      net.ipv6.conf.all.accept_source_route
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_source_route\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces
      - Find all files that contain net.ipv6.conf.all.accept_source_route'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Find all files
        that contain net.ipv6.conf.all.accept_source_route'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Find all files that set net.ipv6.conf.all.accept_source_route
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.accept_source_route\s*=\s*{{
        sysctl_net_ipv6_conf_all_accept_source_route_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces
      - Find all files that set net.ipv6.conf.all.accept_source_route to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Find all files
        that set net.ipv6.conf.all.accept_source_route to correct value'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Comment out any occurrences
      of net.ipv6.conf.all.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_source_route
      replace: '#net.ipv6.conf.all.accept_source_route'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces
      - Comment out any occurrences of net.ipv6.conf.all.accept_source_route from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Comment out
        any occurrences of net.ipv6.conf.all.accept_source_route from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Comment out any occurrences
      of net.ipv6.conf.all.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces
      - Comment out any occurrences of net.ipv6.conf.all.accept_source_route from /etc/ufw/sysctl.conf'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Comment out
        any occurrences of net.ipv6.conf.all.accept_source_route from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Ensure sysctl net.ipv6.conf.all.accept_source_route
      is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_source_route
      value: '{{ sysctl_net_ipv6_conf_all_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces
      - Ensure sysctl net.ipv6.conf.all.accept_source_route is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv6 Interfaces - Ensure sysctl
        net.ipv6.conf.all.accept_source_route is set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for IPv6 Forwarding - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IPv6 Forwarding - Set fact for sysctl paths''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IPv6 Forwarding - Set fact for sysctl paths'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for IPv6 Forwarding - Find all files that contain net.ipv6.conf.all.forwarding
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.forwarding\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IPv6 Forwarding - Find all files that contain
      net.ipv6.conf.all.forwarding'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IPv6 Forwarding - Find all files that contain net.ipv6.conf.all.forwarding''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for IPv6 Forwarding - Find all files that set net.ipv6.conf.all.forwarding to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.all.forwarding\s*=\s*{{
        sysctl_net_ipv6_conf_all_forwarding_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IPv6 Forwarding - Find all files that set net.ipv6.conf.all.forwarding
      to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IPv6 Forwarding - Find all files that set net.ipv6.conf.all.forwarding
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for IPv6 Forwarding - Comment out any occurrences of net.ipv6.conf.all.forwarding from
      config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv6.conf.all.forwarding
      replace: '#net.ipv6.conf.all.forwarding'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IPv6 Forwarding - Comment out any occurrences
      of net.ipv6.conf.all.forwarding from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IPv6 Forwarding - Comment out any occurrences of net.ipv6.conf.all.forwarding
        from config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for IPv6 Forwarding - Comment out any occurrences of net.ipv6.conf.all.forwarding from
      /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.all.forwarding.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IPv6 Forwarding - Comment out any occurrences
      of net.ipv6.conf.all.forwarding from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IPv6 Forwarding - Comment out any occurrences of net.ipv6.conf.all.forwarding
        from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for IPv6 Forwarding - Ensure sysctl net.ipv6.conf.all.forwarding is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.forwarding
      value: '{{ sysctl_net_ipv6_conf_all_forwarding_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IPv6 Forwarding - Ensure sysctl net.ipv6.conf.all.forwarding
      is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IPv6 Forwarding - Ensure sysctl net.ipv6.conf.all.forwarding is set''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Set fact for sysctl paths''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Find all files that contain net.ipv6.conf.default.accept_ra
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.default.accept_ra\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default
      - Find all files that contain net.ipv6.conf.default.accept_ra'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Find all files that contain
        net.ipv6.conf.default.accept_ra'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Find all files that set net.ipv6.conf.default.accept_ra
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.default.accept_ra\s*=\s*{{
        sysctl_net_ipv6_conf_default_accept_ra_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default
      - Find all files that set net.ipv6.conf.default.accept_ra to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Find all files that set
        net.ipv6.conf.default.accept_ra to correct value'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Comment out any occurrences of net.ipv6.conf.default.accept_ra
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_ra
      replace: '#net.ipv6.conf.default.accept_ra'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default
      - Comment out any occurrences of net.ipv6.conf.default.accept_ra from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Comment out any occurrences
        of net.ipv6.conf.default.accept_ra from config files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Comment out any occurrences of net.ipv6.conf.default.accept_ra
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.default.accept_ra.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default
      - Comment out any occurrences of net.ipv6.conf.default.accept_ra from /etc/ufw/sysctl.conf'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Comment out any occurrences
        of net.ipv6.conf.default.accept_ra from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Ensure sysctl net.ipv6.conf.default.accept_ra
      is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_ra
      value: '{{ sysctl_net_ipv6_conf_default_accept_ra_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default
      - Ensure sysctl net.ipv6.conf.default.accept_ra is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting Router Advertisements on all IPv6 Interfaces by Default - Ensure sysctl net.ipv6.conf.default.accept_ra
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Set fact for
        sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Find all files that contain
      net.ipv6.conf.default.accept_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.default.accept_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces
      - Find all files that contain net.ipv6.conf.default.accept_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Find all files
        that contain net.ipv6.conf.default.accept_redirects'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Find all files that set net.ipv6.conf.default.accept_redirects
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.default.accept_redirects\s*=\s*{{
        sysctl_net_ipv6_conf_default_accept_redirects_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces
      - Find all files that set net.ipv6.conf.default.accept_redirects to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Find all files
        that set net.ipv6.conf.default.accept_redirects to correct value'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Comment out any occurrences
      of net.ipv6.conf.default.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_redirects
      replace: '#net.ipv6.conf.default.accept_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces
      - Comment out any occurrences of net.ipv6.conf.default.accept_redirects from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Comment out
        any occurrences of net.ipv6.conf.default.accept_redirects from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Comment out any occurrences
      of net.ipv6.conf.default.accept_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.default.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces
      - Comment out any occurrences of net.ipv6.conf.default.accept_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Comment out
        any occurrences of net.ipv6.conf.default.accept_redirects from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Ensure sysctl net.ipv6.conf.default.accept_redirects
      is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_redirects
      value: '{{ sysctl_net_ipv6_conf_default_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces
      - Ensure sysctl net.ipv6.conf.default.accept_redirects is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv6 Interfaces - Ensure sysctl
        net.ipv6.conf.default.accept_redirects is set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Set fact for sysctl
      paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces
      by Default - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Set
        fact for sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Find all files that
      contain net.ipv6.conf.default.accept_source_route
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.default.accept_source_route\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces
      by Default - Find all files that contain net.ipv6.conf.default.accept_source_route'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Find
        all files that contain net.ipv6.conf.default.accept_source_route'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Find all files that
      set net.ipv6.conf.default.accept_source_route to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv6.conf.default.accept_source_route\s*=\s*{{
        sysctl_net_ipv6_conf_default_accept_source_route_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces
      by Default - Find all files that set net.ipv6.conf.default.accept_source_route to correct value'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Find
        all files that set net.ipv6.conf.default.accept_source_route to correct value'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Comment out any occurrences
      of net.ipv6.conf.default.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_source_route
      replace: '#net.ipv6.conf.default.accept_source_route'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces
      by Default - Comment out any occurrences of net.ipv6.conf.default.accept_source_route from config files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Comment
        out any occurrences of net.ipv6.conf.default.accept_source_route from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Comment out any occurrences
      of net.ipv6.conf.default.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv6.conf.default.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces
      by Default - Comment out any occurrences of net.ipv6.conf.default.accept_source_route from /etc/ufw/sysctl.conf'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Comment
        out any occurrences of net.ipv6.conf.default.accept_source_route from /etc/ufw/sysctl.conf'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Ensure sysctl net.ipv6.conf.default.accept_source_route
      is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_source_route
      value: '{{ sysctl_net_ipv6_conf_default_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces
      by Default - Ensure sysctl net.ipv6.conf.default.accept_source_route is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv6 Interfaces by Default - Ensure
        sysctl net.ipv6.conf.default.accept_source_route is set'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv6_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Set fact for sysctl
      paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Set fact for sysctl paths'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv4 Interfaces - Find all files that contain net.ipv4.conf.all.accept_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.accept_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Find all files
      that contain net.ipv4.conf.all.accept_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Find all files that contain net.ipv4.conf.all.accept_redirects''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv4 Interfaces - Find all files that set net.ipv4.conf.all.accept_redirects
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.accept_redirects\s*=\s*{{
        sysctl_net_ipv4_conf_all_accept_redirects_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Find all files
      that set net.ipv4.conf.all.accept_redirects to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Find all files that set net.ipv4.conf.all.accept_redirects
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.accept_redirects
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.all.accept_redirects
      replace: '#net.ipv4.conf.all.accept_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Comment out any
      occurrences of net.ipv4.conf.all.accept_redirects from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.accept_redirects
        from config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.accept_redirects
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Comment out any
      occurrences of net.ipv4.conf.all.accept_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.accept_redirects
        from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Accepting ICMP Redirects for All IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.accept_redirects
      value: '{{ sysctl_net_ipv4_conf_all_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.accept_redirects
      is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Accepting ICMP Redirects for All IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.accept_redirects
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Set fact for
        sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Find all files that contain
      net.ipv4.conf.all.accept_source_route
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.accept_source_route\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces
      - Find all files that contain net.ipv4.conf.all.accept_source_route'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Find all files
        that contain net.ipv4.conf.all.accept_source_route'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Find all files that set net.ipv4.conf.all.accept_source_route
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.accept_source_route\s*=\s*{{
        sysctl_net_ipv4_conf_all_accept_source_route_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces
      - Find all files that set net.ipv4.conf.all.accept_source_route to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Find all files
        that set net.ipv4.conf.all.accept_source_route to correct value'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.conf.all.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.all.accept_source_route
      replace: '#net.ipv4.conf.all.accept_source_route'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.accept_source_route from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Comment out
        any occurrences of net.ipv4.conf.all.accept_source_route from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.conf.all.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.accept_source_route from /etc/ufw/sysctl.conf'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Comment out
        any occurrences of net.ipv4.conf.all.accept_source_route from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.accept_source_route
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.accept_source_route
      value: '{{ sysctl_net_ipv4_conf_all_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces
      - Ensure sysctl net.ipv4.conf.all.accept_source_route is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on all IPv4 Interfaces - Ensure sysctl
        net.ipv4.conf.all.accept_source_route is set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Set
      fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Set fact for sysctl paths''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Find all files that contain net.ipv4.conf.all.log_martians
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.log_martians\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Find
      all files that contain net.ipv4.conf.all.log_martians'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Find all files that contain
        net.ipv4.conf.all.log_martians'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Find all files that set net.ipv4.conf.all.log_martians
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.log_martians\s*=\s*{{
        sysctl_net_ipv4_conf_all_log_martians_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Find
      all files that set net.ipv4.conf.all.log_martians to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Find all files that set net.ipv4.conf.all.log_martians
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.log_martians
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.all.log_martians
      replace: '#net.ipv4.conf.all.log_martians'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Comment
      out any occurrences of net.ipv4.conf.all.log_martians from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Comment out any occurrences
        of net.ipv4.conf.all.log_martians from config files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.log_martians
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.log_martians.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Comment
      out any occurrences of net.ipv4.conf.all.log_martians from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Comment out any occurrences
        of net.ipv4.conf.all.log_martians from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.log_martians
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.log_martians
      value: '{{ sysctl_net_ipv4_conf_all_log_martians_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Ensure
      sysctl net.ipv4.conf.all.log_martians is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.log_martians
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Set fact for sysctl
        paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Find all files that contain net.ipv4.conf.all.rp_filter
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.rp_filter\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      - Find all files that contain net.ipv4.conf.all.rp_filter'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Find all files that
        contain net.ipv4.conf.all.rp_filter'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Find all files that set net.ipv4.conf.all.rp_filter
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.rp_filter\s*=\s*{{
        sysctl_net_ipv4_conf_all_rp_filter_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      - Find all files that set net.ipv4.conf.all.rp_filter to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Find all files that
        set net.ipv4.conf.all.rp_filter to correct value'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.rp_filter
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.all.rp_filter
      replace: '#net.ipv4.conf.all.rp_filter'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.rp_filter from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Comment out any occurrences
        of net.ipv4.conf.all.rp_filter from config files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.rp_filter
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.rp_filter.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.rp_filter from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Comment out any occurrences
        of net.ipv4.conf.all.rp_filter from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.rp_filter
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.rp_filter
      value: '{{ sysctl_net_ipv4_conf_all_rp_filter_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      - Ensure sysctl net.ipv4.conf.all.rp_filter is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.rp_filter
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Set fact for
        sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Find all files that contain
      net.ipv4.conf.all.secure_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.secure_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces
      - Find all files that contain net.ipv4.conf.all.secure_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Find all files
        that contain net.ipv4.conf.all.secure_redirects'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Find all files that set net.ipv4.conf.all.secure_redirects
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.secure_redirects\s*=\s*{{
        sysctl_net_ipv4_conf_all_secure_redirects_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces
      - Find all files that set net.ipv4.conf.all.secure_redirects to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Find all files
        that set net.ipv4.conf.all.secure_redirects to correct value'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.conf.all.secure_redirects from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.all.secure_redirects
      replace: '#net.ipv4.conf.all.secure_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.secure_redirects from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Comment out
        any occurrences of net.ipv4.conf.all.secure_redirects from config files'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.conf.all.secure_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.secure_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.secure_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Comment out
        any occurrences of net.ipv4.conf.all.secure_redirects from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.secure_redirects
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.secure_redirects
      value: '{{ sysctl_net_ipv4_conf_all_secure_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces
      - Ensure sysctl net.ipv4.conf.all.secure_redirects is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Secure ICMP Redirects on all IPv4 Interfaces - Ensure sysctl
        net.ipv4.conf.all.secure_redirects is set'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Set fact for
        sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Find all files that contain
      net.ipv4.conf.default.accept_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.accept_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces
      - Find all files that contain net.ipv4.conf.default.accept_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Find all files
        that contain net.ipv4.conf.default.accept_redirects'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Find all files that set net.ipv4.conf.default.accept_redirects
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.accept_redirects\s*=\s*{{
        sysctl_net_ipv4_conf_default_accept_redirects_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces
      - Find all files that set net.ipv4.conf.default.accept_redirects to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Find all files
        that set net.ipv4.conf.default.accept_redirects to correct value'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.conf.default.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.default.accept_redirects
      replace: '#net.ipv4.conf.default.accept_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.default.accept_redirects from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Comment out
        any occurrences of net.ipv4.conf.default.accept_redirects from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.conf.default.accept_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.accept_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.default.accept_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Comment out
        any occurrences of net.ipv4.conf.default.accept_redirects from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Ensure sysctl net.ipv4.conf.default.accept_redirects
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.accept_redirects
      value: '{{ sysctl_net_ipv4_conf_default_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces
      - Ensure sysctl net.ipv4.conf.default.accept_redirects is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting ICMP Redirects by Default on IPv4 Interfaces - Ensure sysctl
        net.ipv4.conf.default.accept_redirects is set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_redirects |
    default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Set fact for sysctl
      paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces
      by Default - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Set
        fact for sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Find all files that
      contain net.ipv4.conf.default.accept_source_route
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.accept_source_route\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces
      by Default - Find all files that contain net.ipv4.conf.default.accept_source_route'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Find
        all files that contain net.ipv4.conf.default.accept_source_route'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Find all files that
      set net.ipv4.conf.default.accept_source_route to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.accept_source_route\s*=\s*{{
        sysctl_net_ipv4_conf_default_accept_source_route_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces
      by Default - Find all files that set net.ipv4.conf.default.accept_source_route to correct value'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Find
        all files that set net.ipv4.conf.default.accept_source_route to correct value'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Comment out any occurrences
      of net.ipv4.conf.default.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.default.accept_source_route
      replace: '#net.ipv4.conf.default.accept_source_route'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces
      by Default - Comment out any occurrences of net.ipv4.conf.default.accept_source_route from config files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Comment
        out any occurrences of net.ipv4.conf.default.accept_source_route from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Comment out any occurrences
      of net.ipv4.conf.default.accept_source_route from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.accept_source_route.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces
      by Default - Comment out any occurrences of net.ipv4.conf.default.accept_source_route from /etc/ufw/sysctl.conf'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Comment
        out any occurrences of net.ipv4.conf.default.accept_source_route from /etc/ufw/sysctl.conf'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Ensure sysctl net.ipv4.conf.default.accept_source_route
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.accept_source_route
      value: '{{ sysctl_net_ipv4_conf_default_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces
      by Default - Ensure sysctl net.ipv4.conf.default.accept_source_route is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Accepting Source-Routed Packets on IPv4 Interfaces by Default - Ensure
        sysctl net.ipv4.conf.default.accept_source_route is set'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_accept_source_route
    | default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Set fact for sysctl
        paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Find all files that contain net.ipv4.conf.default.log_martians
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.log_martians\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default
      - Find all files that contain net.ipv4.conf.default.log_martians'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Find all files
        that contain net.ipv4.conf.default.log_martians'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Find all files that set net.ipv4.conf.default.log_martians
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.log_martians\s*=\s*{{
        sysctl_net_ipv4_conf_default_log_martians_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default
      - Find all files that set net.ipv4.conf.default.log_martians to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Find all files
        that set net.ipv4.conf.default.log_martians to correct value'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Comment out any occurrences of
      net.ipv4.conf.default.log_martians from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.default.log_martians
      replace: '#net.ipv4.conf.default.log_martians'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default
      - Comment out any occurrences of net.ipv4.conf.default.log_martians from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Comment out any
        occurrences of net.ipv4.conf.default.log_martians from config files'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Comment out any occurrences of
      net.ipv4.conf.default.log_martians from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.log_martians.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default
      - Comment out any occurrences of net.ipv4.conf.default.log_martians from /etc/ufw/sysctl.conf'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Comment out any
        occurrences of net.ipv4.conf.default.log_martians from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Ensure sysctl net.ipv4.conf.default.log_martians
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.log_martians
      value: '{{ sysctl_net_ipv4_conf_default_log_martians_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default
      - Ensure sysctl net.ipv4.conf.default.log_martians is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Log Martian Packets on all IPv4 Interfaces by Default - Ensure sysctl
        net.ipv4.conf.default.log_martians is set'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      by Default - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Set fact
        for sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Find all files that contain
      net.ipv4.conf.default.rp_filter
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.rp_filter\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      by Default - Find all files that contain net.ipv4.conf.default.rp_filter'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Find all
        files that contain net.ipv4.conf.default.rp_filter'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Find all files that set
      net.ipv4.conf.default.rp_filter to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.rp_filter\s*=\s*{{
        sysctl_net_ipv4_conf_default_rp_filter_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      by Default - Find all files that set net.ipv4.conf.default.rp_filter to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Find all
        files that set net.ipv4.conf.default.rp_filter to correct value'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Comment out any occurrences
      of net.ipv4.conf.default.rp_filter from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.default.rp_filter
      replace: '#net.ipv4.conf.default.rp_filter'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      by Default - Comment out any occurrences of net.ipv4.conf.default.rp_filter from config files'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Comment
        out any occurrences of net.ipv4.conf.default.rp_filter from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Comment out any occurrences
      of net.ipv4.conf.default.rp_filter from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.rp_filter.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      by Default - Comment out any occurrences of net.ipv4.conf.default.rp_filter from /etc/ufw/sysctl.conf'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Comment
        out any occurrences of net.ipv4.conf.default.rp_filter from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Ensure sysctl net.ipv4.conf.default.rp_filter
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.rp_filter
      value: '{{ sysctl_net_ipv4_conf_default_rp_filter_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces
      by Default - Ensure sysctl net.ipv4.conf.default.rp_filter is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use Reverse Path Filtering on all IPv4 Interfaces by Default - Ensure
        sysctl net.ipv4.conf.default.rp_filter is set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Configure Kernel Parameter for Accepting Secure Redirects By Default - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Set
      fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Set fact for sysctl paths''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_secure_redirects |
    default(true) | bool)
- block:
  - name: Configure Kernel Parameter for Accepting Secure Redirects By Default - Find all files that contain net.ipv4.conf.default.secure_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.secure_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Find
      all files that contain net.ipv4.conf.default.secure_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Find all files that contain
        net.ipv4.conf.default.secure_redirects'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_secure_redirects |
    default(true) | bool)
- block:
  - name: Configure Kernel Parameter for Accepting Secure Redirects By Default - Find all files that set net.ipv4.conf.default.secure_redirects
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.secure_redirects\s*=\s*{{
        sysctl_net_ipv4_conf_default_secure_redirects_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Find
      all files that set net.ipv4.conf.default.secure_redirects to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Find all files that set net.ipv4.conf.default.secure_redirects
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_secure_redirects |
    default(true) | bool)
- block:
  - name: Configure Kernel Parameter for Accepting Secure Redirects By Default - Comment out any occurrences of net.ipv4.conf.default.secure_redirects
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.default.secure_redirects
      replace: '#net.ipv4.conf.default.secure_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Comment
      out any occurrences of net.ipv4.conf.default.secure_redirects from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Comment out any occurrences
        of net.ipv4.conf.default.secure_redirects from config files'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_secure_redirects |
    default(true) | bool)
- block:
  - name: Configure Kernel Parameter for Accepting Secure Redirects By Default - Comment out any occurrences of net.ipv4.conf.default.secure_redirects
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.secure_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Comment
      out any occurrences of net.ipv4.conf.default.secure_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Comment out any occurrences
        of net.ipv4.conf.default.secure_redirects from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_secure_redirects |
    default(true) | bool)
- block:
  - name: Configure Kernel Parameter for Accepting Secure Redirects By Default - Ensure sysctl net.ipv4.conf.default.secure_redirects
      is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.secure_redirects
      value: '{{ sysctl_net_ipv4_conf_default_secure_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Ensure
      sysctl net.ipv4.conf.default.secure_redirects is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Kernel Parameter for Accepting Secure Redirects By Default - Ensure sysctl net.ipv4.conf.default.secure_redirects
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_secure_redirects |
    default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Set fact for
        sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Find all files that contain
      net.ipv4.icmp_echo_ignore_broadcasts
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.icmp_echo_ignore_broadcasts\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces
      - Find all files that contain net.ipv4.icmp_echo_ignore_broadcasts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Find all files
        that contain net.ipv4.icmp_echo_ignore_broadcasts'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Find all files that set net.ipv4.icmp_echo_ignore_broadcasts
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.icmp_echo_ignore_broadcasts\s*=\s*{{
        sysctl_net_ipv4_icmp_echo_ignore_broadcasts_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces
      - Find all files that set net.ipv4.icmp_echo_ignore_broadcasts to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Find all files
        that set net.ipv4.icmp_echo_ignore_broadcasts to correct value'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.icmp_echo_ignore_broadcasts from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.icmp_echo_ignore_broadcasts
      replace: '#net.ipv4.icmp_echo_ignore_broadcasts'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Comment out any
        occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config files'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Comment out any occurrences
      of net.ipv4.icmp_echo_ignore_broadcasts from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.icmp_echo_ignore_broadcasts.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from /etc/ufw/sysctl.conf'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Comment out any
        occurrences of net.ipv4.icmp_echo_ignore_broadcasts from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts
      is set
    ansible.posix.sysctl:
      name: net.ipv4.icmp_echo_ignore_broadcasts
      value: '{{ sysctl_net_ipv4_icmp_echo_ignore_broadcasts_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces
      - Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces - Ensure sysctl
        net.ipv4.icmp_echo_ignore_broadcasts is set'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Set fact for sysctl
        paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Find all files that contain net.ipv4.icmp_ignore_bogus_error_responses
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.icmp_ignore_bogus_error_responses\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces
      - Find all files that contain net.ipv4.icmp_ignore_bogus_error_responses'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Find all files
        that contain net.ipv4.icmp_ignore_bogus_error_responses'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Find all files that set net.ipv4.icmp_ignore_bogus_error_responses
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.icmp_ignore_bogus_error_responses\s*=\s*{{
        sysctl_net_ipv4_icmp_ignore_bogus_error_responses_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces
      - Find all files that set net.ipv4.icmp_ignore_bogus_error_responses to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Find all files
        that set net.ipv4.icmp_ignore_bogus_error_responses to correct value'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Comment out any occurrences of
      net.ipv4.icmp_ignore_bogus_error_responses from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.icmp_ignore_bogus_error_responses
      replace: '#net.ipv4.icmp_ignore_bogus_error_responses'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from config files'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Comment out any
        occurrences of net.ipv4.icmp_ignore_bogus_error_responses from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Comment out any occurrences of
      net.ipv4.icmp_ignore_bogus_error_responses from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.icmp_ignore_bogus_error_responses.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from /etc/ufw/sysctl.conf'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Comment out any
        occurrences of net.ipv4.icmp_ignore_bogus_error_responses from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses
      is set
    ansible.posix.sysctl:
      name: net.ipv4.icmp_ignore_bogus_error_responses
      value: '{{ sysctl_net_ipv4_icmp_ignore_bogus_error_responses_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces
      - Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Ignore Bogus ICMP Error Responses on IPv4 Interfaces - Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Set
      fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Set fact for sysctl paths''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_tcp_syncookies | default(true) |
    bool)
- block:
  - name: Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Find all files that contain net.ipv4.tcp_syncookies
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.tcp_syncookies\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Find
      all files that contain net.ipv4.tcp_syncookies'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Find all files that contain
        net.ipv4.tcp_syncookies'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_tcp_syncookies | default(true) |
    bool)
- block:
  - name: Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Find all files that set net.ipv4.tcp_syncookies
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.tcp_syncookies\s*=\s*{{
        sysctl_net_ipv4_tcp_syncookies_value }}$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Find
      all files that set net.ipv4.tcp_syncookies to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Find all files that set net.ipv4.tcp_syncookies
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_tcp_syncookies | default(true) |
    bool)
- block:
  - name: Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Comment out any occurrences of net.ipv4.tcp_syncookies
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.tcp_syncookies
      replace: '#net.ipv4.tcp_syncookies'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Comment
      out any occurrences of net.ipv4.tcp_syncookies from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Comment out any occurrences
        of net.ipv4.tcp_syncookies from config files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_tcp_syncookies | default(true) |
    bool)
- block:
  - name: Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Comment out any occurrences of net.ipv4.tcp_syncookies
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.tcp_syncookies.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Comment
      out any occurrences of net.ipv4.tcp_syncookies from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Comment out any occurrences
        of net.ipv4.tcp_syncookies from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_tcp_syncookies | default(true) |
    bool)
- block:
  - name: Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Ensure sysctl net.ipv4.tcp_syncookies is set
    ansible.posix.sysctl:
      name: net.ipv4.tcp_syncookies
      value: '{{ sysctl_net_ipv4_tcp_syncookies_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - DISA-STIG-UBTU-22-253010
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Ensure
      sysctl net.ipv4.tcp_syncookies is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Kernel Parameter to Use TCP Syncookies on Network Interfaces - Ensure sysctl net.ipv4.tcp_syncookies
        is set'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_tcp_syncookies | default(true) |
    bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Set fact for sysctl
        paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Find all files that contain net.ipv4.conf.all.send_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.send_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      - Find all files that contain net.ipv4.conf.all.send_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Find all files that
        contain net.ipv4.conf.all.send_redirects'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Find all files that set net.ipv4.conf.all.send_redirects
      to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.all.send_redirects\s*=\s*0$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      - Find all files that set net.ipv4.conf.all.send_redirects to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Find all files that
        set net.ipv4.conf.all.send_redirects to correct value'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.send_redirects
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.all.send_redirects
      replace: '#net.ipv4.conf.all.send_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.send_redirects from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Comment out any occurrences
        of net.ipv4.conf.all.send_redirects from config files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Comment out any occurrences of net.ipv4.conf.all.send_redirects
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.all.send_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      - Comment out any occurrences of net.ipv4.conf.all.send_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Comment out any occurrences
        of net.ipv4.conf.all.send_redirects from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.send_redirects
      is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.send_redirects
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      - Ensure sysctl net.ipv4.conf.all.send_redirects is set to 0'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces - Ensure sysctl net.ipv4.conf.all.send_redirects
        is set to 0'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      by Default - Set fact for sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Set fact
        for sysctl paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Find all files that contain
      net.ipv4.conf.default.send_redirects
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.send_redirects\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      by Default - Find all files that contain net.ipv4.conf.default.send_redirects'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Find all
        files that contain net.ipv4.conf.default.send_redirects'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Find all files that set
      net.ipv4.conf.default.send_redirects to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.conf.default.send_redirects\s*=\s*0$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      by Default - Find all files that set net.ipv4.conf.default.send_redirects to correct value'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Find all
        files that set net.ipv4.conf.default.send_redirects to correct value'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Comment out any occurrences
      of net.ipv4.conf.default.send_redirects from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.conf.default.send_redirects
      replace: '#net.ipv4.conf.default.send_redirects'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      by Default - Comment out any occurrences of net.ipv4.conf.default.send_redirects from config files'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Comment out
        any occurrences of net.ipv4.conf.default.send_redirects from config files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Comment out any occurrences
      of net.ipv4.conf.default.send_redirects from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.conf.default.send_redirects.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      by Default - Comment out any occurrences of net.ipv4.conf.default.send_redirects from /etc/ufw/sysctl.conf'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Comment out
        any occurrences of net.ipv4.conf.default.send_redirects from /etc/ufw/sysctl.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Ensure sysctl net.ipv4.conf.default.send_redirects
      is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.send_redirects
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces
      by Default - Ensure sysctl net.ipv4.conf.default.send_redirects is set to 0'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for Sending ICMP Redirects on all IPv4 Interfaces by Default - Ensure sysctl
        net.ipv4.conf.default.send_redirects is set to 0'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Set fact for
      sysctl paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Set fact for sysctl paths'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Find all files that contain net.ipv4.ip_forward
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.ip_forward\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Find all files
      that contain net.ipv4.ip_forward'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Find all files that contain net.ipv4.ip_forward''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Find all files that set net.ipv4.ip_forward to correct
      value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*net.ipv4.ip_forward\s*=\s*0$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Find all files
      that set net.ipv4.ip_forward to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Find all files that set net.ipv4.ip_forward
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Comment out any occurrences of net.ipv4.ip_forward
      from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*net.ipv4.ip_forward
      replace: '#net.ipv4.ip_forward'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Comment out
      any occurrences of net.ipv4.ip_forward from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Comment out any occurrences of net.ipv4.ip_forward
        from config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Comment out any occurrences of net.ipv4.ip_forward
      from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*net.ipv4.ip_forward.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Comment out
      any occurrences of net.ipv4.ip_forward from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Comment out any occurrences of net.ipv4.ip_forward
        from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Ensure sysctl net.ipv4.ip_forward is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.ip_forward
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Ensure sysctl
      net.ipv4.ip_forward is set to 0'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Kernel Parameter for IP Forwarding on IPv4 Interfaces - Ensure sysctl net.ipv4.ip_forward
        is set to 0'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Ensure Base Chains Exist for Nftables - Check Existence of Nftables Table
    ansible.builtin.shell: nft list tables | grep '{{ var_nftables_family }} {{ var_nftables_table }}'
    register: existing_nftables
    changed_when: false
    failed_when: false
    when: '"nftables" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_base_chain
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Base Chains Exist for Nftables - Check Existence of Nftables Table''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Base Chains Exist for Nftables - Check Existence of Nftables Table'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_base_chain | default(true) | bool)
- block:
  - name: Ensure Base Chains Exist for Nftables - Set NFTables Table
    ansible.builtin.command: nft create table {{ var_nftables_family }} {{ var_nftables_table }}
    when:
    - '"nftables" in ansible_facts.packages'
    - existing_nftables is not skipped and existing_nftables.rc > 0
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_base_chain
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Base Chains Exist for Nftables - Set NFTables Table'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Base Chains Exist for Nftables - Set NFTables Table'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_base_chain | default(true) | bool)
- block:
  - name: Ensure Base Chains Exist for Nftables - Add Base Chains
    ansible.builtin.command: nft 'add chain {{ var_nftables_family }} {{ var_nftables_table }} {{ item.0 }} { type {{ item.1
      }} hook {{ item.2 }} priority {{ item.3 }} ; policy {{ item.4 }} ; }'
    with_together:
    - '{{ var_nftables_base_chain_names.split(",") }}'
    - '{{ var_nftables_base_chain_types.split(",") }}'
    - '{{ var_nftables_base_chain_hooks.split(",") }}'
    - '{{ var_nftables_base_chain_priorities.split(",") }}'
    - '{{ var_nftables_base_chain_policies.split(",") }}'
    when: '"nftables" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_base_chain
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Base Chains Exist for Nftables - Add Base Chains'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Base Chains Exist for Nftables - Add Base Chains'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_base_chain | default(true) | bool)
- block:
  - name: Implement Loopback Rules
    ansible.builtin.command: nft add rule inet filter input iif lo accept
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Implement Loopback Rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Implement Loopback Rules'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_loopback_traffic | default(true) |
    bool)
- block:
  - name: Create Rule to Drop Input IP Address from Loopback
    ansible.builtin.command: nft add rule inet filter input ip saddr 127.0.0.0/8 counter drop
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Create Rule to Drop Input IP Address from Loopback'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create Rule to Drop Input IP Address from Loopback'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_loopback_traffic | default(true) |
    bool)
- block:
  - name: Check if IPv6 is Disabled in grub Configuration
    ansible.builtin.shell: '[ -z "$(grep "^\s*linux" /boot/grub2/grub.cfg | grep -v ipv6.disabled=1)" ]

      '
    register: ipv6_status
    changed_when: false
    check_mode: false
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check if IPv6 is Disabled in grub Configuration'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if IPv6 is Disabled in grub Configuration'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_loopback_traffic | default(true) |
    bool)
- block:
  - name: Check sysctl value of net.ipv6.conf.all.disable_ipv6
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.disable_ipv6
      state: present
      value: '1'
    check_mode: true
    register: sysctl_ipv6_all
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check sysctl value of net.ipv6.conf.all.disable_ipv6'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check sysctl value of net.ipv6.conf.all.disable_ipv6'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_loopback_traffic | default(true) |
    bool)
- block:
  - name: Check sysctl value of net.ipv6.conf.default.disable_ipv6
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.disable_ipv6
      state: present
      value: '1'
    check_mode: true
    register: sysctl_ipv6_default
    when: ( "nftables" in ansible_facts.packages )
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Check sysctl value of net.ipv6.conf.default.disable_ipv6'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check sysctl value of net.ipv6.conf.default.disable_ipv6'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_loopback_traffic | default(true) |
    bool)
- block:
  - name: Implement IPv6 loopback rules
    ansible.builtin.command: nft add rule inet filter input ip6 saddr ::1 counter drop
    when:
    - ( "nftables" in ansible_facts.packages )
    - ipv6_status is not skipped
    - sysctl_ipv6_default is not skipped
    - sysctl_ipv6_all is not skipped
    - ipv6_status.rc == 0 or sysctl_ipv6_all.found > 0 or sysctl_ipv6_default.found > 0
    tags:
    - PCI-DSS-Req-1.4.1
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_loopback_traffic
  rescue:
  - name: 'StigForge: continue after error in ''Implement IPv6 loopback rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Implement IPv6 loopback rules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_loopback_traffic | default(true) |
    bool)
- block:
  - name: Collect Existing Nftables
    ansible.builtin.command: nft list table {{ var_nftables_family }} {{ var_nftables_table }}
    register: result_nftables_table_family
    changed_when: false
    failed_when: result_nftables_table_family.rc not in [0, 1]
    when: '"nftables" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_table
  rescue:
  - name: 'StigForge: continue after error in ''Collect Existing Nftables'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect Existing Nftables'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_table | default(true) | bool)
- block:
  - name: Set Nftable Table
    ansible.builtin.command: nft create table {{ var_nftables_family }} {{ var_nftables_table }}
    when:
    - '"nftables" in ansible_facts.packages'
    - result_nftables_table_family is not skipped
    - result_nftables_table_family.rc != 0
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_table
  rescue:
  - name: 'StigForge: continue after error in ''Set Nftable Table'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Nftable Table'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_set_nftables_table | default(true) | bool)
- block:
  - name: Enable service ufw
    block:
    - name: Enable service ufw
      ansible.builtin.systemd:
        name: ufw
        enabled: 'yes'
        state: started
        masked: 'no'
      when:
      - '"ufw" in ansible_facts.packages'
      - var_network_filtering_service == "ufw"
      - var_network_filtering_service == "ufw"
    when:
    - '"linux-base" in ansible_facts.packages'
    - ( "ufw" in ansible_facts.packages and "linux-base" in ansible_facts.packages )
    tags:
    - DISA-STIG-UBTU-22-251020
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_ufw_enabled
  rescue:
  - name: 'StigForge: continue after error in ''Enable service ufw'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable service ufw'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_ufw_enabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'dccp' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/dccp.conf
      regexp: install\s+dccp
      line: install dccp /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - kernel_module_dccp_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''dccp'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''dccp'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_dccp_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'dccp' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/dccp.conf
      regexp: ^blacklist dccp$
      line: blacklist dccp
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - kernel_module_dccp_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''dccp'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''dccp'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_dccp_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'rds' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/rds.conf
      regexp: install\s+rds
      line: install rds /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_rds_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''rds'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''rds'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_rds_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'rds' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/rds.conf
      regexp: ^blacklist rds$
      line: blacklist rds
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_rds_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''rds'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''rds'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_rds_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'sctp' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/sctp.conf
      regexp: install\s+sctp
      line: install sctp /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - kernel_module_sctp_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''sctp'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''sctp'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_sctp_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'sctp' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/sctp.conf
      regexp: ^blacklist sctp$
      line: blacklist sctp
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - kernel_module_sctp_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''sctp'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''sctp'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_sctp_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'tipc' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/tipc.conf
      regexp: install\s+tipc
      line: install tipc /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_tipc_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''tipc'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''tipc'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_tipc_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'tipc' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/tipc.conf
      regexp: ^blacklist tipc$
      line: blacklist tipc
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_tipc_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''tipc'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''tipc'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_tipc_disabled | default(true) | bool)
- block:
  - name: Find /var/log/ file(s) recursively
    ansible.builtin.command: find -P /var/log/  -perm /u+xs,g+xws,o+xwrt ! -name "history.log*" ! -name "eipp.log.xz*" ! -name
      "[bw]tmp" ! -name "[bw]tmp.*" ! -name "[bw]tmp-*" ! -name "lastlog" ! -name "lastlog.*" -type f -regextype posix-extended
      -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - DISA-STIG-UBTU-22-232026
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - NIST-800-53-SI-11.1(iii)
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - permissions_local_var_log
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) recursively'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) recursively'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_permissions_local_var_log | default(true) | bool)
- block:
  - name: Set permissions for /var/log/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - DISA-STIG-UBTU-22-232026
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - NIST-800-53-SI-11.1(iii)
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - permissions_local_var_log
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_permissions_local_var_log | default(true) | bool)
- block:
  - name: Set the file_groupowner_backup_etc_group_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_group_newgroup: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_group_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_group_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_group | default(true)
    | bool)
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_group | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      follow: false
      group: '{{ file_groupowner_backup_etc_group_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_group | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_gshadow_newgroup: '42'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      follow: false
      group: '{{ file_groupowner_backup_etc_gshadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_backup_etc_passwd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_passwd_newgroup: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_passwd_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_passwd_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      follow: false
      group: '{{ file_groupowner_backup_etc_passwd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_backup_etc_shadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_shadow_newgroup: '42'
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_shadow_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_shadow_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      follow: false
      group: '{{ file_groupowner_backup_etc_shadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_etc_group_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_group_newgroup: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_group_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_group_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_group | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/group
    ansible.builtin.file:
      path: /etc/group
      follow: false
      group: '{{ file_groupowner_etc_group_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_group | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_gshadow_newgroup: '42'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_gshadow | default(true) | bool)
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_gshadow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      follow: false
      group: '{{ file_groupowner_etc_gshadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_gshadow | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_passwd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_passwd_newgroup: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_passwd_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_passwd_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_passwd | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      follow: false
      group: '{{ file_groupowner_etc_passwd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_passwd | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_security_opasswd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_security_opasswd_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_security_opasswd_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_security_opasswd_newgroup variable if represented by gid'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/security/opasswd
    ansible.builtin.stat:
      path: /etc/security/opasswd
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/security/opasswd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/security/opasswd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/security/opasswd
    ansible.builtin.file:
      path: /etc/security/opasswd
      follow: false
      group: '{{ file_groupowner_etc_security_opasswd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/security/opasswd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/security/opasswd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_etc_security_opasswd_old_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_security_opasswd_old_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_security_opasswd_old_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_security_opasswd_old_newgroup variable if represented by gid'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Test for existence /etc/security/opasswd.old
    ansible.builtin.stat:
      path: /etc/security/opasswd.old
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/security/opasswd.old'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/security/opasswd.old'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/security/opasswd.old
    ansible.builtin.file:
      path: /etc/security/opasswd.old
      follow: false
      group: '{{ file_groupowner_etc_security_opasswd_old_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/security/opasswd.old'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/security/opasswd.old'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_etc_shadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_shadow_newgroup: '42'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_shadow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_shadow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_shadow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      follow: false
      group: '{{ file_groupowner_etc_shadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_shadow | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_shells_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_shells_newgroup: '0'
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_groupowner_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_shells_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_shells_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_shells | default(true) | bool)
- block:
  - name: Test for existence /etc/shells
    ansible.builtin.stat:
      path: /etc/shells
    register: file_exists
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_groupowner_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shells'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shells'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_shells | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/shells
    ansible.builtin.file:
      path: /etc/shells
      follow: false
      group: '{{ file_groupowner_etc_shells_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_groupowner_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/shells'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/shells'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_etc_shells | default(true) | bool)
- block:
  - name: Set the file_owner_backup_etc_group_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_group_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_group_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_group_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_group | default(true) | bool)
- block:
  - name: Ensure owner on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      follow: false
      owner: '{{ file_owner_backup_etc_group_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_group | default(true) | bool)
- block:
  - name: Set the file_owner_backup_etc_gshadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_gshadow_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_gshadow_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_gshadow_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_gshadow | default(true) |
    bool)
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_gshadow | default(true) |
    bool)
- block:
  - name: Ensure owner on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      follow: false
      owner: '{{ file_owner_backup_etc_gshadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_gshadow | default(true) |
    bool)
- block:
  - name: Set the file_owner_backup_etc_passwd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_passwd_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_passwd_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_passwd_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_passwd | default(true) | bool)
- block:
  - name: Ensure owner on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      follow: false
      owner: '{{ file_owner_backup_etc_passwd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_passwd | default(true) | bool)
- block:
  - name: Set the file_owner_backup_etc_shadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_shadow_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_shadow_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_shadow_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_shadow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      follow: false
      owner: '{{ file_owner_backup_etc_shadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_backup_etc_shadow | default(true) | bool)
- block:
  - name: Set the file_owner_etc_group_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_group_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_group_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_group_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_group | default(true) | bool)
- block:
  - name: Ensure owner on /etc/group
    ansible.builtin.file:
      path: /etc/group
      follow: false
      owner: '{{ file_owner_etc_group_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/group'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_group | default(true) | bool)
- block:
  - name: Set the file_owner_etc_gshadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_gshadow_newown: '0'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_gshadow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_gshadow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_gshadow | default(true) | bool)
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_gshadow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      follow: false
      owner: '{{ file_owner_etc_gshadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_gshadow | default(true) | bool)
- block:
  - name: Set the file_owner_etc_passwd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_passwd_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_passwd_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_passwd_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_passwd | default(true) | bool)
- block:
  - name: Ensure owner on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      follow: false
      owner: '{{ file_owner_etc_passwd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_passwd | default(true) | bool)
- block:
  - name: Set the file_owner_etc_security_opasswd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_security_opasswd_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_security_opasswd_newown variable if represented by
      uid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_security_opasswd_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/security/opasswd
    ansible.builtin.stat:
      path: /etc/security/opasswd
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/security/opasswd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/security/opasswd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Ensure owner on /etc/security/opasswd
    ansible.builtin.file:
      path: /etc/security/opasswd
      follow: false
      owner: '{{ file_owner_etc_security_opasswd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/security/opasswd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/security/opasswd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Set the file_owner_etc_security_opasswd_old_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_security_opasswd_old_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_security_opasswd_old_newown variable if represented
      by uid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_security_opasswd_old_newown variable if represented by uid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Test for existence /etc/security/opasswd.old
    ansible.builtin.stat:
      path: /etc/security/opasswd.old
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/security/opasswd.old'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/security/opasswd.old'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Ensure owner on /etc/security/opasswd.old
    ansible.builtin.file:
      path: /etc/security/opasswd.old
      follow: false
      owner: '{{ file_owner_etc_security_opasswd_old_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/security/opasswd.old'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/security/opasswd.old'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Set the file_owner_etc_shadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_shadow_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_shadow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_shadow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_shadow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      follow: false
      owner: '{{ file_owner_etc_shadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_shadow | default(true) | bool)
- block:
  - name: Set the file_owner_etc_shells_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_shells_newown: '0'
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_owner_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_shells_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_shells_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_shells | default(true) | bool)
- block:
  - name: Test for existence /etc/shells
    ansible.builtin.stat:
      path: /etc/shells
    register: file_exists
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_owner_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shells'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shells'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_shells | default(true) | bool)
- block:
  - name: Ensure owner on /etc/shells
    ansible.builtin.file:
      path: /etc/shells
      follow: false
      owner: '{{ file_owner_etc_shells_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_owner_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/shells'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/shells'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_etc_shells | default(true) | bool)
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_group | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/group-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/group-'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_group | default(true)
    | bool)
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - configure_strategy
    - file_permissions_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - configure_strategy
    - file_permissions_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow-'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow-'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_group | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/group
    ansible.builtin.file:
      path: /etc/group
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/group'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/group'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_gshadow | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/gshadow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_gshadow | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_passwd | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/security/opasswd
    ansible.builtin.stat:
      path: /etc/security/opasswd
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/security/opasswd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/security/opasswd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/security/opasswd
    ansible.builtin.file:
      path: /etc/security/opasswd
      mode: u-xs,g-xwrs,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_security_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/security/opasswd'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/security/opasswd'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_security_opasswd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/security/opasswd.old
    ansible.builtin.stat:
      path: /etc/security/opasswd.old
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/security/opasswd.old'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/security/opasswd.old'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/security/opasswd.old
    ansible.builtin.file:
      path: /etc/security/opasswd.old
      mode: u-xs,g-xwrs,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_security_opasswd_old
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/security/opasswd.old'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/security/opasswd.old'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_security_opasswd_old | default(true)
    | bool)
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_shadow | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/shadow'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/shells
    ansible.builtin.stat:
      path: /etc/shells
    register: file_exists
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_permissions_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shells'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shells'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_shells | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/shells
    ansible.builtin.file:
      path: /etc/shells
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-3
    - NIST-800-53-MP-2
    - configure_strategy
    - file_permissions_etc_shells
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/shells'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/shells'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_shells | default(true) | bool)
- block:
  - name: Check that the adm group is defined
    ansible.builtin.getent:
      database: group
      key: adm
    ignore_errors: true
    when: file_groupowner_var_log_auth_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the adm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the adm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_auth | default(true) | bool)
- block:
  - name: Set the file_groupowner_var_log_auth_newgroup variable if adm found
    ansible.builtin.set_fact:
      file_groupowner_var_log_auth_newgroup: adm
    when: ansible_facts.getent_group["adm"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_auth_newgroup variable if adm found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_auth_newgroup variable if adm found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_auth | default(true) | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_auth_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_auth | default(true) | bool)
- block:
  - name: Set the file_groupowner_var_log_auth_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_auth_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_auth_newgroup variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_auth_newgroup variable if root found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_auth | default(true) | bool)
- block:
  - name: Test for existence /var/log/auth.log
    ansible.builtin.stat:
      path: /var/log/auth.log
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/auth.log'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/auth.log'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_auth | default(true) | bool)
- block:
  - name: Ensure group owner on /var/log/auth.log
    ansible.builtin.file:
      path: /var/log/auth.log
      follow: false
      group: '{{ file_groupowner_var_log_auth_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/auth.log'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/auth.log'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_auth | default(true) | bool)
- block:
  - name: Check that the adm group is defined
    ansible.builtin.getent:
      database: group
      key: adm
    ignore_errors: true
    when: file_groupowner_var_log_cloud_init_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the adm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the adm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_cloud_init | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_cloud_init_newgroup variable if adm found
    ansible.builtin.set_fact:
      file_groupowner_var_log_cloud_init_newgroup: adm
    when: ansible_facts.getent_group["adm"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_cloud_init_newgroup variable if adm found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_cloud_init_newgroup variable if adm found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_cloud_init | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_cloud_init_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_cloud_init | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_cloud_init_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_cloud_init_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_cloud_init_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_cloud_init_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_cloud_init | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s) matching .*cloud-init\.log.*
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -group adm ! -group root -regextype posix-extended -regex
      ".*cloud-init\.log.*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupowner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*cloud-init\\.log.*'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*cloud-init\\.log.*'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_cloud_init | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/ file(s) matching .*cloud-init\.log.*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_var_log_cloud_init_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupowner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/ file(s) matching .*cloud-init\\.log.*'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/ file(s) matching .*cloud-init\\.log.*'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_cloud_init | default(true)
    | bool)
- block:
  - name: Check that the systemd-journal group is defined
    ansible.builtin.getent:
      database: group
      key: systemd-journal
    ignore_errors: true
    when: file_groupowner_var_log_journal_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the systemd-journal group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the systemd-journal group is defined'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_journal | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_journal_newgroup variable if systemd-journal found
    ansible.builtin.set_fact:
      file_groupowner_var_log_journal_newgroup: systemd-journal
    when: ansible_facts.getent_group["systemd-journal"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_journal_newgroup variable if systemd-journal
      found'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_journal_newgroup variable if systemd-journal found'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_journal | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_journal_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_journal | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_journal_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_journal_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_journal_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_journal_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_journal | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s) matching .*\.journal[~]? recursively
    ansible.builtin.command: find -P /var/log/  -type f  ! -group systemd-journal ! -group root -regextype posix-extended
      -regex ".*\.journal[~]?"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupowner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*\\.journal[~]? recursively'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*\\.journal[~]? recursively'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_journal | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/ file(s) matching .*\.journal[~]?
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_var_log_journal_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupowner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/ file(s) matching .*\\.journal[~]?'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/ file(s) matching .*\\.journal[~]?'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_journal | default(true)
    | bool)
- block:
  - name: Check that the utmp group is defined
    ansible.builtin.getent:
      database: group
      key: utmp
    ignore_errors: true
    when: file_groupowner_var_log_lastlog_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the utmp group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the utmp group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_lastlog_newgroup variable if utmp found
    ansible.builtin.set_fact:
      file_groupowner_var_log_lastlog_newgroup: utmp
    when: ansible_facts.getent_group["utmp"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_lastlog_newgroup variable if utmp found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_lastlog_newgroup variable if utmp found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_lastlog_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_lastlog_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_lastlog_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_lastlog_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_lastlog_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s) matching .*lastlog(\.[^\/]+)?
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -group utmp ! -group root -regextype posix-extended
      -regex ".*lastlog(\.[^\/]+)?"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupowner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/ file(s) matching .*lastlog(\.[^\/]+)?
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_var_log_lastlog_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupowner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Check that the adm group is defined
    ansible.builtin.getent:
      database: group
      key: adm
    ignore_errors: true
    when: file_groupowner_var_log_localmessages_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the adm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the adm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_localmessages_newgroup variable if adm found
    ansible.builtin.set_fact:
      file_groupowner_var_log_localmessages_newgroup: adm
    when: ansible_facts.getent_group["adm"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_localmessages_newgroup variable if adm found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_localmessages_newgroup variable if adm found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_localmessages_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_localmessages_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_localmessages_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_localmessages_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_localmessages_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s) matching .*localmessages.*
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -group adm ! -group root -regextype posix-extended -regex
      ".*localmessages.*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupowner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*localmessages.*'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*localmessages.*'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/ file(s) matching .*localmessages.*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_var_log_localmessages_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupowner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/ file(s) matching .*localmessages.*'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/ file(s) matching .*localmessages.*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_messages_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_var_log_messages_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_messages_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_messages_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_messages | default(true)
    | bool)
- block:
  - name: Test for existence /var/log/messages
    ansible.builtin.stat:
      path: /var/log/messages
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/messages'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/messages'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_messages | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/messages
    ansible.builtin.file:
      path: /var/log/messages
      follow: false
      group: '{{ file_groupowner_var_log_messages_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/messages'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/messages'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_messages | default(true)
    | bool)
- block:
  - name: Check that the adm group is defined
    ansible.builtin.getent:
      database: group
      key: adm
    ignore_errors: true
    when: file_groupowner_var_log_secure_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the adm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the adm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_secure | default(true) |
    bool)
- block:
  - name: Set the file_groupowner_var_log_secure_newgroup variable if adm found
    ansible.builtin.set_fact:
      file_groupowner_var_log_secure_newgroup: adm
    when: ansible_facts.getent_group["adm"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_secure_newgroup variable if adm found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_secure_newgroup variable if adm found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_secure | default(true) |
    bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_secure_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_secure | default(true) |
    bool)
- block:
  - name: Set the file_groupowner_var_log_secure_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_secure_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_secure_newgroup variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_secure_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_secure | default(true) |
    bool)
- block:
  - name: Find /var/log/ file(s) matching .*secure(.*[-\.].*)?
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -group adm ! -group root -regextype posix-extended -regex
      ".*secure(.*[-\.].*)?"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupowner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_secure | default(true) |
    bool)
- block:
  - name: Ensure group owner on /var/log/ file(s) matching .*secure(.*[-\.].*)?
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_var_log_secure_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupowner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_secure | default(true) |
    bool)
- block:
  - name: Set the file_groupowner_var_log_syslog_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_var_log_syslog_newgroup: '4'
    when: '"rsyslog" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232135
    - configure_strategy
    - file_groupowner_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_syslog_newgroup variable if represented by
      gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_syslog_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_syslog | default(true) |
    bool)
- block:
  - name: Test for existence /var/log/syslog
    ansible.builtin.stat:
      path: /var/log/syslog
    register: file_exists
    when: '"rsyslog" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232135
    - configure_strategy
    - file_groupowner_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/syslog'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_syslog | default(true) |
    bool)
- block:
  - name: Ensure group owner on /var/log/syslog
    ansible.builtin.file:
      path: /var/log/syslog
      follow: false
      group: '{{ file_groupowner_var_log_syslog_newgroup }}'
    when:
    - '"rsyslog" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232135
    - configure_strategy
    - file_groupowner_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/syslog'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_syslog | default(true) |
    bool)
- block:
  - name: Check that the adm group is defined
    ansible.builtin.getent:
      database: group
      key: adm
    ignore_errors: true
    when: file_groupowner_var_log_waagent_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the adm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the adm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_waagent | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_waagent_newgroup variable if adm found
    ansible.builtin.set_fact:
      file_groupowner_var_log_waagent_newgroup: adm
    when: ansible_facts.getent_group["adm"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_waagent_newgroup variable if adm found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_waagent_newgroup variable if adm found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_waagent | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_waagent_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_waagent | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_var_log_waagent_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_waagent_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_waagent_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_waagent_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_waagent | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s) matching .*waagent.log.*
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -group adm ! -group root -regextype posix-extended -regex
      ".*waagent.log.*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupowner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*waagent.log.*'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*waagent.log.*'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_waagent | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/ file(s) matching .*waagent.log.*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_var_log_waagent_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupowner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/ file(s) matching .*waagent.log.*'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/ file(s) matching .*waagent.log.*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_waagent | default(true)
    | bool)
- block:
  - name: Check that the utmp group is defined
    ansible.builtin.getent:
      database: group
      key: utmp
    ignore_errors: true
    when: file_groupowner_var_log_wbtmp_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the utmp group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the utmp group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Set the file_groupowner_var_log_wbtmp_newgroup variable if utmp found
    ansible.builtin.set_fact:
      file_groupowner_var_log_wbtmp_newgroup: utmp
    when: ansible_facts.getent_group["utmp"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_wbtmp_newgroup variable if utmp found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_wbtmp_newgroup variable if utmp found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupowner_var_log_wbtmp_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupowner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Set the file_groupowner_var_log_wbtmp_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupowner_var_log_wbtmp_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupowner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_var_log_wbtmp_newgroup variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_var_log_wbtmp_newgroup variable if root found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Find /var/log/ file(s) matching .*(b|w)tmp((\.|-)[^\/]+)?
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -group utmp ! -group root -regextype posix-extended
      -regex ".*(b|w)tmp((\.|-)[^\/]+)?"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupowner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Ensure group owner on /var/log/ file(s) matching .*(b|w)tmp((\.|-)[^\/]+)?
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_var_log_wbtmp_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupowner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Check that the adm group is defined
    ansible.builtin.getent:
      database: group
      key: adm
    ignore_errors: true
    when: file_groupownerships_var_log_apt_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the adm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the adm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_apt | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_apt_newgroup variable if adm found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_apt_newgroup: adm
    when: ansible_facts.getent_group["adm"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_apt_newgroup variable if adm found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_apt_newgroup variable if adm found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_apt | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupownerships_var_log_apt_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_apt | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_apt_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_apt_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_apt_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_apt_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_apt | default(true)
    | bool)
- block:
  - name: Find /var/log/apt/ file(s) matching .*
    ansible.builtin.command: find -P /var/log/apt/ -maxdepth 1 -type f  ! -group adm ! -group root -regextype posix-extended
      -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/apt/ file(s) matching .*'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/apt/ file(s) matching .*'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_apt | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/apt/ file(s) matching .*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownerships_var_log_apt_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/apt/ file(s) matching .*'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/apt/ file(s) matching .*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_apt | default(true)
    | bool)
- block:
  - name: Check that the gdm group is defined
    ansible.builtin.getent:
      database: group
      key: gdm
    ignore_errors: true
    when: file_groupownerships_var_log_gdm_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the gdm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the gdm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_gdm_newgroup variable if gdm found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_gdm_newgroup: gdm
    when: ansible_facts.getent_group["gdm"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_gdm_newgroup variable if gdm found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_gdm_newgroup variable if gdm found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupownerships_var_log_gdm_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_gdm_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_gdm_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_gdm_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_gdm_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm | default(true)
    | bool)
- block:
  - name: Find /var/log/gdm/ file(s) matching .* recursively
    ansible.builtin.command: find -P /var/log/gdm/  -type f  ! -group gdm ! -group root -regextype posix-extended -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/gdm/ file(s) matching .* recursively'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/gdm/ file(s) matching .* recursively'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/gdm/ file(s) matching .*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownerships_var_log_gdm_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/gdm/ file(s) matching .*'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/gdm/ file(s) matching .*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm | default(true)
    | bool)
- block:
  - name: Check that the gdm group is defined
    ansible.builtin.getent:
      database: group
      key: gdm
    ignore_errors: true
    when: file_groupownerships_var_log_gdm3_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the gdm group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the gdm group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_gdm3_newgroup variable if gdm found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_gdm3_newgroup: gdm
    when: ansible_facts.getent_group["gdm"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_gdm3_newgroup variable if gdm found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_gdm3_newgroup variable if gdm found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Check that the gdm3 group is defined
    ansible.builtin.getent:
      database: group
      key: gdm3
    ignore_errors: true
    when: file_groupownerships_var_log_gdm3_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the gdm3 group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the gdm3 group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_gdm3_newgroup variable if gdm3 found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_gdm3_newgroup: gdm3
    when: ansible_facts.getent_group["gdm3"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_gdm3_newgroup variable if gdm3 found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_gdm3_newgroup variable if gdm3 found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupownerships_var_log_gdm3_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_gdm3_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_gdm3_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_gdm3_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_gdm3_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Find /var/log/gdm3/ file(s) matching .* recursively
    ansible.builtin.command: find -P /var/log/gdm3/  -type f  ! -group gdm ! -group gdm3 ! -group root -regextype posix-extended
      -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/gdm3/ file(s) matching .* recursively'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/gdm3/ file(s) matching .* recursively'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/gdm3/ file(s) matching .*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownerships_var_log_gdm3_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/gdm3/ file(s) matching .*'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/gdm3/ file(s) matching .*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_gdm3 | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupownerships_var_log_landscape_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_landscape_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_landscape_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_landscape_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_landscape_newgroup variable if root found'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Check that the landscape group is defined
    ansible.builtin.getent:
      database: group
      key: landscape
    ignore_errors: true
    when: file_groupownerships_var_log_landscape_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the landscape group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the landscape group is defined'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_landscape_newgroup variable if landscape found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_landscape_newgroup: landscape
    when: ansible_facts.getent_group["landscape"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_landscape_newgroup variable if landscape
      found'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_landscape_newgroup variable if landscape found'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Find /var/log/landscape/ file(s) matching ^.*$
    ansible.builtin.command: find -P /var/log/landscape/ -maxdepth 1 -type f  ! -group root ! -group landscape -regextype
      posix-extended -regex "^.*$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/landscape/ file(s) matching ^.*$'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/landscape/ file(s) matching ^.*$'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/landscape/ file(s) matching ^.*$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownerships_var_log_landscape_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/landscape/ file(s) matching ^.*$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/landscape/ file(s) matching ^.*$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Check that the sssd group is defined
    ansible.builtin.getent:
      database: group
      key: sssd
    ignore_errors: true
    when: file_groupownerships_var_log_sssd_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the sssd group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the sssd group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_sssd | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_sssd_newgroup variable if sssd found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_sssd_newgroup: sssd
    when: ansible_facts.getent_group["sssd"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_sssd_newgroup variable if sssd found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_sssd_newgroup variable if sssd found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_sssd | default(true)
    | bool)
- block:
  - name: Check that the root group is defined
    ansible.builtin.getent:
      database: group
      key: root
    ignore_errors: true
    when: file_groupownerships_var_log_sssd_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root group is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_sssd | default(true)
    | bool)
- block:
  - name: Set the file_groupownerships_var_log_sssd_newgroup variable if root found
    ansible.builtin.set_fact:
      file_groupownerships_var_log_sssd_newgroup: root
    when: ansible_facts.getent_group["root"] is defined
    tags:
    - configure_strategy
    - file_groupownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownerships_var_log_sssd_newgroup variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownerships_var_log_sssd_newgroup variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_sssd | default(true)
    | bool)
- block:
  - name: Find /var/log/sssd/ file(s) matching .* recursively
    ansible.builtin.command: find -P /var/log/sssd/  -type f  ! -group sssd ! -group root -regextype posix-extended -regex
      ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_groupownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/sssd/ file(s) matching .* recursively'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/sssd/ file(s) matching .* recursively'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_sssd | default(true)
    | bool)
- block:
  - name: Ensure group owner on /var/log/sssd/ file(s) matching .*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownerships_var_log_sssd_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_groupownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /var/log/sssd/ file(s) matching .*'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /var/log/sssd/ file(s) matching .*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownerships_var_log_sssd | default(true)
    | bool)
- block:
  - name: Check that the syslog user is defined
    ansible.builtin.getent:
      database: passwd
      key: syslog
    ignore_errors: true
    tags:
    - configure_strategy
    - file_owner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the syslog user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the syslog user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_auth | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_auth_newown variable if syslog found
    ansible.builtin.set_fact:
      file_owner_var_log_auth_newown: syslog
    when: ansible_facts.getent_passwd["syslog"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_auth_newown variable if syslog found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_auth_newown variable if syslog found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_auth | default(true) | bool)
- block:
  - name: Check that the root user is defined
    ansible.builtin.getent:
      database: passwd
      key: root
    ignore_errors: true
    when: file_owner_var_log_auth_newown is undefined
    tags:
    - configure_strategy
    - file_owner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_auth | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_auth_newown variable if root found
    ansible.builtin.set_fact:
      file_owner_var_log_auth_newown: root
    when: ansible_facts.getent_passwd["root"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_auth_newown variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_auth_newown variable if root found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_auth | default(true) | bool)
- block:
  - name: Test for existence /var/log/auth.log
    ansible.builtin.stat:
      path: /var/log/auth.log
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/auth.log'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/auth.log'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_auth | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/auth.log
    ansible.builtin.file:
      path: /var/log/auth.log
      follow: false
      owner: '{{ file_owner_var_log_auth_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/auth.log'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/auth.log'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_auth | default(true) | bool)
- block:
  - name: Check that the syslog user is defined
    ansible.builtin.getent:
      database: passwd
      key: syslog
    ignore_errors: true
    tags:
    - configure_strategy
    - file_owner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the syslog user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the syslog user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_cloud_init | default(true) |
    bool)
- block:
  - name: Set the file_owner_var_log_cloud_init_newown variable if syslog found
    ansible.builtin.set_fact:
      file_owner_var_log_cloud_init_newown: syslog
    when: ansible_facts.getent_passwd["syslog"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_cloud_init_newown variable if syslog found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_cloud_init_newown variable if syslog found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_cloud_init | default(true) |
    bool)
- block:
  - name: Check that the root user is defined
    ansible.builtin.getent:
      database: passwd
      key: root
    ignore_errors: true
    when: file_owner_var_log_cloud_init_newown is undefined
    tags:
    - configure_strategy
    - file_owner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_cloud_init | default(true) |
    bool)
- block:
  - name: Set the file_owner_var_log_cloud_init_newown variable if root found
    ansible.builtin.set_fact:
      file_owner_var_log_cloud_init_newown: root
    when: ansible_facts.getent_passwd["root"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_cloud_init_newown variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_cloud_init_newown variable if root found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_cloud_init | default(true) |
    bool)
- block:
  - name: Find /var/log/ file(s) matching .*cloud-init\.log.*
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -user syslog ! -user root -regextype posix-extended
      -regex ".*cloud-init\.log.*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_owner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*cloud-init\\.log.*'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*cloud-init\\.log.*'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_cloud_init | default(true) |
    bool)
- block:
  - name: Ensure owner on /var/log/ file(s) matching .*cloud-init\.log.*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_var_log_cloud_init_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_owner_var_log_cloud_init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/ file(s) matching .*cloud-init\\.log.*'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/ file(s) matching .*cloud-init\\.log.*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_cloud_init | default(true) |
    bool)
- block:
  - name: Set the file_owner_var_log_journal_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_var_log_journal_newown: '0'
    tags:
    - configure_strategy
    - file_owner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_journal_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_journal_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_journal | default(true) | bool)
- block:
  - name: Find /var/log/ file(s) matching .*\.journal(~)?$ recursively
    ansible.builtin.command: find -P /var/log/  -type f  ! -user 0 -regextype posix-extended -regex ".*\.journal(~)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_owner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*\\.journal(~)?$ recursively'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*\\.journal(~)?$ recursively'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_journal | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/ file(s) matching .*\.journal(~)?$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_var_log_journal_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_owner_var_log_journal
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/ file(s) matching .*\\.journal(~)?$'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/ file(s) matching .*\\.journal(~)?$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_journal | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_lastlog_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_var_log_lastlog_newown: '0'
    tags:
    - configure_strategy
    - file_owner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_lastlog_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_lastlog_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_lastlog | default(true) | bool)
- block:
  - name: Find /var/log/ file(s) matching .*lastlog(\.[^\/]+)?$
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex ".*lastlog(\.[^\/]+)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_owner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_lastlog | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/ file(s) matching .*lastlog(\.[^\/]+)?$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_var_log_lastlog_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_owner_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/ file(s) matching .*lastlog(\\.[^\\/]+)?$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_lastlog | default(true) | bool)
- block:
  - name: Check that the syslog user is defined
    ansible.builtin.getent:
      database: passwd
      key: syslog
    ignore_errors: true
    tags:
    - configure_strategy
    - file_owner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the syslog user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the syslog user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Set the file_owner_var_log_localmessages_newown variable if syslog found
    ansible.builtin.set_fact:
      file_owner_var_log_localmessages_newown: syslog
    when: ansible_facts.getent_passwd["syslog"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_localmessages_newown variable if syslog found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_localmessages_newown variable if syslog found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Check that the root user is defined
    ansible.builtin.getent:
      database: passwd
      key: root
    ignore_errors: true
    when: file_owner_var_log_localmessages_newown is undefined
    tags:
    - configure_strategy
    - file_owner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Set the file_owner_var_log_localmessages_newown variable if root found
    ansible.builtin.set_fact:
      file_owner_var_log_localmessages_newown: root
    when: ansible_facts.getent_passwd["root"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_localmessages_newown variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_localmessages_newown variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s) matching .*localmessages.*
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -user syslog ! -user root -regextype posix-extended
      -regex ".*localmessages.*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_owner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*localmessages.*'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*localmessages.*'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Ensure owner on /var/log/ file(s) matching .*localmessages.*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_var_log_localmessages_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_owner_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/ file(s) matching .*localmessages.*'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/ file(s) matching .*localmessages.*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Set the file_owner_var_log_messages_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_var_log_messages_newown: '0'
    tags:
    - configure_strategy
    - file_owner_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_messages_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_messages_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_messages | default(true) | bool)
- block:
  - name: Test for existence /var/log/messages
    ansible.builtin.stat:
      path: /var/log/messages
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/messages'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/messages'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_messages | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/messages
    ansible.builtin.file:
      path: /var/log/messages
      follow: false
      owner: '{{ file_owner_var_log_messages_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/messages'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/messages'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_messages | default(true) | bool)
- block:
  - name: Check that the syslog user is defined
    ansible.builtin.getent:
      database: passwd
      key: syslog
    ignore_errors: true
    tags:
    - configure_strategy
    - file_owner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the syslog user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the syslog user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_secure | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_secure_newown variable if syslog found
    ansible.builtin.set_fact:
      file_owner_var_log_secure_newown: syslog
    when: ansible_facts.getent_passwd["syslog"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_secure_newown variable if syslog found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_secure_newown variable if syslog found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_secure | default(true) | bool)
- block:
  - name: Check that the root user is defined
    ansible.builtin.getent:
      database: passwd
      key: root
    ignore_errors: true
    when: file_owner_var_log_secure_newown is undefined
    tags:
    - configure_strategy
    - file_owner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_secure | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_secure_newown variable if root found
    ansible.builtin.set_fact:
      file_owner_var_log_secure_newown: root
    when: ansible_facts.getent_passwd["root"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_secure_newown variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_secure_newown variable if root found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_secure | default(true) | bool)
- block:
  - name: Find /var/log/ file(s) matching .*secure(.*[-\.].*)?
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -user syslog ! -user root -regextype posix-extended
      -regex ".*secure(.*[-\.].*)?"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_owner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_secure | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/ file(s) matching .*secure(.*[-\.].*)?
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_var_log_secure_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_owner_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/ file(s) matching .*secure(.*[-\\.].*)?'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_secure | default(true) | bool)
- block:
  - name: Check that the syslog user is defined
    ansible.builtin.getent:
      database: passwd
      key: syslog
    ignore_errors: true
    when: '"rsyslog" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232130
    - configure_strategy
    - file_owner_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the syslog user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the syslog user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_syslog | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_syslog_newown variable if syslog found
    ansible.builtin.set_fact:
      file_owner_var_log_syslog_newown: syslog
    when:
    - '"rsyslog" in ansible_facts.packages'
    - ansible_facts.getent_passwd["syslog"] is defined
    tags:
    - DISA-STIG-UBTU-22-232130
    - configure_strategy
    - file_owner_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_syslog_newown variable if syslog found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_syslog_newown variable if syslog found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_syslog | default(true) | bool)
- block:
  - name: Test for existence /var/log/syslog
    ansible.builtin.stat:
      path: /var/log/syslog
    register: file_exists
    when: '"rsyslog" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232130
    - configure_strategy
    - file_owner_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/syslog'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_syslog | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/syslog
    ansible.builtin.file:
      path: /var/log/syslog
      follow: false
      owner: '{{ file_owner_var_log_syslog_newown }}'
    when:
    - '"rsyslog" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232130
    - configure_strategy
    - file_owner_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/syslog'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_syslog | default(true) | bool)
- block:
  - name: Check that the syslog user is defined
    ansible.builtin.getent:
      database: passwd
      key: syslog
    ignore_errors: true
    tags:
    - configure_strategy
    - file_owner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the syslog user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the syslog user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_waagent | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_waagent_newown variable if syslog found
    ansible.builtin.set_fact:
      file_owner_var_log_waagent_newown: syslog
    when: ansible_facts.getent_passwd["syslog"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_waagent_newown variable if syslog found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_waagent_newown variable if syslog found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_waagent | default(true) | bool)
- block:
  - name: Check that the root user is defined
    ansible.builtin.getent:
      database: passwd
      key: root
    ignore_errors: true
    when: file_owner_var_log_waagent_newown is undefined
    tags:
    - configure_strategy
    - file_owner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_waagent | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_waagent_newown variable if root found
    ansible.builtin.set_fact:
      file_owner_var_log_waagent_newown: root
    when: ansible_facts.getent_passwd["root"] is defined
    tags:
    - configure_strategy
    - file_owner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_waagent_newown variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_waagent_newown variable if root found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_waagent | default(true) | bool)
- block:
  - name: Find /var/log/ file(s) matching .*waagent.log.*
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -user syslog ! -user root -regextype posix-extended
      -regex ".*waagent.log.*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_owner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*waagent.log.*'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*waagent.log.*'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_waagent | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/ file(s) matching .*waagent.log.*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_var_log_waagent_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_owner_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/ file(s) matching .*waagent.log.*'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/ file(s) matching .*waagent.log.*'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_waagent | default(true) | bool)
- block:
  - name: Set the file_owner_var_log_wbtmp_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_var_log_wbtmp_newown: '0'
    tags:
    - configure_strategy
    - file_owner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_var_log_wbtmp_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_var_log_wbtmp_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_wbtmp | default(true) | bool)
- block:
  - name: Find /var/log/ file(s) matching .*(b|w)tmp((\.|-)[^\/]+)?$
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex ".*(b|w)tmp((\.|-)[^\/]+)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_owner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?$'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_wbtmp | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/ file(s) matching .*(b|w)tmp((\.|-)[^\/]+)?$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_var_log_wbtmp_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_owner_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/ file(s) matching .*(b|w)tmp((\\.|-)[^\\/]+)?$'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_var_log_wbtmp | default(true) | bool)
- block:
  - name: Set the file_ownerships_var_log_apt_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownerships_var_log_apt_newown: '0'
    tags:
    - configure_strategy
    - file_ownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownerships_var_log_apt_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownerships_var_log_apt_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_apt | default(true) | bool)
- block:
  - name: Find /var/log/apt/ file(s) matching ^.*$
    ansible.builtin.command: find -P /var/log/apt/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_ownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/apt/ file(s) matching ^.*$'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/apt/ file(s) matching ^.*$'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_apt | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/apt/ file(s) matching ^.*$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownerships_var_log_apt_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_ownerships_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/apt/ file(s) matching ^.*$'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/apt/ file(s) matching ^.*$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_apt | default(true) | bool)
- block:
  - name: Set the file_ownerships_var_log_gdm_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownerships_var_log_gdm_newown: '0'
    tags:
    - configure_strategy
    - file_ownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownerships_var_log_gdm_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownerships_var_log_gdm_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_gdm | default(true) | bool)
- block:
  - name: Find /var/log/gdm/ file(s) matching .* recursively
    ansible.builtin.command: find -P /var/log/gdm/  -type f  ! -user 0 -regextype posix-extended -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_ownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/gdm/ file(s) matching .* recursively'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/gdm/ file(s) matching .* recursively'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_gdm | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/gdm/ file(s) matching .*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownerships_var_log_gdm_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_ownerships_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/gdm/ file(s) matching .*'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/gdm/ file(s) matching .*'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_gdm | default(true) | bool)
- block:
  - name: Set the file_ownerships_var_log_gdm3_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownerships_var_log_gdm3_newown: '0'
    tags:
    - configure_strategy
    - file_ownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownerships_var_log_gdm3_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownerships_var_log_gdm3_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_gdm3 | default(true) | bool)
- block:
  - name: Find /var/log/gdm3/ file(s) matching .* recursively
    ansible.builtin.command: find -P /var/log/gdm3/  -type f  ! -user 0 -regextype posix-extended -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_ownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/gdm3/ file(s) matching .* recursively'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/gdm3/ file(s) matching .* recursively'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_gdm3 | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/gdm3/ file(s) matching .*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownerships_var_log_gdm3_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_ownerships_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/gdm3/ file(s) matching .*'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/gdm3/ file(s) matching .*'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_gdm3 | default(true) | bool)
- block:
  - name: Check that the root user is defined
    ansible.builtin.getent:
      database: passwd
      key: root
    ignore_errors: true
    tags:
    - configure_strategy
    - file_ownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Set the file_ownerships_var_log_landscape_newown variable if root found
    ansible.builtin.set_fact:
      file_ownerships_var_log_landscape_newown: root
    when: ansible_facts.getent_passwd["root"] is defined
    tags:
    - configure_strategy
    - file_ownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownerships_var_log_landscape_newown variable if root found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownerships_var_log_landscape_newown variable if root found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Check that the landscape user is defined
    ansible.builtin.getent:
      database: passwd
      key: landscape
    ignore_errors: true
    when: file_ownerships_var_log_landscape_newown is undefined
    tags:
    - configure_strategy
    - file_ownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the landscape user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the landscape user is defined'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Set the file_ownerships_var_log_landscape_newown variable if landscape found
    ansible.builtin.set_fact:
      file_ownerships_var_log_landscape_newown: landscape
    when: ansible_facts.getent_passwd["landscape"] is defined
    tags:
    - configure_strategy
    - file_ownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownerships_var_log_landscape_newown variable if landscape found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownerships_var_log_landscape_newown variable if landscape found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Find /var/log/landscape/ file(s) matching ^.*$
    ansible.builtin.command: find -P /var/log/landscape/ -maxdepth 1 -type f  ! -user root ! -user landscape -regextype posix-extended
      -regex "^.*$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_ownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/landscape/ file(s) matching ^.*$'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/landscape/ file(s) matching ^.*$'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Ensure owner on /var/log/landscape/ file(s) matching ^.*$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownerships_var_log_landscape_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_ownerships_var_log_landscape
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/landscape/ file(s) matching ^.*$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/landscape/ file(s) matching ^.*$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_landscape | default(true)
    | bool)
- block:
  - name: Check that the sssd user is defined
    ansible.builtin.getent:
      database: passwd
      key: sssd
    ignore_errors: true
    tags:
    - configure_strategy
    - file_ownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the sssd user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the sssd user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_sssd | default(true) | bool)
- block:
  - name: Set the file_ownerships_var_log_sssd_newown variable if sssd found
    ansible.builtin.set_fact:
      file_ownerships_var_log_sssd_newown: sssd
    when: ansible_facts.getent_passwd["sssd"] is defined
    tags:
    - configure_strategy
    - file_ownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownerships_var_log_sssd_newown variable if sssd found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownerships_var_log_sssd_newown variable if sssd found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_sssd | default(true) | bool)
- block:
  - name: Check that the root user is defined
    ansible.builtin.getent:
      database: passwd
      key: root
    ignore_errors: true
    when: file_ownerships_var_log_sssd_newown is undefined
    tags:
    - configure_strategy
    - file_ownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the root user is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the root user is defined'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_sssd | default(true) | bool)
- block:
  - name: Set the file_ownerships_var_log_sssd_newown variable if root found
    ansible.builtin.set_fact:
      file_ownerships_var_log_sssd_newown: root
    when: ansible_facts.getent_passwd["root"] is defined
    tags:
    - configure_strategy
    - file_ownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownerships_var_log_sssd_newown variable if root found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownerships_var_log_sssd_newown variable if root found'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_sssd | default(true) | bool)
- block:
  - name: Find /var/log/sssd/ file(s) matching .* recursively
    ansible.builtin.command: find -P /var/log/sssd/  -type f  ! -user sssd ! -user root -regextype posix-extended -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_ownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/sssd/ file(s) matching .* recursively'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/sssd/ file(s) matching .* recursively'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_sssd | default(true) | bool)
- block:
  - name: Ensure owner on /var/log/sssd/ file(s) matching .*
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownerships_var_log_sssd_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_ownerships_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /var/log/sssd/ file(s) matching .*'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /var/log/sssd/ file(s) matching .*'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownerships_var_log_sssd | default(true) | bool)
- block:
  - name: Find /var/log/apt/ file(s)
    ansible.builtin.command: find -P /var/log/apt/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended
      -regex "^.*$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/apt/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/apt/ file(s)'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_apt | default(true) | bool)
- block:
  - name: Set permissions for /var/log/apt/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_apt
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/apt/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/apt/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_apt | default(true) | bool)
- block:
  - name: Test for existence /var/log/auth.log
    ansible.builtin.stat:
      path: /var/log/auth.log
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/auth.log'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/auth.log'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_auth | default(true) |
    bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /var/log/auth.log
    ansible.builtin.file:
      path: /var/log/auth.log
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_var_log_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /var/log/auth.log'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /var/log/auth.log'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_auth | default(true) |
    bool)
- block:
  - name: Find /var/log/ file(s)
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended -regex
      ".*cloud-init.log([^\/]+)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_cloud-init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Set permissions for /var/log/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_cloud-init
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Find /var/log/gdm/ file(s) recursively
    ansible.builtin.command: find -P /var/log/gdm/  -perm /u+xs,g+xs,o+xwrt  -type f -regextype posix-extended -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/gdm/ file(s) recursively'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/gdm/ file(s) recursively'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_gdm | default(true) | bool)
- block:
  - name: Set permissions for /var/log/gdm/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xs,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_gdm
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/gdm/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/gdm/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_gdm | default(true) | bool)
- block:
  - name: Find /var/log/gdm3/ file(s) recursively
    ansible.builtin.command: find -P /var/log/gdm3/  -perm /u+xs,g+xs,o+xwrt  -type f -regextype posix-extended -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/gdm3/ file(s) recursively'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/gdm3/ file(s) recursively'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_gdm3 | default(true) |
    bool)
- block:
  - name: Set permissions for /var/log/gdm3/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xs,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_gdm3
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/gdm3/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/gdm3/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_gdm3 | default(true) |
    bool)
- block:
  - name: Find /var/log/ file(s)
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -perm /u+xs,g+xs,o+xwt  -type f -regextype posix-extended -regex
      ".*lastlog(\.[^\/]+)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Set permissions for /var/log/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xs,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_lastlog | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s)
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended -regex
      ".*localmessages([^\/]+)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Set permissions for /var/log/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_localmessages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_localmessages | default(true)
    | bool)
- block:
  - name: Test for existence /var/log/messages
    ansible.builtin.stat:
      path: /var/log/messages
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/messages'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/messages'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_messages | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /var/log/messages
    ansible.builtin.file:
      path: /var/log/messages
      mode: u-xs,g-xwrs,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_var_log_messages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /var/log/messages'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /var/log/messages'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_messages | default(true)
    | bool)
- block:
  - name: Test for existence /var/log/secure
    ansible.builtin.stat:
      path: /var/log/secure
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/secure'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/secure'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_secure | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /var/log/secure
    ansible.builtin.file:
      path: /var/log/secure
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_var_log_secure
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /var/log/secure'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /var/log/secure'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_secure | default(true)
    | bool)
- block:
  - name: Find /var/log/sssd/ file(s)
    ansible.builtin.command: find -P /var/log/sssd/ -maxdepth 1 -perm /u+xs,g+xs,o+xwrt  -type f -regextype posix-extended
      -regex ".*"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/sssd/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/sssd/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_sssd | default(true) |
    bool)
- block:
  - name: Set permissions for /var/log/sssd/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xs,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_sssd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/sssd/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/sssd/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_sssd | default(true) |
    bool)
- block:
  - name: Test for existence /var/log/syslog
    ansible.builtin.stat:
      path: /var/log/syslog
    register: file_exists
    tags:
    - DISA-STIG-UBTU-22-232030
    - configure_strategy
    - file_permissions_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /var/log/syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /var/log/syslog'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_syslog | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /var/log/syslog
    ansible.builtin.file:
      path: /var/log/syslog
      mode: u-xs,g-xws,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232030
    - configure_strategy
    - file_permissions_var_log_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /var/log/syslog'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /var/log/syslog'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_syslog | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s)
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended -regex
      ".*waagent.log([^\/]+)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_waagent | default(true)
    | bool)
- block:
  - name: Set permissions for /var/log/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_waagent
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_waagent | default(true)
    | bool)
- block:
  - name: Find /var/log/ file(s)
    ansible.builtin.command: find -P /var/log/ -maxdepth 1 -perm /u+xs,g+xs,o+xwt  -type f -regextype posix-extended -regex
      ".*(b|w)tmp((\.|-)[^\/]+)?$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - configure_strategy
    - file_permissions_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Set permissions for /var/log/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xs,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    tags:
    - configure_strategy
    - file_permissions_var_log_wbtmp
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /var/log/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /var/log/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_wbtmp | default(true) |
    bool)
- block:
  - name: Ensure kernel module 'cramfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/cramfs.conf
      regexp: install\s+cramfs
      line: install cramfs /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_cramfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''cramfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''cramfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_cramfs_disabled | default(true) |
    bool)
- block:
  - name: Ensure kernel module 'cramfs' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/cramfs.conf
      regexp: ^blacklist cramfs$
      line: blacklist cramfs
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_cramfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''cramfs'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''cramfs'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_cramfs_disabled | default(true) |
    bool)
- block:
  - name: Ensure kernel module 'freevxfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/freevxfs.conf
      regexp: install\s+freevxfs
      line: install freevxfs /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_freevxfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''freevxfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''freevxfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_freevxfs_disabled | default(true)
    | bool)
- block:
  - name: Ensure kernel module 'freevxfs' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/freevxfs.conf
      regexp: ^blacklist freevxfs$
      line: blacklist freevxfs
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_freevxfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''freevxfs'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''freevxfs'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_freevxfs_disabled | default(true)
    | bool)
- block:
  - name: Ensure kernel module 'hfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/hfs.conf
      regexp: install\s+hfs
      line: install hfs /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_hfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''hfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''hfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_hfs_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'hfs' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/hfs.conf
      regexp: ^blacklist hfs$
      line: blacklist hfs
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_hfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''hfs'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''hfs'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_hfs_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'hfsplus' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/hfsplus.conf
      regexp: install\s+hfsplus
      line: install hfsplus /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_hfsplus_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''hfsplus'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''hfsplus'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_hfsplus_disabled | default(true) |
    bool)
- block:
  - name: Ensure kernel module 'hfsplus' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/hfsplus.conf
      regexp: ^blacklist hfsplus$
      line: blacklist hfsplus
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_hfsplus_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''hfsplus'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''hfsplus'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_hfsplus_disabled | default(true) |
    bool)
- block:
  - name: Ensure kernel module 'jffs2' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/jffs2.conf
      regexp: install\s+jffs2
      line: install jffs2 /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_jffs2_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''jffs2'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''jffs2'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_jffs2_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'jffs2' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/jffs2.conf
      regexp: ^blacklist jffs2$
      line: blacklist jffs2
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_jffs2_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''jffs2'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''jffs2'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_jffs2_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'squashfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/squashfs.conf
      regexp: install\s+squashfs
      line: install squashfs /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_squashfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''squashfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''squashfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_squashfs_disabled | default(true)
    | bool)
- block:
  - name: Ensure kernel module 'squashfs' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/squashfs.conf
      regexp: ^blacklist squashfs$
      line: blacklist squashfs
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_squashfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''squashfs'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''squashfs'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_squashfs_disabled | default(true)
    | bool)
- block:
  - name: Ensure kernel module 'udf' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/udf.conf
      regexp: install\s+udf
      line: install udf /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_udf_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''udf'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''udf'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_udf_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'udf' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/udf.conf
      regexp: ^blacklist udf$
      line: blacklist udf
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_udf_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''udf'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''udf'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_kernel_module_udf_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'usb-storage' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/usb-storage.conf
      regexp: install\s+usb-storage
      line: install usb-storage /bin/false
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-291010
    - NIST-800-171-3.1.21
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - PCI-DSSv4-3.4
    - PCI-DSSv4-3.4.2
    - disable_strategy
    - kernel_module_usb-storage_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''usb-storage'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''usb-storage'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure kernel module 'usb-storage' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/usb-storage.conf
      regexp: ^blacklist usb-storage$
      line: blacklist usb-storage
      mode: 420
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-291010
    - NIST-800-171-3.1.21
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - PCI-DSSv4-3.4
    - PCI-DSSv4-3.4.2
    - disable_strategy
    - kernel_module_usb-storage_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''usb-storage'' is blacklisted" (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''usb-storage'' is blacklisted" raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /dev/shm: Check information associated to mountpoint'
    ansible.builtin.command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when: not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined and "nodev" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option'
    ansible.posix.mount:
      path: /dev/shm
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Check information associated to mountpoint'
    ansible.builtin.command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when: not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined and "noexec" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option'
    ansible.posix.mount:
      path: /dev/shm
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Check information associated to mountpoint'
    ansible.builtin.command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when: not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined and "nosuid" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option'
    ansible.posix.mount:
      path: /dev/shm
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/home'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: If /home not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /home
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: If /home not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: If /home not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Make sure nodev option is part of the to /home options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in (mount_info.options | default(''))
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Make sure nodev option is part of the to /home
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Make sure nodev option is part of the to /home options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Ensure /home is mounted with nodev option'
    ansible.posix.mount:
      path: /home
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Ensure /home is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Ensure /home is mounted with nodev option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/home'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: If /home not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /home
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: If /home not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: If /home not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Make sure nosuid option is part of the to /home options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Make sure nosuid option is part of the to /home
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Make sure nosuid option is part of the to /home options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Ensure /home is mounted with nosuid option'
    ansible.posix.mount:
      path: /home
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Ensure /home is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Ensure /home is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'
    ansible.posix.mount:
      path: /tmp
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'
    ansible.posix.mount:
      path: /tmp
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'
    ansible.posix.mount:
      path: /tmp
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nodev | default(true)
    | bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nodev | default(true)
    | bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info
      manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nodev | default(true)
    | bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Make sure nodev option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Make sure nodev option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Make sure nodev option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nodev | default(true)
    | bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with nodev option'
    ansible.posix.mount:
      path: /var/log/audit
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with
      nodev option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with nodev option'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nodev | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft
      mount_info manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Make sure noexec option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Make sure noexec option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Make sure noexec option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with noexec option'
    ansible.posix.mount:
      path: /var/log/audit
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with
      noexec option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with noexec option'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft
      mount_info manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with nosuid option'
    ansible.posix.mount:
      path: /var/log/audit
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with
      nosuid option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with nosuid option'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nodev Option to /var/log: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option'
    ansible.posix.mount:
      path: /var/log
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option'
    ansible.posix.mount:
      path: /var/log
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option'
    ansible.posix.mount:
      path: /var/log
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: If /var not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: If /var not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: If /var not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Make sure nodev option is part of the to /var options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in (mount_info.options | default(''))
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Make sure nodev option is part of the to /var options''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Make sure nodev option is part of the to /var options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Ensure /var is mounted with nodev option'
    ansible.posix.mount:
      path: /var
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Ensure /var is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Ensure /var is mounted with nodev option'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: If /var not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: If /var not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: If /var not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Make sure nosuid option is part of the to /var options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in (mount_info.options | default(''))
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Make sure nosuid option is part of the to /var
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Make sure nosuid option is part of the to /var options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Ensure /var is mounted with nosuid option'
    ansible.posix.mount:
      path: /var
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Ensure /var is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Ensure /var is mounted with nosuid option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nodev'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in (mount_info.options | default(''))
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option'
    ansible.posix.mount:
      path: /var/tmp
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''noexec'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in (mount_info.options | default(''))
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option'
    ansible.posix.mount:
      path: /var/tmp
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Check information associated to mountpoint'
    ansible.builtin.command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    check_mode: false
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | map(''lower'') | list }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~(mount_info.options | default(''''))~('','' if (mount_info.options
        | default('''')) else '''')~''nosuid'' }) }}'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in (mount_info.options | default(''))
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option'
    ansible.posix.mount:
      path: /var/tmp
      src: '{{ mount_info.source | default('''') }}'
      opts: '{{ mount_info.options | default('''') }}'
      state: mounted
      fstype: '{{ mount_info.fstype | default('''') }}'
    register: mount_result
    failed_when:
    - mount_result is failed
    - '''target is busy'' not in (mount_result.msg | default(''''))'
    - '''already mounted'' not in (mount_result.msg | default(''''))'
    when:
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: Restrict usage of ptrace to descendant processes - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Restrict usage of ptrace to descendant processes - Set fact for sysctl paths''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict usage of ptrace to descendant processes - Set fact for sysctl paths'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_yama_ptrace_scope | default(true)
    | bool)
- block:
  - name: Restrict usage of ptrace to descendant processes - Find all files that contain kernel.yama.ptrace_scope
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.yama.ptrace_scope\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Restrict usage of ptrace to descendant processes - Find all files that contain
      kernel.yama.ptrace_scope'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict usage of ptrace to descendant processes - Find all files that contain kernel.yama.ptrace_scope''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_yama_ptrace_scope | default(true)
    | bool)
- block:
  - name: Restrict usage of ptrace to descendant processes - Find all files that set kernel.yama.ptrace_scope to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.yama.ptrace_scope\s*=\s*1$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Restrict usage of ptrace to descendant processes - Find all files that set
      kernel.yama.ptrace_scope to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict usage of ptrace to descendant processes - Find all files that set kernel.yama.ptrace_scope
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_yama_ptrace_scope | default(true)
    | bool)
- block:
  - name: Restrict usage of ptrace to descendant processes - Comment out any occurrences of kernel.yama.ptrace_scope from
      config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*kernel.yama.ptrace_scope
      replace: '#kernel.yama.ptrace_scope'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Restrict usage of ptrace to descendant processes - Comment out any occurrences
      of kernel.yama.ptrace_scope from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict usage of ptrace to descendant processes - Comment out any occurrences of kernel.yama.ptrace_scope
        from config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_yama_ptrace_scope | default(true)
    | bool)
- block:
  - name: Restrict usage of ptrace to descendant processes - Comment out any occurrences of kernel.yama.ptrace_scope from
      /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*kernel.yama.ptrace_scope.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Restrict usage of ptrace to descendant processes - Comment out any occurrences
      of kernel.yama.ptrace_scope from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict usage of ptrace to descendant processes - Comment out any occurrences of kernel.yama.ptrace_scope
        from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_yama_ptrace_scope | default(true)
    | bool)
- block:
  - name: Restrict usage of ptrace to descendant processes - Ensure sysctl kernel.yama.ptrace_scope is set to 1
    ansible.posix.sysctl:
      name: kernel.yama.ptrace_scope
      value: '1'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Restrict usage of ptrace to descendant processes - Ensure sysctl kernel.yama.ptrace_scope
      is set to 1'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict usage of ptrace to descendant processes - Ensure sysctl kernel.yama.ptrace_scope is set
        to 1'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_yama_ptrace_scope | default(true)
    | bool)
- block:
  - name: Disable Core Dumps for All Users - Set dirs, files and regex variables
    ansible.builtin.set_fact:
      limits_dropin_dir: /etc/security/limits.d
      limits_dropin_file: /etc/security/limits.d/10-ssg-hardening.conf
      limits_main_file: /etc/security/limits.conf
      limits_correct_regex: ^\s*\*\s+hard\s+core\s+0\s*$
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Set dirs, files and regex variables'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Set dirs, files and regex variables'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Find valid drop-ins for core limit
    ansible.builtin.find:
      paths: '{{ limits_dropin_dir }}'
      patterns: '*.conf'
      contains: '{{ limits_correct_regex }}'
      file_type: file
    register: valid_dropins
    failed_when: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Find valid drop-ins for core limit'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Find valid drop-ins for core limit'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Find all drop-ins with any core limit
    ansible.builtin.find:
      paths: '{{ limits_dropin_dir }}'
      patterns: '*.conf'
      contains: ^\s*\*\s+hard\s+core\s+
      file_type: file
    register: all_dropins
    failed_when: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Find all drop-ins with any core limit''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Find all drop-ins with any core limit'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Get invalid drop-ins
    ansible.builtin.set_fact:
      invalid_dropins: '{{ all_dropins.files | rejectattr(''path'', ''in'', valid_dropins.files | map(attribute=''path'')
        | list) | map(attribute=''path'') | list }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Get invalid drop-ins'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Get invalid drop-ins'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Comment invalid * hard core lines in drop-ins
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: (^\s*\*\s+hard\s+core\s+.*$)
      replace: '#\1'
    loop: '{{ invalid_dropins }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    - invalid_dropins | length > 0
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Comment invalid * hard core lines in drop-ins''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Comment invalid * hard core lines in drop-ins'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Check if main limits.conf contains correct core limit
    ansible.builtin.find:
      paths: /etc/security
      patterns: limits.conf
      contains: '{{ limits_correct_regex }}'
      file_type: file
    register: main_valid
    failed_when: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    - not (valid_dropins.matched | default(0) > 0)
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Check if main limits.conf contains correct
      core limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Check if main limits.conf contains correct core limit'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Set fact if configuration is valid
    ansible.builtin.set_fact:
      core_limit_valid: '{{ (valid_dropins.matched | default(0)) > 0 or (main_valid.matched | default(0)) > 0 }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Set fact if configuration is valid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Set fact if configuration is valid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Ensure drop-in directory exists
    ansible.builtin.file:
      path: '{{ limits_dropin_dir }}'
      state: directory
      mode: '0755'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    - not core_limit_valid
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Ensure drop-in directory exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Ensure drop-in directory exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for All Users - Deploy 10-ssg-hardening.conf drop-in with correct core limit
    ansible.builtin.copy:
      dest: '{{ limits_dropin_file }}'
      content: '*     hard   core    0

        '
      mode: '0644'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"libpam-runtime" in ansible_facts.packages'
    - not core_limit_valid
    tags:
    - NIST-800-53-CM-6
    - NIST-800-53-SC-7(10)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_users_coredumps
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for All Users - Deploy 10-ssg-hardening.conf drop-in with
      correct core limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for All Users - Deploy 10-ssg-hardening.conf drop-in with correct core limit''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_users_coredumps | default(true) | bool)
- block:
  - name: Disable Core Dumps for SUID programs - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for SUID programs - Set fact for sysctl paths'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for SUID programs - Set fact for sysctl paths'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_fs_suid_dumpable | default(true) | bool)
- block:
  - name: Disable Core Dumps for SUID programs - Find all files that contain fs.suid_dumpable
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*fs.suid_dumpable\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for SUID programs - Find all files that contain fs.suid_dumpable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for SUID programs - Find all files that contain fs.suid_dumpable'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_fs_suid_dumpable | default(true) | bool)
- block:
  - name: Disable Core Dumps for SUID programs - Find all files that set fs.suid_dumpable to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*fs.suid_dumpable\s*=\s*0$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for SUID programs - Find all files that set fs.suid_dumpable
      to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for SUID programs - Find all files that set fs.suid_dumpable to correct value''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_fs_suid_dumpable | default(true) | bool)
- block:
  - name: Disable Core Dumps for SUID programs - Comment out any occurrences of fs.suid_dumpable from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*fs.suid_dumpable
      replace: '#fs.suid_dumpable'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for SUID programs - Comment out any occurrences of fs.suid_dumpable
      from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for SUID programs - Comment out any occurrences of fs.suid_dumpable from config
        files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_fs_suid_dumpable | default(true) | bool)
- block:
  - name: Disable Core Dumps for SUID programs - Comment out any occurrences of fs.suid_dumpable from /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*fs.suid_dumpable.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for SUID programs - Comment out any occurrences of fs.suid_dumpable
      from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for SUID programs - Comment out any occurrences of fs.suid_dumpable from /etc/ufw/sysctl.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_fs_suid_dumpable | default(true) | bool)
- block:
  - name: Disable Core Dumps for SUID programs - Ensure sysctl fs.suid_dumpable is set to 0
    ansible.posix.sysctl:
      name: fs.suid_dumpable
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_fs_suid_dumpable
  rescue:
  - name: 'StigForge: continue after error in ''Disable Core Dumps for SUID programs - Ensure sysctl fs.suid_dumpable is set
      to 0'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Core Dumps for SUID programs - Ensure sysctl fs.suid_dumpable is set to 0'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_fs_suid_dumpable | default(true) | bool)
- block:
  - name: Enable Randomized Layout of Virtual Address Space - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Enable Randomized Layout of Virtual Address Space - Set fact for sysctl paths''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Randomized Layout of Virtual Address Space - Set fact for sysctl paths'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_randomize_va_space | default(true)
    | bool)
- block:
  - name: Enable Randomized Layout of Virtual Address Space - Find all files that contain kernel.randomize_va_space
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.randomize_va_space\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Enable Randomized Layout of Virtual Address Space - Find all files that contain
      kernel.randomize_va_space'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Randomized Layout of Virtual Address Space - Find all files that contain kernel.randomize_va_space''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_randomize_va_space | default(true)
    | bool)
- block:
  - name: Enable Randomized Layout of Virtual Address Space - Find all files that set kernel.randomize_va_space to correct
      value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.randomize_va_space\s*=\s*2$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Enable Randomized Layout of Virtual Address Space - Find all files that set
      kernel.randomize_va_space to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Randomized Layout of Virtual Address Space - Find all files that set kernel.randomize_va_space
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_randomize_va_space | default(true)
    | bool)
- block:
  - name: Enable Randomized Layout of Virtual Address Space - Comment out any occurrences of kernel.randomize_va_space from
      config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*kernel.randomize_va_space
      replace: '#kernel.randomize_va_space'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Enable Randomized Layout of Virtual Address Space - Comment out any occurrences
      of kernel.randomize_va_space from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Randomized Layout of Virtual Address Space - Comment out any occurrences of kernel.randomize_va_space
        from config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_randomize_va_space | default(true)
    | bool)
- block:
  - name: Enable Randomized Layout of Virtual Address Space - Comment out any occurrences of kernel.randomize_va_space from
      /etc/ufw/sysctl.conf
    ansible.builtin.replace:
      path: /etc/ufw/sysctl.conf
      regexp: (^[\s]*kernel.randomize_va_space.*$)
      replace: '# \1'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Enable Randomized Layout of Virtual Address Space - Comment out any occurrences
      of kernel.randomize_va_space from /etc/ufw/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Randomized Layout of Virtual Address Space - Comment out any occurrences of kernel.randomize_va_space
        from /etc/ufw/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_randomize_va_space | default(true)
    | bool)
- block:
  - name: Enable Randomized Layout of Virtual Address Space - Ensure sysctl kernel.randomize_va_space is set to 2
    ansible.posix.sysctl:
      name: kernel.randomize_va_space
      value: '2'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-213020
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Enable Randomized Layout of Virtual Address Space - Ensure sysctl kernel.randomize_va_space
      is set to 2'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable Randomized Layout of Virtual Address Space - Ensure sysctl kernel.randomize_va_space is set
        to 2'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sysctl_kernel_randomize_va_space | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_cron_d_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_d_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_d_newgroup variable if represented by gid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_d_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_d | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.d/
    ansible.builtin.file:
      path: /etc/cron.d/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_d_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.d/'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_d | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_daily_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_daily_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_daily_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_daily_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_daily | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.daily/
    ansible.builtin.file:
      path: /etc/cron.daily/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_daily_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.daily/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.daily/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_daily | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_hourly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_hourly_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_hourly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_hourly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_hourly | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.hourly/
    ansible.builtin.file:
      path: /etc/cron.hourly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_hourly_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.hourly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.hourly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_hourly | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_monthly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_monthly_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_monthly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_monthly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_monthly | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.monthly/
    ansible.builtin.file:
      path: /etc/cron.monthly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_monthly_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.monthly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.monthly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_monthly | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_weekly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_weekly_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_weekly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_weekly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_weekly | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.weekly/
    ansible.builtin.file:
      path: /etc/cron.weekly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_weekly_newgroup }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.weekly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.weekly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_weekly | default(true) | bool)
- block:
  - name: Set the file_groupowner_crontab_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_crontab_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_crontab_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_crontab_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_crontab | default(true) | bool)
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_crontab | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      follow: false
      group: '{{ file_groupowner_crontab_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_crontab | default(true) | bool)
- block:
  - name: Set the file_owner_cron_d_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_d_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_d_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_d_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_d | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.d/
    ansible.builtin.file:
      path: /etc/cron.d/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_d_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.d/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_d | default(true) | bool)
- block:
  - name: Set the file_owner_cron_daily_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_daily_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_daily_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_daily_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_daily | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.daily/
    ansible.builtin.file:
      path: /etc/cron.daily/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_daily_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.daily/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.daily/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_daily | default(true) | bool)
- block:
  - name: Set the file_owner_cron_hourly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_hourly_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_hourly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_hourly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_hourly | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.hourly/
    ansible.builtin.file:
      path: /etc/cron.hourly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_hourly_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.hourly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.hourly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_hourly | default(true) | bool)
- block:
  - name: Set the file_owner_cron_monthly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_monthly_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_monthly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_monthly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_monthly | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.monthly/
    ansible.builtin.file:
      path: /etc/cron.monthly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_monthly_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.monthly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.monthly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_monthly | default(true) | bool)
- block:
  - name: Set the file_owner_cron_weekly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_weekly_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_weekly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_weekly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_weekly | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.weekly/
    ansible.builtin.file:
      path: /etc/cron.weekly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_weekly_newown }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.weekly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.weekly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_weekly | default(true) | bool)
- block:
  - name: Set the file_owner_crontab_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_crontab_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_crontab_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_crontab_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_crontab | default(true) | bool)
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_crontab | default(true) | bool)
- block:
  - name: Ensure owner on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      follow: false
      owner: '{{ file_owner_crontab_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_crontab | default(true) | bool)
- block:
  - name: Find /etc/cron.d/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.d/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.d/ file(s)'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_d | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.d/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.d/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_d | default(true) | bool)
- block:
  - name: Find /etc/cron.daily/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.daily/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.daily/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.daily/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_daily | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.daily/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.daily/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.daily/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_daily | default(true) | bool)
- block:
  - name: Find /etc/cron.hourly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.hourly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.hourly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.hourly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_hourly | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.hourly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.hourly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.hourly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_hourly | default(true) | bool)
- block:
  - name: Find /etc/cron.monthly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.monthly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.monthly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.monthly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_monthly | default(true) |
    bool)
- block:
  - name: Set permissions for /etc/cron.monthly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.monthly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.monthly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_monthly | default(true) |
    bool)
- block:
  - name: Find /etc/cron.weekly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.weekly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.weekly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.weekly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_weekly | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.weekly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.weekly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.weekly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_weekly | default(true) | bool)
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_crontab | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_crontab | default(true) | bool)
- block:
  - name: Ensure that /etc/at.allow exists - Add empty /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      state: touch
      owner: '0'
      mode: '0640'
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - disable_strategy
    - file_at_allow_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that /etc/at.allow exists - Add empty /etc/at.allow'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that /etc/at.allow exists - Add empty /etc/at.allow'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_at_allow_exists | default(true) | bool)
- block:
  - name: Ensure that /etc/cron.allow exists - Add empty /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      state: touch
      owner: '0'
      mode: '0640'
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - disable_strategy
    - file_cron_allow_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that /etc/cron.allow exists - Add empty /etc/cron.allow'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that /etc/cron.allow exists - Add empty /etc/cron.allow'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_cron_allow_exists | default(true) | bool)
- block:
  - name: Ensure that /etc/cron.deny does not exist - Remove /etc/cron.deny
    ansible.builtin.file:
      path: /etc/cron.deny
      state: absent
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - disable_strategy
    - file_cron_deny_not_exist
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that /etc/cron.deny does not exist - Remove /etc/cron.deny'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that /etc/cron.deny does not exist - Remove /etc/cron.deny'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_cron_deny_not_exist | default(true) | bool)
- block:
  - name: Set the file_groupowner_at_allow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_at_allow_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_at_allow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_at_allow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_at_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_at_allow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      follow: false
      group: '{{ file_groupowner_at_allow_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_at_allow | default(true) | bool)
- block:
  - name: Set the file_groupowner_at_deny_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_at_deny_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupowner_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_at_deny_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_at_deny_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_at_deny | default(true) | bool)
- block:
  - name: Test for existence /etc/at.deny
    ansible.builtin.stat:
      path: /etc/at.deny
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupowner_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.deny'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_at_deny | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/at.deny
    ansible.builtin.file:
      path: /etc/at.deny
      follow: false
      group: '{{ file_groupowner_at_deny_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/at.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/at.deny'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_at_deny | default(true) | bool)
- block:
  - name: Check that the crontab group is defined
    ansible.builtin.getent:
      database: group
      key: crontab
    ignore_errors: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_groupowner_cron_allow_newgroup is undefined
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the crontab group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the crontab group is defined'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_allow | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_allow_newgroup variable if crontab found
    ansible.builtin.set_fact:
      file_groupowner_cron_allow_newgroup: crontab
    when:
    - '"linux-base" in ansible_facts.packages'
    - ansible_facts.getent_group["crontab"] is defined
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_allow_newgroup variable if crontab found'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_allow_newgroup variable if crontab found'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_allow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      follow: false
      group: '{{ file_groupowner_cron_allow_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_cron_allow | default(true) | bool)
- block:
  - name: Set the file_owner_at_allow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_at_allow_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_at_allow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_at_allow_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_at_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_at_allow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      follow: false
      owner: '{{ file_owner_at_allow_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_at_allow | default(true) | bool)
- block:
  - name: Set the file_owner_at_deny_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_at_deny_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_owner_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_at_deny_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_at_deny_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_at_deny | default(true) | bool)
- block:
  - name: Test for existence /etc/at.deny
    ansible.builtin.stat:
      path: /etc/at.deny
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_owner_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.deny'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_at_deny | default(true) | bool)
- block:
  - name: Ensure owner on /etc/at.deny
    ansible.builtin.file:
      path: /etc/at.deny
      follow: false
      owner: '{{ file_owner_at_deny_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/at.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/at.deny'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_at_deny | default(true) | bool)
- block:
  - name: Set the file_owner_cron_allow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_allow_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_allow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_allow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_allow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      follow: false
      owner: '{{ file_owner_cron_allow_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_cron_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_at_allow | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_at_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/at.deny
    ansible.builtin.stat:
      path: /etc/at.deny
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.deny'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_at_deny | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/at.deny
    ansible.builtin.file:
      path: /etc/at.deny
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_at_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.deny'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.deny'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_at_deny | default(true) | bool)
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_allow | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_cron_allow | default(true) | bool)
- block:
  - name: Enable service chrony
    block:
    - name: Enable service chrony
      ansible.builtin.systemd:
        name: chrony
        enabled: 'yes'
        state: started
        masked: 'no'
      when:
      - '"chrony" in ansible_facts.packages'
      - var_timesync_service == "chronyd"
      - var_timesync_service == "chronyd"
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_chronyd_enabled
  rescue:
  - name: 'StigForge: continue after error in ''Enable service chrony'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable service chrony'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_chronyd_enabled | default(true) | bool)
- block:
  - name: Enable service systemd-timesyncd
    block:
    - name: Enable service systemd-timesyncd
      ansible.builtin.systemd:
        name: systemd-timesyncd
        enabled: 'yes'
        state: started
        masked: 'no'
      when:
      - '"systemd-timesyncd" in ansible_facts.packages'
      - var_timesync_service == "systemd-timesyncd"
      - var_timesync_service == "systemd-timesyncd"
    when:
    - '"linux-base" in ansible_facts.packages'
    - ( not ( "chrony" in ansible_facts.packages ) and not ( "ntp" in ansible_facts.packages ) )
    tags:
    - NIST-800-53-AU-8(1)(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.1
    - enable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_timesyncd_enabled
  rescue:
  - name: 'StigForge: continue after error in ''Enable service systemd-timesyncd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable service systemd-timesyncd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_enabled | default(true) | bool)
- block:
  - name: The Chronyd service is disabled - Collect systemd Services Present in the System
    ansible.builtin.command: systemctl -q list-unit-files --type service
    register: service_exists
    changed_when: false
    failed_when: service_exists.rc not in [0, 1]
    check_mode: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_chronyd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''The Chronyd service is disabled - Collect systemd Services Present in the
      System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''The Chronyd service is disabled - Collect systemd Services Present in the System'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_chronyd_disabled | default(true) | bool)
- block:
  - name: The Chronyd service is disabled - Ensure "chrony.service" is Masked
    ansible.builtin.systemd:
      name: chrony.service
      state: stopped
      enabled: false
      masked: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - service_exists.stdout_lines is search("chrony.service",multiline=True)
    - var_timesync_service != "chronyd"
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_chronyd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''The Chronyd service is disabled - Ensure "chrony.service" is Masked'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''The Chronyd service is disabled - Ensure "chrony.service" is Masked'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_chronyd_disabled | default(true) | bool)
- block:
  - name: Unit Socket Exists - chrony.socket
    ansible.builtin.command: systemctl -q list-unit-files chrony.socket
    register: socket_file_exists
    changed_when: false
    failed_when: socket_file_exists.rc not in [0, 1]
    check_mode: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_chronyd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Unit Socket Exists - chrony.socket'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Unit Socket Exists - chrony.socket'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_chronyd_disabled | default(true) | bool)
- block:
  - name: Disable socket chrony
    ansible.builtin.systemd:
      name: chrony.socket
      enabled: 'no'
      state: stopped
      masked: 'yes'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - socket_file_exists.stdout_lines is search("chrony.socket",multiline=True)
    - var_timesync_service != "chronyd"
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_chronyd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable socket chrony'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable socket chrony'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_chronyd_disabled | default(true) | bool)
- block:
  - name: Disable systemd_timesyncd Service - Collect systemd Services Present in the System
    ansible.builtin.command: systemctl -q list-unit-files --type service
    register: service_exists
    changed_when: false
    failed_when: service_exists.rc not in [0, 1]
    check_mode: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd_timesyncd Service - Collect systemd Services Present in the
      System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd_timesyncd Service - Collect systemd Services Present in the System'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_disabled | default(true) | bool)
- block:
  - name: Disable systemd_timesyncd Service - Ensure "systemd-timesyncd.service" is Masked
    ansible.builtin.systemd:
      name: systemd-timesyncd.service
      state: stopped
      enabled: false
      masked: true
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    - service_exists.stdout_lines is search("systemd-timesyncd.service",multiline=True)
    - var_timesync_service != "systemd-timesyncd"
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd_timesyncd Service - Ensure "systemd-timesyncd.service" is
      Masked'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd_timesyncd Service - Ensure "systemd-timesyncd.service" is Masked'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_disabled | default(true) | bool)
- block:
  - name: Unit Socket Exists - systemd-timesyncd.socket
    ansible.builtin.command: systemctl -q list-unit-files systemd-timesyncd.socket
    register: socket_file_exists
    changed_when: false
    failed_when: socket_file_exists.rc not in [0, 1]
    check_mode: false
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Unit Socket Exists - systemd-timesyncd.socket'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Unit Socket Exists - systemd-timesyncd.socket'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_disabled | default(true) | bool)
- block:
  - name: Disable socket systemd-timesyncd
    ansible.builtin.systemd:
      name: systemd-timesyncd.socket
      enabled: 'no'
      state: stopped
      masked: 'yes'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    - socket_file_exists.stdout_lines is search("systemd-timesyncd.socket",multiline=True)
    - var_timesync_service != "systemd-timesyncd"
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable socket systemd-timesyncd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable socket systemd-timesyncd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_disabled | default(true) | bool)
- block:
  - name: Chrony Configure Pool and Server - Add missing / update wrong records for remote time servers
    ansible.builtin.lineinfile:
      path: /etc/chrony/chrony.conf
      regexp: ^\s*\bserver\b\s*\b{{ item }}\b
      state: present
      line: server {{ item }}
      create: true
      mode: 420
    with_items:
    - '{{ var_multiple_time_servers.split(",") }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-8(1)(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.3
    - chronyd_configure_pool_and_server
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Chrony Configure Pool and Server - Add missing / update wrong records for
      remote time servers'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Chrony Configure Pool and Server - Add missing / update wrong records for remote time servers'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_chronyd_configure_pool_and_server | default(true)
    | bool)
- block:
  - name: Chrony Configure Pool and Server - Add missing / update wrong records for remote time pools
    ansible.builtin.lineinfile:
      path: /etc/chrony/chrony.conf
      regexp: ^\s*\bpool\b\s*\b{{ item }}\b
      state: present
      line: pool {{ item }}
      create: true
      mode: 420
    with_items:
    - '{{ var_multiple_time_pools.split(",") }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-8(1)(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.3
    - chronyd_configure_pool_and_server
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Chrony Configure Pool and Server - Add missing / update wrong records for
      remote time pools'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Chrony Configure Pool and Server - Add missing / update wrong records for remote time pools'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_chronyd_configure_pool_and_server | default(true)
    | bool)
- block:
  - name: Configure Systemd Timesyncd Servers - Set Primary NTP Servers
    ansible.builtin.set_fact:
      preferred_ntp_servers: '{{ var_multiple_time_servers.split(",") | slice(2)| first | join(" ") }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.4.3
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_configured
  rescue:
  - name: 'StigForge: continue after error in ''Configure Systemd Timesyncd Servers - Set Primary NTP Servers'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Systemd Timesyncd Servers - Set Primary NTP Servers'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_configured | default(true) | bool)
- block:
  - name: Configure Systemd Timesyncd Servers - Set Fallback NTP Servers
    ansible.builtin.set_fact:
      fallback_ntp_servers: '{{ var_multiple_time_servers.split(",") | slice(2)| list | last | join(" ") }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.4.3
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_configured
  rescue:
  - name: 'StigForge: continue after error in ''Configure Systemd Timesyncd Servers - Set Fallback NTP Servers'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Systemd Timesyncd Servers - Set Fallback NTP Servers'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_configured | default(true) | bool)
- block:
  - name: Set 'NTP' to '{{ preferred_ntp_servers }}' in the [Time] section of '/etc/systemd/timesyncd.conf.d/oscap-remedy.conf'
    community.general.ini_file:
      path: /etc/systemd/timesyncd.conf.d/oscap-remedy.conf
      section: Time
      option: NTP
      value: '{{ preferred_ntp_servers }}'
      create: true
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.4.3
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_configured
  rescue:
  - name: 'StigForge: continue after error in "Set ''NTP'' to ''{{ preferred_ntp_servers }}'' in the [Time] section of ''/etc/systemd/timesyncd.conf.d/oscap-remedy.conf''"
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set ''NTP'' to ''{{ preferred_ntp_servers }}'' in the [Time] section of ''/etc/systemd/timesyncd.conf.d/oscap-remedy.conf''"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_configured | default(true) | bool)
- block:
  - name: Set 'FallbackNTP' to '{{ fallback_ntp_servers }}' in the [Time] section of '/etc/systemd/timesyncd.conf.d/oscap-remedy.conf'
    community.general.ini_file:
      path: /etc/systemd/timesyncd.conf.d/oscap-remedy.conf
      section: Time
      option: FallbackNTP
      value: '{{ fallback_ntp_servers }}'
      create: true
      mode: 420
    when:
    - '"linux-base" in ansible_facts.packages'
    - '"systemd-timesyncd" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.4.3
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_timesyncd_configured
  rescue:
  - name: 'StigForge: continue after error in "Set ''FallbackNTP'' to ''{{ fallback_ntp_servers }}'' in the [Time] section
      of ''/etc/systemd/timesyncd.conf.d/oscap-remedy.conf''" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set ''FallbackNTP'' to ''{{ fallback_ntp_servers }}'' in the [Time] section of ''/etc/systemd/timesyncd.conf.d/oscap-remedy.conf''"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_service_timesyncd_configured | default(true) | bool)
- block:
  - name: Set the file_groupowner_sshd_config_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_sshd_config_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_sshd_config_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_sshd_config_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_sshd_config | default(true) | bool)
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_sshd_config | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      follow: false
      group: '{{ file_groupowner_sshd_config_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_sshd_config | default(true) | bool)
- block:
  - name: Set the file_groupowner_sshd_drop_in_config_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_sshd_drop_in_config_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_sshd_drop_in_config_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_sshd_drop_in_config_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_sshd_drop_in_config | default(true)
    | bool)
- block:
  - name: Find /etc/ssh/sshd_config.d/ file(s) matching ^.*$
    ansible.builtin.command: find -P /etc/ssh/sshd_config.d/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex
      "^.*$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_sshd_drop_in_config | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/ssh/sshd_config.d/ file(s) matching ^.*$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupowner_sshd_drop_in_config_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupowner_sshd_drop_in_config | default(true)
    | bool)
- block:
  - name: Set the file_owner_sshd_config_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_sshd_config_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_sshd_config_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_sshd_config_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_sshd_config | default(true) | bool)
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_sshd_config | default(true) | bool)
- block:
  - name: Ensure owner on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      follow: false
      owner: '{{ file_owner_sshd_config_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_sshd_config | default(true) | bool)
- block:
  - name: Set the file_owner_sshd_drop_in_config_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_sshd_drop_in_config_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_sshd_drop_in_config_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_sshd_drop_in_config_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_sshd_drop_in_config | default(true) |
    bool)
- block:
  - name: Find /etc/ssh/sshd_config.d/ file(s) matching ^.*$
    ansible.builtin.command: find -P /etc/ssh/sshd_config.d/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex
      "^.*$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_sshd_drop_in_config | default(true) |
    bool)
- block:
  - name: Ensure owner on /etc/ssh/sshd_config.d/ file(s) matching ^.*$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_owner_sshd_drop_in_config_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/ssh/sshd_config.d/ file(s) matching ^.*$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_owner_sshd_drop_in_config | default(true) |
    bool)
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_config | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_config | default(true) | bool)
- block:
  - name: Find /etc/ssh/sshd_config.d/ file(s)
    ansible.builtin.command: find -P /etc/ssh/sshd_config.d/ -maxdepth 1 -perm /u+xs,g+xwrs,o+xwrt  -type f -regextype posix-extended
      -regex "^.*$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/sshd_config.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/sshd_config.d/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_drop_in_config | default(true)
    | bool)
- block:
  - name: Set permissions for /etc/ssh/sshd_config.d/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xwrs,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_sshd_drop_in_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/ssh/sshd_config.d/ file(s)'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/ssh/sshd_config.d/ file(s)'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_drop_in_config | default(true)
    | bool)
- block:
  - name: Find root:root-owned keys
    ansible.builtin.command: find -H /etc/ssh/ -maxdepth 1 -user root -regex ".*_key$" -type f -group root -perm /u+xs,g+xwrs,o+xwrt
    register: root_owned_keys
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find root:root-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find root:root-owned keys'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_private_key | default(true)
    | bool)
- block:
  - name: Set permissions for root:root-owned keys
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xwrs,o-xwrt
      state: file
    with_items:
    - '{{ root_owned_keys.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for root:root-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for root:root-owned keys'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_private_key | default(true)
    | bool)
- block:
  - name: Find /etc/ssh/ file(s)
    ansible.builtin.command: find -P /etc/ssh/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended -regex
      "^.*\.pub$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_pub_key | default(true) |
    bool)
- block:
  - name: Set permissions for /etc/ssh/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/ssh/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/ssh/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_sshd_pub_key | default(true) |
    bool)
- block:
  - name: Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - Check if /etc/ssh/sshd_config exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - Check if /etc/ssh/sshd_config exists'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+{{ var_sshd_set_keepalive }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+{{ var_sshd_set_keepalive }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - Check if the parameter ClientAliveCountMax is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter ClientAliveCountMax is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        line: ClientAliveCountMax {{ var_sshd_set_keepalive }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255030
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - Check if /etc/ssh/sshd_config exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - Check if /etc/ssh/sshd_config exists'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+{{ sshd_idle_timeout_value }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+{{ sshd_idle_timeout_value }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - Check if the parameter ClientAliveInterval is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter ClientAliveInterval is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        line: ClientAliveInterval {{ sshd_idle_timeout_value }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255035
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - Check if /etc/ssh/sshd_config exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+no$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured
        correctly in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+no$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - Check if the parameter HostbasedAuthentication is configured
        correctly in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter HostbasedAuthentication is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        line: HostbasedAuthentication no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if /etc/ssh/sshd_config exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured correctly in
      /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+no$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        correctly in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured correctly in
      /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+no$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        correctly in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitEmptyPasswords is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        line: PermitEmptyPasswords no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Forwarding - Check if the parameter DisableForwarding is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured
      in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_forwarding | default(true) | bool)
- block:
  - name: Disable SSH Forwarding - Check if the parameter DisableForwarding is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured
      in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_forwarding | default(true) | bool)
- block:
  - name: Disable SSH Forwarding - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Forwarding - Check if /etc/ssh/sshd_config exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Forwarding - Check if /etc/ssh/sshd_config exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_forwarding | default(true) | bool)
- block:
  - name: Disable SSH Forwarding - Check if the parameter DisableForwarding is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+yes$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured
      correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured correctly in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_forwarding | default(true) | bool)
- block:
  - name: Disable SSH Forwarding - Check if the parameter DisableForwarding is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+yes$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured
      correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Forwarding - Check if the parameter DisableForwarding is configured correctly in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_forwarding | default(true) | bool)
- block:
  - name: Disable SSH Forwarding
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter DisableForwarding is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "DisableForwarding"| regex_escape }}\s+
        line: DisableForwarding yes
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Forwarding'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Forwarding'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_forwarding | default(true) | bool)
- block:
  - name: Disable SSH Forwarding - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Forwarding - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Forwarding - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_forwarding | default(true) | bool)
- block:
  - name: Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_gssapi_auth
  rescue:
  - name: 'StigForge: continue after error in ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_gssapi_auth | default(true) | bool)
- block:
  - name: Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_gssapi_auth
  rescue:
  - name: 'StigForge: continue after error in ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_gssapi_auth | default(true) | bool)
- block:
  - name: Disable GSSAPI Authentication - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_gssapi_auth
  rescue:
  - name: 'StigForge: continue after error in ''Disable GSSAPI Authentication - Check if /etc/ssh/sshd_config exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GSSAPI Authentication - Check if /etc/ssh/sshd_config exists'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_gssapi_auth | default(true) | bool)
- block:
  - name: Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+no$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_gssapi_auth
  rescue:
  - name: 'StigForge: continue after error in ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_gssapi_auth | default(true) | bool)
- block:
  - name: Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+no$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_gssapi_auth
  rescue:
  - name: 'StigForge: continue after error in ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GSSAPI Authentication - Check if the parameter GSSAPIAuthentication is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_gssapi_auth | default(true) | bool)
- block:
  - name: Disable GSSAPI Authentication
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter GSSAPIAuthentication is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "GSSAPIAuthentication"| regex_escape }}\s+
        line: GSSAPIAuthentication no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_gssapi_auth
  rescue:
  - name: 'StigForge: continue after error in ''Disable GSSAPI Authentication'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GSSAPI Authentication'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_gssapi_auth | default(true) | bool)
- block:
  - name: Disable GSSAPI Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_gssapi_auth
  rescue:
  - name: 'StigForge: continue after error in ''Disable GSSAPI Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GSSAPI Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_gssapi_auth | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - Check if /etc/ssh/sshd_config exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+yes$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+yes$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - Check if the parameter IgnoreRhosts is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter IgnoreRhosts is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        line: IgnoreRhosts yes
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Root Login - Check if the parameter PermitRootLogin is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured
      in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH Root Login - Check if the parameter PermitRootLogin is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured
      in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH Root Login - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - Check if /etc/ssh/sshd_config exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - Check if /etc/ssh/sshd_config exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH Root Login - Check if the parameter PermitRootLogin is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+no$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured
      correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured correctly in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH Root Login - Check if the parameter PermitRootLogin is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+no$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured
      correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - Check if the parameter PermitRootLogin is configured correctly in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH Root Login
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitRootLogin is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        line: PermitRootLogin no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - Check if /etc/ssh/sshd_config exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+no$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured
        correctly in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+no$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - Check if the parameter PermitUserEnvironment is configured
        correctly in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitUserEnvironment is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        line: PermitUserEnvironment no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255025
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Enable PAM - Check if the parameter UsePAM is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "UsePAM"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - Check if the parameter UsePAM is configured in /etc/ssh/sshd_config''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - Check if the parameter UsePAM is configured in /etc/ssh/sshd_config'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable PAM - Check if the parameter UsePAM is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "UsePAM"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - Check if the parameter UsePAM is configured in /etc/ssh/sshd_config.d''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - Check if the parameter UsePAM is configured in /etc/ssh/sshd_config.d'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable PAM - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - Check if /etc/ssh/sshd_config exists'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - Check if /etc/ssh/sshd_config exists'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable PAM - Check if the parameter UsePAM is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "UsePAM"| regex_escape }}\s+yes$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - Check if the parameter UsePAM is configured correctly in /etc/ssh/sshd_config''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - Check if the parameter UsePAM is configured correctly in /etc/ssh/sshd_config'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable PAM - Check if the parameter UsePAM is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "UsePAM"| regex_escape }}\s+yes$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - Check if the parameter UsePAM is configured correctly in /etc/ssh/sshd_config.d''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - Check if the parameter UsePAM is configured correctly in /etc/ssh/sshd_config.d'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable PAM
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter UsePAM is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "UsePAM"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        line: UsePAM yes
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-255065
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable SSH Warning Banner - Check if the parameter Banner is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "Banner"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - Check if the parameter Banner is configured in
      /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - Check if the parameter Banner is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_warning_banner_net | default(true) |
    bool)
- block:
  - name: Enable SSH Warning Banner - Check if the parameter Banner is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "Banner"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - Check if the parameter Banner is configured in
      /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - Check if the parameter Banner is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_warning_banner_net | default(true) |
    bool)
- block:
  - name: Enable SSH Warning Banner - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - Check if /etc/ssh/sshd_config exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - Check if /etc/ssh/sshd_config exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_warning_banner_net | default(true) |
    bool)
- block:
  - name: Enable SSH Warning Banner - Check if the parameter Banner is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "Banner"| regex_escape }}\s+/etc/issue.net$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - Check if the parameter Banner is configured correctly
      in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - Check if the parameter Banner is configured correctly in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_warning_banner_net | default(true) |
    bool)
- block:
  - name: Enable SSH Warning Banner - Check if the parameter Banner is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "Banner"| regex_escape }}\s+/etc/issue.net$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - Check if the parameter Banner is configured correctly
      in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - Check if the parameter Banner is configured correctly in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_warning_banner_net | default(true) |
    bool)
- block:
  - name: Enable SSH Warning Banner
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter Banner is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "Banner"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        line: Banner /etc/issue.net
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_warning_banner_net | default(true) |
    bool)
- block:
  - name: Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - DISA-STIG-UBTU-22-255020
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_enable_warning_banner_net | default(true) |
    bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured in
        /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured in
        /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - Check if /etc/ssh/sshd_config exists'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+{{ var_sshd_set_login_grace_time }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+{{ var_sshd_set_login_grace_time }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - Check if the parameter LoginGraceTime is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter LoginGraceTime is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        line: LoginGraceTime {{ var_sshd_set_login_grace_time }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Set LogLevel to INFO - Check if the parameter LogLevel is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "LogLevel"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO - Check if the parameter LogLevel is configured in /etc/ssh/sshd_config''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO - Check if the parameter LogLevel is configured in /etc/ssh/sshd_config'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_loglevel_info | default(true) | bool)
- block:
  - name: Set LogLevel to INFO - Check if the parameter LogLevel is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "LogLevel"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO - Check if the parameter LogLevel is configured in /etc/ssh/sshd_config.d''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO - Check if the parameter LogLevel is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_loglevel_info | default(true) | bool)
- block:
  - name: Set LogLevel to INFO - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO - Check if /etc/ssh/sshd_config exists'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO - Check if /etc/ssh/sshd_config exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_loglevel_info | default(true) | bool)
- block:
  - name: Set LogLevel to INFO - Check if the parameter LogLevel is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "LogLevel"| regex_escape }}\s+INFO$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO - Check if the parameter LogLevel is configured correctly
      in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO - Check if the parameter LogLevel is configured correctly in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_loglevel_info | default(true) | bool)
- block:
  - name: Set LogLevel to INFO - Check if the parameter LogLevel is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "LogLevel"| regex_escape }}\s+INFO$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO - Check if the parameter LogLevel is configured correctly
      in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO - Check if the parameter LogLevel is configured correctly in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_loglevel_info | default(true) | bool)
- block:
  - name: Set LogLevel to INFO
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter LogLevel is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "LogLevel"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        line: LogLevel INFO
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_loglevel_info | default(true) | bool)
- block:
  - name: Set LogLevel to INFO - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_info
  rescue:
  - name: 'StigForge: continue after error in ''Set LogLevel to INFO - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set LogLevel to INFO - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_loglevel_info | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - Check if /etc/ssh/sshd_config exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+{{ sshd_max_auth_tries_value }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+{{ sshd_max_auth_tries_value }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - Check if the parameter MaxAuthTries is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxAuthTries is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        line: MaxAuthTries {{ sshd_max_auth_tries_value }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured
      in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured
      in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - Check if /etc/ssh/sshd_config exists'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - Check if /etc/ssh/sshd_config exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MaxSessions"| regex_escape }}\s+{{ var_sshd_max_sessions }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured
      correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured correctly in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MaxSessions"| regex_escape }}\s+{{ var_sshd_max_sessions }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured
      correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - Check if the parameter MaxSessions is configured correctly in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxSessions is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        line: MaxSessions {{ var_sshd_max_sessions }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - Check if /etc/ssh/sshd_config exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MaxStartups"| regex_escape }}\s+{{ var_sshd_set_maxstartups }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MaxStartups"| regex_escape }}\s+{{ var_sshd_set_maxstartups }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - Check if the parameter MaxStartups is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxStartups is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        line: MaxStartups {{ var_sshd_set_maxstartups }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_kex | default(true) | bool)
- block:
  - name: Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_kex | default(true) | bool)
- block:
  - name: Use Only Strong Key Exchange algorithms - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms - Check if /etc/ssh/sshd_config exists'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_kex | default(true) | bool)
- block:
  - name: Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+{{ sshd_strong_kex }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured correctly
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_kex | default(true) | bool)
- block:
  - name: Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+{{ sshd_strong_kex }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms - Check if the parameter KexAlgorithms is configured correctly
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_kex | default(true) | bool)
- block:
  - name: Use Only Strong Key Exchange algorithms
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter KexAlgorithms is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "KexAlgorithms"| regex_escape }}\s+
        line: KexAlgorithms {{ sshd_strong_kex }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_kex | default(true) | bool)
- block:
  - name: Use Only Strong Key Exchange algorithms - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-2.3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_kex
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong Key Exchange algorithms - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong Key Exchange algorithms - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_kex | default(true) | bool)
- block:
  - name: Use Only Strong MACs - Check if the parameter MACs is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MACs"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs - Check if the parameter MACs is configured in /etc/ssh/sshd_config''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs - Check if the parameter MACs is configured in /etc/ssh/sshd_config'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_macs | default(true) | bool)
- block:
  - name: Use Only Strong MACs - Check if the parameter MACs is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MACs"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs - Check if the parameter MACs is configured in /etc/ssh/sshd_config.d''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs - Check if the parameter MACs is configured in /etc/ssh/sshd_config.d'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_macs | default(true) | bool)
- block:
  - name: Use Only Strong MACs - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs - Check if /etc/ssh/sshd_config exists'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs - Check if /etc/ssh/sshd_config exists'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_macs | default(true) | bool)
- block:
  - name: Use Only Strong MACs - Check if the parameter MACs is configured correctly in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "MACs"| regex_escape }}\s+{{ sshd_strong_macs }}$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"linux-base" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs - Check if the parameter MACs is configured correctly
      in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs - Check if the parameter MACs is configured correctly in /etc/ssh/sshd_config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_macs | default(true) | bool)
- block:
  - name: Use Only Strong MACs - Check if the parameter MACs is configured correctly in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "MACs"| regex_escape }}\s+{{ sshd_strong_macs }}$
    register: _config_dir_correctly
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs - Check if the parameter MACs is configured correctly
      in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs - Check if the parameter MACs is configured correctly in /etc/ssh/sshd_config.d''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_macs | default(true) | bool)
- block:
  - name: Use Only Strong MACs
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MACs"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MACs is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MACs"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MACs"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MACs"| regex_escape }}\s+
        line: MACs {{ sshd_strong_macs }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"linux-base" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_macs | default(true) | bool)
- block:
  - name: Use Only Strong MACs - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17 (2)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_use_strong_macs
  rescue:
  - name: 'StigForge: continue after error in ''Use Only Strong MACs - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Use Only Strong MACs - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_sshd_use_strong_macs | default(true) | bool)
- block:
  - name: Make the auditd Configuration Immutable - Collect all files from /etc/audit/rules.d with .rules extension
    ansible.builtin.find:
      paths: /etc/audit/rules.d/
      patterns: '*.rules'
    register: find_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make the auditd Configuration Immutable - Collect all files from /etc/audit/rules.d
      with .rules extension'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make the auditd Configuration Immutable - Collect all files from /etc/audit/rules.d with .rules extension''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Make the auditd Configuration Immutable - Check if target files exist and get their content
    ansible.builtin.stat:
      path: '{{ item }}'
    register: audit_files_stat
    loop:
    - /etc/audit/audit.rules
    - /etc/audit/rules.d/immutable.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make the auditd Configuration Immutable - Check if target files exist and
      get their content'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make the auditd Configuration Immutable - Check if target files exist and get their content'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Make the auditd Configuration Immutable - Read content of existing audit files
    ansible.builtin.slurp:
      src: '{{ item.item }}'
    register: audit_files_content
    loop: '{{ audit_files_stat.results }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - (item is not skipped and item.stat.exists)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make the auditd Configuration Immutable - Read content of existing audit files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make the auditd Configuration Immutable - Read content of existing audit files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Make the auditd Configuration Immutable - Check if -e 2 is already correctly set in target files
    ansible.builtin.set_fact:
      immutable_correctly_set: "{{\n  audit_files_content.results\n  | selectattr('content', 'defined')\n  | map(attribute='content')\n\
        \  | map('b64decode')\n  | select('search', '^-e 2$', multiline=True)\n  | list\n  | length == 2\n}}"
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make the auditd Configuration Immutable - Check if -e 2 is already correctly
      set in target files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make the auditd Configuration Immutable - Check if -e 2 is already correctly set in target files''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Make the auditd Configuration Immutable - Remove any existing -e option from all Audit config files
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: ^\s*-e\s+.*$
      state: absent
    loop: '{{ find_rules_d.files | map(attribute=''path'') | list + [''/etc/audit/audit.rules''] }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not immutable_correctly_set
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make the auditd Configuration Immutable - Remove any existing -e option from
      all Audit config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make the auditd Configuration Immutable - Remove any existing -e option from all Audit config files''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Make the auditd Configuration Immutable - Ensure target directories exist
    ansible.builtin.file:
      path: '{{ item | dirname }}'
      state: directory
      mode: '0750'
    loop:
    - /etc/audit/audit.rules
    - /etc/audit/rules.d/immutable.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not immutable_correctly_set
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make the auditd Configuration Immutable - Ensure target directories exist''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make the auditd Configuration Immutable - Ensure target directories exist'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Make the auditd Configuration Immutable - Add Audit -e 2 option to make rules immutable
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      create: true
      line: -e 2
      regexp: ^\s*-e\s+.*$
      mode: g-rwx,o-rwx
    loop:
    - /etc/audit/audit.rules
    - /etc/audit/rules.d/immutable.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not immutable_correctly_set
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make the auditd Configuration Immutable - Add Audit -e 2 option to make rules
      immutable'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make the auditd Configuration Immutable - Add Audit -e 2 option to make rules immutable'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor) - Check if watch rule for /etc/apparmor
      already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/apparmor\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_mac_modification_etc_apparmor
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor)
      - Check if watch rule for /etc/apparmor already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor) - Check if watch
        rule for /etc/apparmor already exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor) - Search /etc/audit/rules.d for
      other rules with specified key MAC-policy
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)MAC-policy$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor)
      - Search /etc/audit/rules.d for other rules with specified key MAC-policy" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor) - Search /etc/audit/rules.d
        for other rules with specified key MAC-policy" raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor) - Use /etc/audit/rules.d/MAC-policy.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/MAC-policy.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor)
      - Use /etc/audit/rules.d/MAC-policy.rules as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor) - Use /etc/audit/rules.d/MAC-policy.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor) - Use matched file as the recipient
      for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor)
      - Use matched file as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor) - Use matched file
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor) - Add watch rule for /etc/apparmor
      in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/apparmor -p wa -k MAC-policy
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor)
      - Add watch rule for /etc/apparmor in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor) - Add watch rule
        for /etc/apparmor in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor) - Check if watch rule for /etc/apparmor
      already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/apparmor\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_mac_modification_etc_apparmor
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor)
      - Check if watch rule for /etc/apparmor already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor) - Check if watch
        rule for /etc/apparmor already exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor) - Add watch rule for /etc/apparmor
      in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/apparmor -p wa -k MAC-policy
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor)
      - Add watch rule for /etc/apparmor in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor) - Add watch rule
        for /etc/apparmor in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor.d) - Check if watch rule for /etc/apparmor.d
      already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/apparmor.d\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_mac_modification_etc_apparmor_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d)
      - Check if watch rule for /etc/apparmor.d already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d) - Check if watch
        rule for /etc/apparmor.d already exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor_d | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor.d) - Search /etc/audit/rules.d for
      other rules with specified key MAC-policy
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)MAC-policy$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d)
      - Search /etc/audit/rules.d for other rules with specified key MAC-policy" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d) - Search /etc/audit/rules.d
        for other rules with specified key MAC-policy" raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor_d | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor.d) - Use /etc/audit/rules.d/MAC-policy.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/MAC-policy.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d)
      - Use /etc/audit/rules.d/MAC-policy.rules as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d) - Use /etc/audit/rules.d/MAC-policy.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor_d | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor.d) - Use matched file as the recipient
      for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d)
      - Use matched file as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d) - Use matched
        file as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor_d | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor.d) - Add watch rule for /etc/apparmor.d
      in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/apparmor.d -p wa -k MAC-policy
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d)
      - Add watch rule for /etc/apparmor.d in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d) - Add watch rule
        for /etc/apparmor.d in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor_d | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor.d) - Check if watch rule for /etc/apparmor.d
      already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/apparmor.d\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_mac_modification_etc_apparmor_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d)
      - Check if watch rule for /etc/apparmor.d already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d) - Check if watch
        rule for /etc/apparmor.d already exists in /etc/audit/audit.rules" raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor_d | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls (/etc/apparmor.d) - Add watch rule for /etc/apparmor.d
      in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/apparmor.d -p wa -k MAC-policy
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - audit_rules_mac_modification_etc_apparmor_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d)
      - Add watch rule for /etc/apparmor.d in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls (/etc/apparmor.d) - Add watch rule
        for /etc/apparmor.d in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_mac_modification_etc_apparmor_d | default(true)
    | bool)
- block:
  - name: Set architecture for audit mount tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_media_export
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit mount tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit mount tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_media_export | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for mount for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/export.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/export.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_media_export
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for mount for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for mount for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_media_export | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for mount for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/export.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/export.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_media_export
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for mount for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for mount for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_media_export | default(true) | bool)
- block:
  - name: Check the rules script being used
    ansible.builtin.command: grep '^ExecStartPost' /usr/lib/systemd/system/auditd.service
    register: check_rules_scripts_result
    changed_when: false
    failed_when: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_suid_auid_privilege_function
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check the rules script being used'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check the rules script being used'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_suid_auid_privilege_function | default(true)
    | bool)
- block:
  - name: Set suid_audit_rules fact
    ansible.builtin.set_fact:
      suid_audit_rules:
      - rule: -a always,exit -F arch=b32 -S execve -C euid!=uid -F auid!=unset -k user_emulation
        regex: ^[\s]*-a[\s]+always,exit[\s]+-F[\s]+arch=b32[\s]+-C[\s]+euid!=uid[\s]+-F[\s]+auid!=unset[\s]+-S[\s]+execve[\s]+(?:-k[\s]+|-F[\s]+key=)[\S]+[\s]*$
      - rule: -a always,exit -F arch=b64 -S execve -C euid!=uid -F auid!=unset -k user_emulation
        regex: ^[\s]*-a[\s]+always,exit[\s]+-F[\s]+arch=b64[\s]+-C[\s]+euid!=uid[\s]+-F[\s]+auid!=unset[\s]+-S[\s]+execve[\s]+(?:-k[\s]+|-F[\s]+key=)[\S]+[\s]*$
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_suid_auid_privilege_function
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set suid_audit_rules fact'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set suid_audit_rules fact'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_suid_auid_privilege_function | default(true)
    | bool)
- block:
  - name: Update /etc/audit/rules.d/user_emulation.rules to audit privileged functions
    ansible.builtin.lineinfile:
      path: /etc/audit/rules.d/user_emulation.rules
      line: '{{  item.rule  }}'
      regexp: '{{ item.regex }}'
      create: true
      mode: 384
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - '"auditd.service" in ansible_facts.services'
    - '"augenrules" in check_rules_scripts_result.stdout'
    register: augenrules_audit_rules_privilege_function_update_result
    with_items: '{{ suid_audit_rules }}'
    tags:
    - audit_rules_suid_auid_privilege_function
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Update /etc/audit/rules.d/user_emulation.rules to audit privileged functions''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Update /etc/audit/rules.d/user_emulation.rules to audit privileged functions'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_suid_auid_privilege_function | default(true)
    | bool)
- block:
  - name: Update Update /etc/audit/audit.rules to audit privileged functions
    ansible.builtin.lineinfile:
      path: /etc/audit/audit.rules
      line: '{{  item.rule  }}'
      regexp: '{{ item.regex }}'
      create: true
      mode: 384
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - '"auditd.service" in ansible_facts.services'
    - '"auditctl" in check_rules_scripts_result.stdout'
    register: auditctl_audit_rules_privilege_function_update_result
    with_items: '{{ suid_audit_rules }}'
    tags:
    - audit_rules_suid_auid_privilege_function
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Update Update /etc/audit/audit.rules to audit privileged functions'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Update Update /etc/audit/audit.rules to audit privileged functions'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_suid_auid_privilege_function | default(true)
    | bool)
- block:
  - name: Restart Auditd
    ansible.builtin.command: /usr/sbin/service auditd restart
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - (augenrules_audit_rules_privilege_function_update_result.changed or auditctl_audit_rules_privilege_function_update_result.changed)
    - ("auditd.service" in ansible_facts.services and ansible_facts.services["auditd.service"].state == "running")
    tags:
    - audit_rules_suid_auid_privilege_function
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Restart Auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restart Auditd'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_suid_auid_privilege_function | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/sudoers\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already
        exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/sudoers -p wa -k actions
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/sudoers\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already
        exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with specified key
      actions
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)actions$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d
      for other rules with specified key actions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with
        specified key actions'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/actions.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use matched file as
      the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/sudoers -p wa -k actions
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already exists in
      /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/sudoers.d/\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers.d/ already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already
        exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/sudoers.d/ -p wa -k actions
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already exists in
      /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/sudoers.d/\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers.d/ already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already
        exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with specified key
      actions
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)actions$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d
      for other rules with specified key actions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with
        specified key actions'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/actions.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use matched file as
      the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/sudoers.d/ -p wa -k actions
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/group\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654130
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Check if watch
      rule for /etc/group already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Search /etc/audit/rules.d for other rules with specified
      key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654130
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654130
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654130
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/group -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654130
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Add watch
      rule for /etc/group in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in
        /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/group\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654130
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Check if watch
      rule for /etc/group already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/group -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654130
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Add watch
      rule for /etc/group in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in
        /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/gshadow\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654135
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Check if
      watch rule for /etc/gshadow already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654135
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654135
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654135
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/gshadow -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654135
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch
      rule for /etc/gshadow in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/gshadow\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654135
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Check if
      watch rule for /etc/gshadow already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/gshadow -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654135
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch
      rule for /etc/gshadow in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/nsswitch.conf - Check if watch rule for /etc/nsswitch.conf
      already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/nsswitch.conf\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_usergroup_modification_nsswitch_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Check
      if watch rule for /etc/nsswitch.conf already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Check if watch rule for /etc/nsswitch.conf
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_nsswitch_conf
    | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/nsswitch.conf - Search /etc/audit/rules.d for other rules
      with specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_nsswitch_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Search
      /etc/audit/rules.d for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Search /etc/audit/rules.d
        for other rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_nsswitch_conf
    | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/nsswitch.conf - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_nsswitch_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Use
      /etc/audit/rules.d/audit_rules_usergroup_modification.rules as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_nsswitch_conf
    | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/nsswitch.conf - Use matched file as the recipient for the
      rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_nsswitch_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Use
      matched file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_nsswitch_conf
    | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/nsswitch.conf - Add watch rule for /etc/nsswitch.conf in
      /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/nsswitch.conf -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_nsswitch_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Add
      watch rule for /etc/nsswitch.conf in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Add watch rule for /etc/nsswitch.conf
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_nsswitch_conf
    | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/nsswitch.conf - Check if watch rule for /etc/nsswitch.conf
      already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/nsswitch.conf\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_usergroup_modification_nsswitch_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Check
      if watch rule for /etc/nsswitch.conf already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Check if watch rule for /etc/nsswitch.conf
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_nsswitch_conf
    | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/nsswitch.conf - Add watch rule for /etc/nsswitch.conf in
      /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/nsswitch.conf -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - audit_rules_usergroup_modification_nsswitch_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Add
      watch rule for /etc/nsswitch.conf in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/nsswitch.conf - Add watch rule for /etc/nsswitch.conf
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_nsswitch_conf
    | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for /etc/security/opasswd
      already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/security/opasswd\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654140
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Check if watch rule for /etc/security/opasswd already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for
        /etc/security/opasswd already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Search /etc/audit/rules.d for other rules
      with specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654140
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Search /etc/audit/rules.d for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Search /etc/audit/rules.d
        for other rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654140
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules as the recipient for the rule'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Use matched file as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654140
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Use matched file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Use matched file as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
      in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/security/opasswd -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654140
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Add watch rule for /etc/security/opasswd in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for /etc/security/opasswd
      already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/security/opasswd\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654140
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Check if watch rule for /etc/security/opasswd already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for
        /etc/security/opasswd already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
      in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/security/opasswd -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654140
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Add watch rule for /etc/security/opasswd in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.conf - Check if watch rule for /etc/pam.conf already
      exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/pam.conf\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_usergroup_modification_pam_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.conf - Check if
      watch rule for /etc/pam.conf already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.conf - Check if watch rule for /etc/pam.conf
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pam_conf | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.conf - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pam_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.conf - Search
      /etc/audit/rules.d for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.conf - Search /etc/audit/rules.d for
        other rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pam_conf | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.conf - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pam_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.conf - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.conf - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pam_conf | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.conf - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pam_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.conf - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.conf - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pam_conf | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.conf - Add watch rule for /etc/pam.conf in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/pam.conf -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pam_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.conf - Add watch
      rule for /etc/pam.conf in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.conf - Add watch rule for /etc/pam.conf
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pam_conf | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.conf - Check if watch rule for /etc/pam.conf already
      exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/pam.conf\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_usergroup_modification_pam_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.conf - Check if
      watch rule for /etc/pam.conf already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.conf - Check if watch rule for /etc/pam.conf
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pam_conf | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.conf - Add watch rule for /etc/pam.conf in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/pam.conf -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - audit_rules_usergroup_modification_pam_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.conf - Add watch
      rule for /etc/pam.conf in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.conf - Add watch rule for /etc/pam.conf
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pam_conf | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.d/ - Check if watch rule for /etc/pam.d/ already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/pam.d/\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_usergroup_modification_pamd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.d/ - Check if
      watch rule for /etc/pam.d/ already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.d/ - Check if watch rule for /etc/pam.d/
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pamd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.d/ - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pamd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.d/ - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.d/ - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pamd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.d/ - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pamd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.d/ - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.d/ - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pamd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.d/ - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pamd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.d/ - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.d/ - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pamd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.d/ - Add watch rule for /etc/pam.d/ in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/pam.d/ -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_usergroup_modification_pamd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.d/ - Add watch
      rule for /etc/pam.d/ in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.d/ - Add watch rule for /etc/pam.d/ in
        /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pamd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.d/ - Check if watch rule for /etc/pam.d/ already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/pam.d/\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - audit_rules_usergroup_modification_pamd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.d/ - Check if
      watch rule for /etc/pam.d/ already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.d/ - Check if watch rule for /etc/pam.d/
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pamd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/pam.d/ - Add watch rule for /etc/pam.d/ in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/pam.d/ -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - audit_rules_usergroup_modification_pamd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/pam.d/ - Add watch
      rule for /etc/pam.d/ in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/pam.d/ - Add watch rule for /etc/pam.d/ in
        /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_pamd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/passwd\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654145
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Check if
      watch rule for /etc/passwd already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654145
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654145
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654145
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/passwd -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654145
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Add watch
      rule for /etc/passwd in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in
        /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/passwd\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654145
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Check if
      watch rule for /etc/passwd already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/passwd -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654145
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Add watch
      rule for /etc/passwd in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in
        /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/shadow\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654150
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Check if
      watch rule for /etc/shadow already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654150
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654150
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654150
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/shadow -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654150
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Add watch
      rule for /etc/shadow in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in
        /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/shadow\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654150
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Check if
      watch rule for /etc/shadow already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/shadow -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654150
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Add watch
      rule for /etc/shadow in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in
        /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already exists in
      /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/var/log/sudo.log\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654235
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Check if watch rule for
      /var/log/sudo.log already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already
        exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Search /etc/audit/rules.d for other rules with specified key
      maintenance
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)maintenance$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654235
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Search /etc/audit/rules.d
      for other rules with specified key maintenance'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Search /etc/audit/rules.d for other rules with
        specified key maintenance'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Use /etc/audit/rules.d/maintenance.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/maintenance.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654235
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Use /etc/audit/rules.d/maintenance.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Use /etc/audit/rules.d/maintenance.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654235
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Use matched file as the
      recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Use matched file as the recipient for the rule''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /var/log/sudo.log -p wa -k maintenance
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654235
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already exists in
      /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/var/log/sudo.log\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654235
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Check if watch rule for
      /var/log/sudo.log already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already
        exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /var/log/sudo.log -p wa -k maintenance
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654235
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: System Audit Logs Must Have Mode 0750 or Less Permissive - Get audit log file from /etc/audit/auditd.conf
    ansible.builtin.command: grep -iw ^log_file /etc/audit/auditd.conf
    check_mode: false
    failed_when: false
    changed_when: false
    register: log_file_exists
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653060
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9
    - NIST-800-53-CM-6(a)
    - directory_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0750 or Less Permissive - Get audit log file
      from /etc/audit/auditd.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0750 or Less Permissive - Get audit log file from /etc/audit/auditd.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_directory_permissions_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log directory path
    ansible.builtin.set_fact:
      log_file_dir: '{{ (log_file_exists.stdout | default('''') | split('' '') | last | dirname) | default(''/var/log/audit'',
        true) }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653060
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9
    - NIST-800-53-CM-6(a)
    - directory_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log directory
      path'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log directory path'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_directory_permissions_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Have Mode 0750 or Less Permissive - Get audit log group from /etc/audit/auditd.conf
    ansible.builtin.command: grep -iw ^log_group /etc/audit/auditd.conf
    check_mode: false
    failed_when: false
    changed_when: false
    register: log_group_exists
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653060
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9
    - NIST-800-53-CM-6(a)
    - directory_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0750 or Less Permissive - Get audit log group
      from /etc/audit/auditd.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0750 or Less Permissive - Get audit log group from /etc/audit/auditd.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_directory_permissions_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log group
    ansible.builtin.set_fact:
      log_group: '{{ (log_group_exists.stdout | default('''') | split('' '') | last) | default(''root'', true) }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653060
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9
    - NIST-800-53-CM-6(a)
    - directory_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log group''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log group'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_directory_permissions_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log directory permissions
    ansible.builtin.file:
      path: '{{ log_file_dir }}'
      state: directory
      mode: '{{ ''0700'' if log_group == ''root'' else ''0750'' }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653060
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9
    - NIST-800-53-CM-6(a)
    - directory_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log directory
      permissions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0750 or Less Permissive - Set audit log directory permissions''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_directory_permissions_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Get Audit Log Files
    ansible.builtin.command: grep -iw ^log_file /etc/audit/auditd.conf
    failed_when: false
    changed_when: false
    check_mode: false
    register: log_file_exists
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Get Audit Log Files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Get Audit Log Files'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_group_ownership_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Set Log File Facts
    ansible.builtin.set_fact:
      log_file_line: '{{ log_file_exists.stdout | split('' '') | last }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Set Log File Facts'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Set Log File Facts'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_group_ownership_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Set Default log_file if Not Set
    ansible.builtin.set_fact:
      log_file: /var/log/audit/audit.log
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - (log_file_exists is undefined) or (log_file_exists.stdout | length == 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Set Default log_file if Not
      Set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Set Default log_file if Not Set'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_group_ownership_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Set log_file From log_file_line if Not Set Already
    ansible.builtin.set_fact:
      log_file: '{{ log_file_line }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - (log_file_line is defined) and (log_file_line | length > 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Set log_file From log_file_line
      if Not Set Already'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Set log_file From log_file_line if Not Set Already''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_group_ownership_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Get All Log File Backups
    ansible.builtin.find:
      path: '{{ log_file | dirname }}'
      patterns: '{{ log_file | basename }}.*'
    register: backup_files
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Get All Log File Backups''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Get All Log File Backups'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_group_ownership_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Apply Mode to All Backup Log Files
    ansible.builtin.file:
      path: '{{ item }}'
      group: root
    failed_when: false
    loop: '{{ backup_files.files| map(attribute=''path'') | list }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Apply Mode to All Backup Log
      Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Apply Mode to All Backup Log Files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_group_ownership_var_log_audit | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Be Group Owned By Root - Apply Mode to Log File
    ansible.builtin.file:
      path: '{{ log_file }}'
      group: root
    failed_when: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - file_group_ownership_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Be Group Owned By Root - Apply Mode to Log File'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Be Group Owned By Root - Apply Mode to Log File'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_group_ownership_var_log_audit | default(true)
    | bool)
- block:
  - name: Set the file_groupownership_audit_configuration_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupownership_audit_configuration_newgroup: '0'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_audit_configuration_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_audit_configuration_newgroup variable if represented by gid'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.command: find -P /etc/audit/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex "^.*audit(\.rules|d\.conf)$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_audit_configuration_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex
      "^.*\.rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_audit_configuration_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653075
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Set the file_ownership_audit_configuration_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_audit_configuration_newown: '0'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_audit_configuration_newown variable if represented
      by uid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_audit_configuration_newown variable if represented by uid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.command: find -P /etc/audit/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*audit(\.rules|d\.conf)$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure owner on /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_audit_configuration_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*\.rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_audit_configuration_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653070
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: System Audit Logs Must Have Mode 0640 or Less Permissive - Get audit log file from /etc/audit/auditd.conf
    ansible.builtin.command: grep -iw ^log_file /etc/audit/auditd.conf
    check_mode: false
    failed_when: false
    changed_when: false
    register: log_file_exists
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0640 or Less Permissive - Get audit log file
      from /etc/audit/auditd.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0640 or Less Permissive - Get audit log file from /etc/audit/auditd.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_audit | default(true) |
    bool)
- block:
  - name: System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log file path
    ansible.builtin.set_fact:
      log_file_path: '{{ (log_file_exists.stdout | default('''') | split('' '') | last) | default(''/var/log/audit/audit.log'',
        true) }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log file
      path'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log file path'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_audit | default(true) |
    bool)
- block:
  - name: System Audit Logs Must Have Mode 0640 or Less Permissive - Get audit log group from /etc/audit/auditd.conf
    ansible.builtin.command: grep -iw ^log_group /etc/audit/auditd.conf
    check_mode: false
    failed_when: false
    changed_when: false
    register: log_group_exists
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0640 or Less Permissive - Get audit log group
      from /etc/audit/auditd.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0640 or Less Permissive - Get audit log group from /etc/audit/auditd.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_audit | default(true) |
    bool)
- block:
  - name: System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log group
    ansible.builtin.set_fact:
      log_group: '{{ (log_group_exists.stdout | default('''') | split('' '') | last) | default(''root'', true) }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log group''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log group'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_audit | default(true) |
    bool)
- block:
  - name: System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log file permissions
    ansible.builtin.file:
      path: '{{ log_file_path }}'
      state: file
      mode: '{{ ''0600'' if log_group == ''root'' else ''0640'' }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log file
      permissions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''System Audit Logs Must Have Mode 0640 or Less Permissive - Set audit log file permissions'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_var_log_audit | default(true) |
    bool)
- block:
  - name: Set architecture for audit chmod tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit chmod tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit chmod tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_chmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chmod for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chmod for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chmod for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_chmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chmod for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chmod for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chmod for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_chmod | default(true)
    | bool)
- block:
  - name: Set architecture for audit chown tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit chown tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit chown tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_chown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chown for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chown for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chown for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_chown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chown for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chown for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chown for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_chown | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchmod tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchmod tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchmod tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmod for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmod for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmod for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmod for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmod for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmod for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchmod | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchmodat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmodat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchmodat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchmodat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchmodat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmodat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmodat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmodat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmodat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchmodat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmodat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654155
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmodat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmodat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmodat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchmodat | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchown tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchown tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchown tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchown for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchown for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchown for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchown for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchown for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchown for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchown | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchownat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchownat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchownat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchownat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchownat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchownat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchownat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchownat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchownat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchownat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchownat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchownat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchownat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchownat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fchownat | default(true)
    | bool)
- block:
  - name: Set architecture for audit fremovexattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fremovexattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fremovexattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fremovexattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fremovexattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fremovexattr for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fremovexattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fremovexattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fremovexattr for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fremovexattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit fsetxattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fsetxattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fsetxattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fsetxattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fsetxattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fsetxattr for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fsetxattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fsetxattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fsetxattr for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_fsetxattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit lchown tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit lchown tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit lchown tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lchown for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lchown for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lchown for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lchown for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654160
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lchown for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lchown for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lchown | default(true)
    | bool)
- block:
  - name: Set architecture for audit lremovexattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit lremovexattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit lremovexattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lremovexattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lremovexattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lremovexattr for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lremovexattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lremovexattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lremovexattr for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lremovexattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit lsetxattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit lsetxattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit lsetxattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lsetxattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lsetxattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lsetxattr for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lsetxattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lsetxattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lsetxattr for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_lsetxattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit removexattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_removexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit removexattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit removexattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_removexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for removexattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_removexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for removexattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for removexattr for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_removexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for removexattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_removexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for removexattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for removexattr for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_removexattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit setxattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_setxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit setxattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit setxattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_setxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for setxattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_setxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for setxattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for setxattr for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_setxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for setxattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid=0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid=0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid=0 -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - DISA-STIG-UBTU-22-654180
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_setxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for setxattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for setxattr for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_dac_modification_setxattr | default(true)
    | bool)
- block:
  - name: Record Any Attempts to Run chacl - Perform remediation of Audit rules for /usr/bin/chacl
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chacl -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/chacl -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chacl -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chacl -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/chacl
          -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chacl -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654015
    - audit_rules_execution_chacl
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Any Attempts to Run chacl - Perform remediation of Audit rules for
      /usr/bin/chacl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Any Attempts to Run chacl - Perform remediation of Audit rules for /usr/bin/chacl'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_execution_chacl | default(true) | bool)
- block:
  - name: Record Any Attempts to Run setfacl - Perform remediation of Audit rules for /usr/bin/setfacl
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/setfacl -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/setfacl -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/setfacl -F perm=x -F auid>=1000 -F auid!=unset -F
          key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/setfacl -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/setfacl
          -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/setfacl -F perm=x -F auid>=1000 -F auid!=unset -F
          key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654085
    - audit_rules_execution_setfacl
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Any Attempts to Run setfacl - Perform remediation of Audit rules for
      /usr/bin/setfacl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Any Attempts to Run setfacl - Perform remediation of Audit rules for /usr/bin/setfacl'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_execution_setfacl | default(true) |
    bool)
- block:
  - name: Record Any Attempts to Run chcon - Perform remediation of Audit rules for /usr/bin/chcon
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chcon -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chcon -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/chcon
          -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654025
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - audit_rules_execution_chcon
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Any Attempts to Run chcon - Perform remediation of Audit rules for
      /usr/bin/chcon'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Any Attempts to Run chcon - Perform remediation of Audit rules for /usr/bin/chcon'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_execution_chcon | default(true) | bool)
- block:
  - name: Set architecture for audit rename tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_rename
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit rename tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit rename tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_rename | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for rename for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_rename
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for rename for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for rename for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_rename | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for rename for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_rename
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for rename for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for rename for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_rename | default(true)
    | bool)
- block:
  - name: Set architecture for audit renameat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_renameat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit renameat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit renameat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_renameat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for renameat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_renameat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for renameat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for renameat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_renameat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for renameat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_renameat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for renameat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for renameat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_renameat | default(true)
    | bool)
- block:
  - name: Set architecture for audit unlink tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlink
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit unlink tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit unlink tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_unlink | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlink for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlink
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlink for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlink for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_unlink | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlink for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlink
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlink for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlink for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_unlink | default(true)
    | bool)
- block:
  - name: Set architecture for audit unlinkat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlinkat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit unlinkat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit unlinkat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_unlinkat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlinkat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlinkat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlinkat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlinkat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_unlinkat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlinkat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654185
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlinkat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlinkat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlinkat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_file_deletion_events_unlinkat | default(true)
    | bool)
- block:
  - name: Set architecture for audit creat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit creat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit creat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_creat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EACCES for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_creat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EACCES for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_creat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EPERM for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EPERM for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_creat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EPERM for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EPERM for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_creat
    | default(true) | bool)
- block:
  - name: Set architecture for audit ftruncate tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit ftruncate tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit ftruncate tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EACCES for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EACCES for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EPERM for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EPERM for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EPERM for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EPERM for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Set architecture for audit open tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit open tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit open tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_open
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EACCES for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EACCES for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_open
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EACCES for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EACCES for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_open
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EPERM for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EPERM for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_open
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EPERM for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EPERM for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_open
    | default(true) | bool)
- block:
  - name: Set architecture for audit openat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit openat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit openat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EACCES for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EACCES for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EPERM for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EPERM for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EPERM for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EPERM for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Set architecture for audit truncate tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit truncate tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit truncate tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EACCES for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EACCES for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EPERM for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EPERM for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654165
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EPERM for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EPERM for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Unloading - delete_module - Set architecture for audit ['delete_module']
      tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654170
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_delete
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Unloading - delete_module
      - Set architecture for audit [''delete_module''] tasks" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Unloading - delete_module - Set architecture for
        audit [''delete_module''] tasks" raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_delete | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Unloading - delete_module - Perform remediation of Audit rules
      for ['delete_module'] for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of delete_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/modules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/modules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of delete_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654170
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_delete
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Unloading - delete_module
      - Perform remediation of Audit rules for [''delete_module''] for 32bit platform" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Unloading - delete_module - Perform remediation
        of Audit rules for [''delete_module''] for 32bit platform" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_delete | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Unloading - delete_module - Perform remediation of Audit rules
      for ['delete_module'] for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of delete_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/modules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/modules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of delete_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654170
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_delete
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Unloading - delete_module
      - Perform remediation of Audit rules for [''delete_module''] for 64bit platform" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Unloading - delete_module - Perform remediation
        of Audit rules for [''delete_module''] for 64bit platform" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_delete | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Loading and Unloading - finit_module - Set architecture for
      audit ['finit_module'] tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654175
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_finit
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Loading and Unloading -
      finit_module - Set architecture for audit [''finit_module''] tasks" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Loading and Unloading - finit_module - Set architecture
        for audit [''finit_module''] tasks" raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_finit | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Loading and Unloading - finit_module - Perform remediation of
      Audit rules for ['finit_module'] for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of finit_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/modules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/modules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of finit_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654175
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_finit
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Loading and Unloading -
      finit_module - Perform remediation of Audit rules for [''finit_module''] for 32bit platform" (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Loading and Unloading - finit_module - Perform
        remediation of Audit rules for [''finit_module''] for 32bit platform" raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_finit | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Loading and Unloading - finit_module - Perform remediation of
      Audit rules for ['finit_module'] for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of finit_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/modules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/modules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of finit_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654175
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_finit
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Loading and Unloading -
      finit_module - Perform remediation of Audit rules for [''finit_module''] for 64bit platform" (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Loading and Unloading - finit_module - Perform
        remediation of Audit rules for [''finit_module''] for 64bit platform" raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_finit | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Loading - init_module - Set architecture for audit ['init_module']
      tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - DISA-STIG-UBTU-22-654175
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_init
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Loading - init_module -
      Set architecture for audit [''init_module''] tasks" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Loading - init_module - Set architecture for audit
        [''init_module''] tasks" raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_init | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Loading - init_module - Perform remediation of Audit rules for
      ['init_module'] for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of init_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/modules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/modules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of init_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654175
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_init
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Loading - init_module -
      Perform remediation of Audit rules for [''init_module''] for 32bit platform" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Loading - init_module - Perform remediation of
        Audit rules for [''init_module''] for 32bit platform" raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_init | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on Kernel Module Loading - init_module - Perform remediation of Audit rules for
      ['init_module'] for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of init_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/modules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/modules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - create_module
        - delete_module
        - finit_module
        - init_module
        - query_module
    - name: Check existence of init_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=modules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - DISA-STIG-UBTU-22-654175
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_init
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in "Ensure auditd Collects Information on Kernel Module Loading - init_module -
      Perform remediation of Audit rules for [''init_module''] for 64bit platform" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure auditd Collects Information on Kernel Module Loading - init_module - Perform remediation of
        Audit rules for [''init_module''] for 64bit platform" raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_kernel_module_loading_init | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+{{ var_accounts_passwords_pam_faillock_dir }}\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch
      rule for {{ var_accounts_passwords_pam_faillock_dir }} already exists in /etc/audit/rules.d/'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Search /etc/audit/rules.d for other rules with specified
      key logins
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)logins$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Search /etc/audit/rules.d
      for other rules with specified key logins'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Search /etc/audit/rules.d for other
        rules with specified key logins'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Use /etc/audit/rules.d/logins.rules as the recipient
      for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/logins.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Use /etc/audit/rules.d/logins.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Use /etc/audit/rules.d/logins.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Use matched file as the recipient for
        the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w {{ var_accounts_passwords_pam_faillock_dir }} -p wa -k logins
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule
      for {{ var_accounts_passwords_pam_faillock_dir }} in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+{{ var_accounts_passwords_pam_faillock_dir }}\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch
      rule for {{ var_accounts_passwords_pam_faillock_dir }} already exists in /etc/audit/audit.rules'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w {{ var_accounts_passwords_pam_faillock_dir }} -p wa -k logins
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule
      for {{ var_accounts_passwords_pam_faillock_dir }} in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/var/log/lastlog\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654215
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch
      rule for /var/log/lastlog already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_lastlog | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Search /etc/audit/rules.d for other rules with specified
      key logins
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)logins$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654215
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Search /etc/audit/rules.d
      for other rules with specified key logins'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Search /etc/audit/rules.d for other
        rules with specified key logins'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_lastlog | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Use /etc/audit/rules.d/logins.rules as the recipient
      for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/logins.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654215
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Use /etc/audit/rules.d/logins.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Use /etc/audit/rules.d/logins.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_lastlog | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654215
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Use matched file
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Use matched file as the recipient for
        the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_lastlog | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /var/log/lastlog -p wa -k logins
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654215
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule
      for /var/log/lastlog in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_lastlog | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/var/log/lastlog\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654215
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch
      rule for /var/log/lastlog already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_lastlog | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /var/log/lastlog -p wa -k logins
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - DISA-STIG-UBTU-22-654215
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule
      for /var/log/lastlog in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_login_events_lastlog | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - kmod - Perform remediation of Audit rules
      for /usr/bin/kmod
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/kmod -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/kmod -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/kmod -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/kmod -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/kmod
          -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/kmod -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654055
    - NIST-800-53-AU-12(a)
    - NIST-800-53-AU-12.1(ii)
    - NIST-800-53-AU-12.1(iv)AU-12(c)
    - NIST-800-53-AU-3
    - NIST-800-53-AU-3.1
    - NIST-800-53-MA-4(1)(a)
    - audit_rules_privileged_commands_kmod
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - kmod
      - Perform remediation of Audit rules for /usr/bin/kmod'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - kmod - Perform remediation
        of Audit rules for /usr/bin/kmod'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_privileged_commands_kmod | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - usermod - Perform remediation of Audit rules
      for /usr/sbin/usermod
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/sbin/usermod -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/sbin/usermod -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/sbin/usermod -F perm=x -F auid>=1000 -F auid!=unset -F
          key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/sbin/usermod -F perm=x -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/sbin/usermod
          -F perm=x -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/sbin/usermod -F perm=x -F auid>=1000 -F auid!=unset -F
          key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-654125
    - audit_rules_privileged_commands_usermod
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - usermod
      - Perform remediation of Audit rules for /usr/sbin/usermod'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - usermod - Perform remediation
        of Audit rules for /usr/sbin/usermod'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_privileged_commands_usermod | default(true)
    | bool)
- block:
  - name: Set architecture for audit tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_adjtimex
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_adjtimex | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for adjtimex for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of adjtimex in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of adjtimex in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_adjtimex
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for adjtimex for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for adjtimex for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_adjtimex | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for adjtimex for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
    - name: Check existence of adjtimex in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of adjtimex in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_adjtimex
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for adjtimex for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for adjtimex for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_adjtimex | default(true) | bool)
- block:
  - name: Set architecture for audit tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_clock_settime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_clock_settime | default(true) |
    bool)
- block:
  - name: Perform remediation of Audit rules for clock_settime for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/time-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/time-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_clock_settime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for clock_settime for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for clock_settime for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_clock_settime | default(true) |
    bool)
- block:
  - name: Perform remediation of Audit rules for clock_settime for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/time-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/time-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_clock_settime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for clock_settime for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for clock_settime for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_clock_settime | default(true) |
    bool)
- block:
  - name: Set architecture for audit tasks
    set_fact:
      audit_arch: b64
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_settimeofday
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_settimeofday | default(true) |
    bool)
- block:
  - name: Perform remediation of Audit rules for settimeofday for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_settimeofday
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for settimeofday for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for settimeofday for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_settimeofday | default(true) |
    bool)
- block:
  - name: Perform remediation of Audit rules for settimeofday for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_settimeofday
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for settimeofday for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for settimeofday for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_settimeofday | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/localtime\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime
      already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_watch_localtime | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter the localtime File - Search /etc/audit/rules.d for other rules with specified key audit_time_rules
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_time_rules$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Search /etc/audit/rules.d for
      other rules with specified key audit_time_rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Search /etc/audit/rules.d for other rules with specified
        key audit_time_rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_watch_localtime | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter the localtime File - Use /etc/audit/rules.d/audit_time_rules.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_time_rules.rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Use /etc/audit/rules.d/audit_time_rules.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Use /etc/audit/rules.d/audit_time_rules.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_watch_localtime | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter the localtime File - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Use matched file as the recipient
      for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Use matched file as the recipient for the rule'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_watch_localtime | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/localtime -p wa -k audit_time_rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_watch_localtime | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/localtime\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime
      already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_watch_localtime | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/localtime -p wa -k audit_time_rules
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"auditd" in ansible_facts.packages'
    - '"linux-base" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_audit_rules_time_watch_localtime | default(true)
    | bool)
- block:
  - name: Set the file_groupownership_audit_binaries_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupownership_audit_binaries_newgroup: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_audit_binaries_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_audit_binaries_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/augenrules'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/audisp-syslog
    ansible.builtin.stat:
      path: /sbin/audisp-syslog
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audisp-syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audisp-syslog'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/audisp-syslog
    ansible.builtin.file:
      path: /sbin/audisp-syslog
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/audisp-syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/audisp-syslog'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/audisp-remote
    ansible.builtin.stat:
      path: /sbin/audisp-remote
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audisp-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audisp-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/audisp-remote
    ansible.builtin.file:
      path: /sbin/audisp-remote
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/audisp-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/audisp-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/audispd-zos-remote
    ansible.builtin.stat:
      path: /sbin/audispd-zos-remote
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd-zos-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd-zos-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/audispd-zos-remote
    ansible.builtin.file:
      path: /sbin/audispd-zos-remote
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/audispd-zos-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/audispd-zos-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Set the file_ownership_audit_binaries_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_audit_binaries_newown: '0'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_audit_binaries_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_audit_binaries_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/audisp-syslog
    ansible.builtin.stat:
      path: /sbin/audisp-syslog
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audisp-syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audisp-syslog'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/audisp-syslog
    ansible.builtin.file:
      path: /sbin/audisp-syslog
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/audisp-syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/audisp-syslog'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/audisp-remote
    ansible.builtin.stat:
      path: /sbin/audisp-remote
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audisp-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audisp-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/audisp-remote
    ansible.builtin.file:
      path: /sbin/audisp-remote
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/audisp-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/audisp-remote'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/audispd-zos-remote
    ansible.builtin.stat:
      path: /sbin/audispd-zos-remote
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd-zos-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd-zos-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure owner on /sbin/audispd-zos-remote
    ansible.builtin.file:
      path: /sbin/audispd-zos-remote
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232110
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/audispd-zos-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/audispd-zos-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_ownership_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/auditctl'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/auditctl'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/aureport'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/aureport'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/ausearch'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/ausearch'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/autrace'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/autrace'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/auditd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/augenrules'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/augenrules'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/audisp-syslog
    ansible.builtin.stat:
      path: /sbin/audisp-syslog
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audisp-syslog'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audisp-syslog'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/audisp-syslog
    ansible.builtin.file:
      path: /sbin/audisp-syslog
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/audisp-syslog'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/audisp-syslog'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/audisp-remote
    ansible.builtin.stat:
      path: /sbin/audisp-remote
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audisp-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audisp-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/audisp-remote
    ansible.builtin.file:
      path: /sbin/audisp-remote
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/audisp-remote'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/audisp-remote'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/audispd-zos-remote
    ansible.builtin.stat:
      path: /sbin/audispd-zos-remote
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd-zos-remote'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd-zos-remote'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/audispd-zos-remote
    ansible.builtin.file:
      path: /sbin/audispd-zos-remote
      mode: u-s,g-ws,o-wt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-232035
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/audispd-zos-remote'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/audispd-zos-remote'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /etc/audit/auditd.conf
    ansible.builtin.stat:
      path: /etc/audit/auditd.conf
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_auditd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/audit/auditd.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/audit/auditd.conf'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_audit_auditd | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/auditd.conf
    ansible.builtin.file:
      path: /etc/audit/auditd.conf
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_auditd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/auditd.conf'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/auditd.conf'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_audit_auditd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/audit/audit.rules
    ansible.builtin.stat:
      path: /etc/audit/audit.rules
    register: file_exists
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653065
    - configure_strategy
    - file_permissions_etc_audit_rules
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/audit/audit.rules'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_audit_rules | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/audit.rules
    ansible.builtin.file:
      path: /etc/audit/audit.rules
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"linux-base" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - DISA-STIG-UBTU-22-653065
    - configure_strategy
    - file_permissions_etc_audit_rules
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/audit.rules'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/audit/audit.rules'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_audit_rules | default(true)
    | bool)
- block:
  - name: Find /etc/audit/rules.d/ file(s)
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -perm /u+xs,g+xwrs,o+xwrt  -type f -regextype posix-extended
      -regex "^.*rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_rulesd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_audit_rulesd | default(true)
    | bool)
- block:
  - name: Set permissions for /etc/audit/rules.d/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xwrs,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"linux-base" in ansible_facts.packages'
    tags:
    - DISA-STIG-UBTU-22-653065
    - NIST-800-53-AU-12(b)
    - configure_strategy
    - file_permissions_etc_audit_rulesd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/audit/rules.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/audit/rules.d/ file(s)'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (ubuntu22cis_file_permissions_etc_audit_rulesd | default(true)
    | bool)
